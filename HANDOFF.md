# TowerWar — 인수인계

최종 갱신: 2026-08-01

---

## -41. 등급 아우라 + 등급 표식 (2026-08-03, 사용자 선택)

"비싼 유닛을 사서 착용하면 직관적으로 보여야 한다"에 넷을 제시했고 사용자가 **① 아우라
+ ② 표식**을 골랐다.

### 왜 필요했나

**그림 자체에 세기가 없다.** 다섯 종이 같은 캐릭터를 하의 색만 바꿔 구운 것이라
흰·금·초록·보라 사이에 순서가 없다. 크기 사다리(§-36)는 폭이 1.0~1.2라 **나란히 놓아야**
읽히고 실전에서는 거의 안 보인다.

### 아우라 (`drawTierAura`)

캐릭터 **뒤에** 깐다 — 위에 얹으면 재킷 색을 덮는데 그게 "누구 편인가"의 유일한
신호다. 등급이 높을수록 넓고 진하다:

| 등급 | 반경 | 알파 |
|---|---|---|
| 0 (기본) | **안 그림** | — |
| 1 (흰) | 8.5 | 0.28 |
| 3 (초록) | 11.1 | 0.39 |
| 5 (무지개) | 14.4 | 0.50 |

**색은 그 종류의 하의 색(`accent`)이다.** 화면에 이미 보이는 색이라 따로 배울 것이
없다. 무지개만 색이 돈다 — 정지색이면 다른 등급과 같은 물건으로 보인다.

`beergang_rainbow` 전용이던 `drawRainbowAura` 를 전 등급으로 넓힌 것이다.

### 표식 (`drawTierPips`)

머리 위에 등급 수만큼 마름모. 아우라는 "센 놈이다"까지만 말하고 **몇 등급인지는 못
세기** 때문이다.

**폭이 캐릭터를 넘으면 안 된다.** 고정 크기로 찍었더니 5개짜리가 29.1px인데 유닛 폭이
20.2px이었다 — 옆 유닛 표식과 섞인다. 줄 폭 공식(`반지름·(2.6n − 0.6)`)을 뒤집어
크기를 줄인다:

| 등급 | 표식 폭 | 유닛 폭 |
|---|---|---|
| 1 | 4.0 | 17.4 |
| 3 | 15.4 | 18.5 |
| 5 | 19.3 | 20.2 |

**기본 등급은 아우라도 표식도 없다.** 판에 유닛이 수십 기라 가장 흔한 것에까지 찍으면
화면이 점으로 덮인다.

### 상점도 같은 표식을 쓴다

`◆` 를 등급 수만큼. **판에서 머리 위에 찍히는 것과 개수가 같다** — 상점과 화면이 다른
말을 하면 무엇을 산 것인지 알 수가 없다.

### 검증

- 서버 **127/127**, `tsc` 통과
- 상점: 빈칸 → `◆` → `◆◆` → `◆◆◆` → `◆◆◆◆` → `◆◆◆◆◆`, 색이 하의 색과 일치
- 표식 폭이 전 등급에서 캐릭터 폭 안 (위 표)
- **판 안의 그림은 눈으로 못 봤다** — 캔버스 rAF 가 멈춰 있었다 (§-30의 그 문제).
  좌표·크기·색은 계산으로 맞췄지만 아우라가 실제로 어떻게 보이는지는 실기 확인 필요

---

## -40. 영어 기본 + 언어 설정, 격자 제거 (2026-08-03, 사용자 지시)

### 사전 하나로 몰았다

`game/src/i18n.ts`. 문자열이 씬마다 흩어져 있으면 언어를 하나 더 넣을 때 **어디를
안 고쳤는지 알 수가 없다.** 표가 하나면 빠진 키가 타입 검사에서 걸린다
(`Strings` 를 두 언어에 똑같이 요구한다).

**기본은 영어다.** 기기 언어를 안 본다 — 브라우저 언어를 읽으면 한국 기기에서 저절로
한국어가 돼 "기본 영어"라는 지시가 무너진다. 고른 값은 `towerwar.lang` 에 남는다.

### 안 번역한 것

- **콘솔 경고**: 개발자가 읽는다. 번역하면 검색이 안 된다
- **`throw` 하는 내부 오류**(`…DOM이 예상과 다릅니다`): 화면에 안 나온다. 나오면 버그다
- **`server.js` 오류**: 단일 파일 샌드박스라 이 모듈을 못 읽는다

### 봇 이름도 언어를 따라간다

**영어 화면에 `차가운늑대60` 이 뜨면 그게 곧 "이 상대는 사람이 아니다"다** (§-7은
안 알리기로 했다). 낱말 목록을 사전에 넣고 조합 함수(`botJoin`)도 언어별로 뒀다.

### 카드 글자는 매번 다시 쓴다 (실제로 낸 버그)

상점 카드는 생성자에서 `innerHTML` 로 한 번만 만들어진다. 이름·스탯·설명을 거기서
넣었더니 **언어를 바꿔도 처음 언어가 그대로 남았다.** `data-role` 로 빼고 `render()`
에서 매번 채운다 — `state` 가 이미 그렇게 돼 있었다.

### 설정 화면

로비에 `설정` 버튼. **매치 중에는 못 연다** — 판 위 설정 창은 봇전에서만 판을 멈춰서
상대가 봇인지 알려 줬고 그래서 §-38에서 걷어냈다.

언어를 바꾸면 정적 문자열을 다시 칠하고(`applyStaticText`) 지금 씬의 `enter()` 를 다시
부른다. **새로고침을 안 시킨다** — 매칭 중이거나 상점을 보고 있었을 수 있고, 언어 하나
바꾸는 값으로는 너무 비싸다.

각 언어를 **그 언어로** 적는다(`English` / `한국어`). 한국어 화면의 `Korean` 은 아무
도움이 안 된다.

### 격자 제거

`drawField` 의 50논리px 간격 옅은 선을 걷어냈다. 건물 스프라이트가 들어온 뒤로는 판이
지도가 아니라 모눈종이로 읽혔다. **중앙선은 남겼다** — `maps.ts` 가 그 축으로 거울을
찍으므로 대칭을 눈으로 확인하는 선이다.

### 검증

- 서버 **127/127**, `tsc` 통과
- 실기: 처음 켜면 영어(`Quick Match` / `Draw supply lines…`), 설정에서 한국어로 바꾸면
  로비·상점·유닛 이름·스탯이 전부 따라온다(`비어갱` / `체력 1.5 · 공격력 1.5`).
  `towerwar.lang` 에 저장돼 다시 켜도 유지
- 격자: 그리는 코드와 `GRID` 상수 모두 제거 확인 (캔버스 픽셀은 이 시점에 rAF 가
  멈춰 있어 못 읽었다 — §-30의 그 문제다)

---

## -39. 봇이 한 단계 아래를 입는다 + 배속 버튼 자리 (2026-08-03, 사용자 지시)

### 배속 버튼 — 우상단, 막대 아래

`top: 72px`. 판때기가 66px이고 전선 막대가 46~55px이라 그 밑에서 시작해야 안 겹친다.
실측: `y 72~116`, HUD 겹침 0.

§-38에서 좌하단으로 옮겼던 것을 되돌린 것이다 — 그때는 "위쪽은 전부 HUD"로 봤는데,
**판때기 아래는 비어 있다.** 375×812에서 판은 y=113부터라 3px만 스치고, 375×667에서는
판 위쪽 구석을 조금 덮는다 (버튼이라 넘길 만하다).

### 봇이 사람보다 한 단계 아래를 입는다

`stepDownKind(kind)` — 힘 오름차순 목록(`POWER_ORDER`)에서 한 칸 내린다.
**유료 종류도 포함한다**: 무지개(4) → 보라(3). 가장 약한 것이면 더 내려갈 곳이 없어
그대로다.

```
비어갱(1)      → 비어갱(1)        더 아래 없음
흰(1.5)        → 비어갱(1)
금(2)          → 흰(1.5)
초록(2.5)      → 금(2)
보라(3)        → 초록(2.5)
무지개(4)      → 보라(3)
```

**그리는 종류와 `botModsFor` 의 힘이 같은 함수에서 나온다.** 어긋나면 화면이 거짓말을
한다 — §-31에서 같은 이유로 봇을 내 종류로 그리게 바꿨던 자리다.

### ⚠ 이 한 줄이 난이도를 통째로 바꾼다

§-0.75의 실측표가 이유다. 충돌이 `Math.min(a.power, b.power)` 라 **조금이라도 높은
쪽은 정면 교환에서 절대 먼저 안 죽는다.** 사람 전투력 2 기준 사람 승률:

| 봇 전투력 | 사람 승률 |
|---|---|
| 1.85 | 95% |
| 1.95 | 95% |
| **2.0 (동등)** | **45%** |

**한 단계만 낮춰도 사람 승률이 95~100%로 뛴다.** 지금까지 66~68%(`BOT_SPEED_LAG = 0.5`)에
맞춰져 있었고, §-29의 점수 천장(1123)은 그 승률 위에 서 있는 숫자다.

**둘 다 다시 재야 한다.** 사용자가 직접 플레이해 정할 영역이라(§8) 여기서는 안 건드렸다 —
`BOT_SPEED_LAG` 를 낮추거나(0.5 → 0에 가깝게) `power` 표를 좁히는 것이 손잡이다.

### 검증

- 서버 **127/127**, `tsc` 통과
- 한 단계 아래 표(위)를 실제 함수로 전수 확인
- 실기: 보라 착용 상태에서 봇전 → 내 힘 3 / 봇 힘 2.5, 봇 표시도 초록
- 배속 버튼 `y 72~116`, HUD(0~66) 겹침 0

---

## -38. 착용 버그 + 캔버스 위 버튼 재설계 (2026-08-03, 사용자 지시)

### 유닛 착용이 안 되던 이유 — 디버그 플래그를 한쪽에만 켰다

`DEBUG_UNLOCK_ALL` 을 **클라이언트에만** 넣었다. 그래서:

1. 상점은 `ownsUnitKind`(플래그 반영)로 물어 **'착용하기'** 를 띄우고 버튼을 살렸다
2. 그런데 `store.pickUnit` 은 `account.ownedUnits` 를 **직접** 봤다 → `owned=false`
3. → `buyUnitKind` 호출 → 서버: 코인이 모자랍니다 → **눌러도 아무 일이 안 일어난다**

고친 곳 둘:
- `pickUnit` 이 `ownsUnitKind` 로 묻는다. 직접 `ownedUnits` 를 보면 디버그 플래그도
  유료 소유(`entitlements`)도 빠진다
- **`server.js` 에도 같은 플래그를 뒀다.** 소유 판정이 서버에 있으므로(§-10) 한쪽만
  켜면 반드시 이 버그가 난다. 두 파일 주석에 "같이 켜고 끈다"를 박아 뒀다

**하네스는 플래그를 꺼서 돌린다** (`__TW_NO_DEBUG_UNLOCK`). 켜 둔 채로 재면
"안 산 것을 못 입는다" 계열 검사가 통째로 통과해 아무것도 증명하지 못한다.

### 배속 버튼이 HUD를 덮고 있었다

우상단(top 14, 52×44 → `309~361 × 14~58`)에 뒀는데, 그 자리는 이미 HUD가 쓴다:

| | 자리 |
|---|---|
| 배속 버튼 | x 309~361, y 14~58 |
| 상대 아바타 | x 341~363 |
| 전선 막대 | x 12~363, y 46~55 |

**위쪽은 통째로 HUD 것이다**(판때기 66px). 그래서 **좌하단**으로 옮겼다 — 항복(우하단)과
좌우로 갈라 둔다. 하나는 판을 끝내고 하나는 빠르게 하니 붙여 두면 잘못 누른다.

### 두 버튼을 한 벌로

"게임이랑 따로 논다"는 지적. 원인은 **둘이 서로 다른 물건이었다는 것**이다 — 크기도
모서리도 글꼴도 달라서 웹 버튼 두 개가 게임 위에 떠 있는 것으로 보였다.

같은 판때기에서 잘라 낸 두 조각처럼 바꿨다 (`.hud-btn` 공통 + 색만 다른 변형):

- 바탕이 **HUD 판때기와 같은 색**(`rgba(7,11,17,.92)`). 화면 위 겹침은 같은 재질이어야 한다
- **아래로 두툼한 그림자를 깔아 세운다.** Tiny Swords 건물이 3/4 시점으로 서 있는
  그림이라 납작한 사각형은 그 위에서 종잇조각처럼 뜬다
- 누르면 그 두께만큼 실제로 내려앉는다 (`:active` 에서 `translateY(3px)` + 그림자 축소)
- 모서리 **8px**. 10px 이상은 웹 버튼으로 읽힌다 — 스프라이트가 각졌다
- 표식을 앞에 붙였다: 항복 `⚑`, 배속 `»`. 글자만 있으면 급할 때 안 읽힌다

### 검증

- 서버 **127/127**, `tsc` 통과
- 착용: 보라(코인) `beergang` → `beergang_purple`, 무지개(유료) → `beergang_rainbow`
  둘 다 실제로 갈아입힌다. 카드도 `착용 중` 으로 바뀐다
- 자리(375×812): 배속 `14~82`, 항복 `290~361`, 둘 다 y `754~798`.
  **HUD(0~66)와 겹침 0, 서로도 겹침 0.** 961×910에서도 같다
- 스타일: 두 버튼이 같은 바탕·같은 높이(44px)·같은 모서리(8px), 표식 적용 확인

---

## -37. 배속 시스템 (2026-08-03, 사용자 설계)

사용자 설계: **"pvp에서 상대도 배속을 가지고 있으면 둘다 누르면 x2배가 되는거고,
다른 상대가 누를때 동기화돼서 숫자가 계쏙 바뀌고"**.

### 이 설계라서 된다

§-36에서 "배속은 락스텝과 안 맞는다"고 적었는데 **그건 한쪽만 빨라지는 경우였다.**
사용자 설계는 **양쪽이 언제나 같은 배속**이다:

```
아무도 안 켬   1.0배
한 명이 켬     1.5배      (켠 사람만이 아니라 판 전체가)
둘 다 켬       2.0배
```

**시뮬레이션은 한 글자도 안 바뀐다.** 바뀌는 것은 `MatchScene.frame` 이 누산기에
넣는 값뿐이다 (`dt * tempoScaleOf(state)`). 틱 하나가 계산하는 내용은 그대로라
`step()` 의 결과가 배속과 무관하다.

**실측으로 확인했다**: 같은 시드로 두 판을 돌리고 한쪽만 배속을 켰을 때
**틱 300개 동안 상태가 완전히 동일**했다. 락스텝 검증 하네스도 10/10.

### 권한은 `mods` 로 흘린다 (클라이언트가 아니라)

`setTempo` 를 무시할지는 **양쪽이 똑같이** 판단해야 한다. "나는 못 켜니까 안 보낸다"로만
막으면 조작된 클라이언트가 보낸 순간 그쪽만 배속이 걸려 갈라진다.

그래서 `PlayerMods.canTempo` 를 두고 **서버가 판 시작에 내려준다**
(`MatchSetup.tempo` ← 룸 상태 `tempo` ← 계정 `entitlements`). `applyCommands` 가
그 값을 보고 못 켜는 사람의 명령을 **양쪽에서 똑같이** 버린다.

봇에게는 안 준다 — 사람이 안 켰는데 판이 빨라지면 살 이유가 없다.

### 버튼

우상단(`.tempo-btn`). 항복(우하단)과 화면 반대편이다 — 하나는 판을 끝내고 하나는
빠르게 하니 붙여 두면 잘못 누른다.

**판 전체 배속을 적는다**: 상대가 켜도 숫자가 바뀐다. 내가 켠 상태는 테두리로 따로
표시한다 — 숫자만으로는 누가 켰는지 알 수 없다. 못 켜는 사람에게는 **버튼이 안 보인다**.

### 아직 안 한 것 — 입력 지연

2배속이면 `INPUT_DELAY_TICKS`(12틱)가 현실 **0.2초**로 줄어든다(원래 0.4초).
왕복 지연이 그보다 크면 판이 자꾸 "상대를 기다리는 중"에서 멈칫한다.

지금은 **로컬 백엔드(지연 0)에서만 확인했다.** 실서버에서 재고 필요하면 배속에 맞춰
지연 틱을 늘리면 되는데, **줄이는 방향은 위험하다** — `place()` 가 지나간 틱의 배치를
버리므로 지연이 줄면 배치가 과거로 예약돼 사라진다. 늘리기만 하는 래칫으로 갈 것.

### 시간당 보상 2배는 의도다

사용자 결정: **"배속을 샀으면 더 빠른 시간에 보상을 누려야지"**. 판은 여전히
`MATCH_TIME`(90 게임초)이고 2배속이면 현실 45초에 끝난다. 전투에는 이득이 없다 —
양쪽이 같이 빨라지므로 대칭이다.

### 디버그 해금

`account.ts` 의 **`DEBUG_UNLOCK_ALL = true`** (사용자 지시). 유닛 5종 + 무지개 + 배속이
전부 열린 것으로 보인다. **화면에만 먹는다** — 서버는 이 값을 모르므로 온라인에서
실제로 입으려 하면 `selectUnitKind` 가 거절한다. 봇전·오프라인에서는 그대로 먹는다.

**출시 전에 `false` 로 되돌릴 것.** 그 한 줄이다.

---

## -36. 유닛 강함을 크기로 + VX 유료 결제 배관 (2026-08-03, 사용자 지시)

사용자 지시 셋 중 둘째·셋째:
**"상점에 유닛들이 강해지는게 외형만 보고도 판단이 서야해"**,
**"vx라는 verse8의 유료결제 시스템을 이용한 아이템도 나와야해(ex: 제일 강한 무지개
비어갱, 배속 시스템)"**.

### 색에는 순서가 없다 — 크기에는 있다

다섯 종이 같은 캐릭터를 **하의·머리 색만 바꿔 구운 것**이라 흰·금·초록·보라 사이에
세다/약하다가 안 읽힌다. `sizeFactorOf(power)` 를 두고 **상점과 실제 판이 같은 함수를
쓴다** — 상점에서만 크게 그리면 산 것이 판에서는 똑같아 보인다(화면이 거짓말).

폭은 **1.00~1.20으로 좁다.** 판에서 유닛은 26px이고 경로 위에 여럿이 붙어 다녀서,
1.5배까지 벌리면 센 유닛 줄이 겹쳐 몇 기인지 안 보인다.

크기 차이만으로는 옆에 안 놓인 두 카드를 못 비교해서 **힘 막대**를 같이 뒀다
(청록→호박. 진영색 파랑·빨강을 피한다).

실측(375px): 미리보기 44·46·48·51·53px, 막대 29·44·59·72·87px — 둘 다 단조 증가.

### 무지개 비어갱은 그림이 없다

**베이커 원본(GLB)이 저장소에 없어 새로 구울 수 없다.** `assets-src/` 의 zip은 이미
뽑아낸 PNG였다(§-27 작업 때 확인). 그래서:

- `spriteOf: 'beergang'` — 기본 스프라이트를 빌린다 (`spriteKindOf`)
- 구분은 **코드로 그리는 무지개 아우라**가 맡는다. 판에서는 캔버스
  (`drawRainbowAura`), 상점에서는 CSS(`.aura-rainbow`)
- **캐릭터 뒤에 깐다.** 위에 얹으면 재킷 색을 덮는데 그게 "누구 편인가"의 유일한
  신호다 (§-15의 변형색 제약과 같은 이유)

전투력 4로 카탈로그 최상단이다. 그래서 `MAX_UNIT_POWER` 가 3→4가 되고
기존 종류들의 크기 배수도 같이 조정된다.

### VX — 확인한 것만 썼다

`node_modules/@agent8/gameserver` 의 타입 선언에서 **직접 읽은** API:

```
getCrossRampShopUrl(lang?): Promise<string>    결제 창 URL
subscribeAsset(account, cb: (assets: Record<string, number>) => void)
```

**결제는 우리 코드 밖에서 일어난다.** 우리가 하는 것은 창을 열어 주는 것과, 늘어난
자산을 읽는 것뿐이다. 결제가 다른 탭에서 끝나므로 **구독**이다 — 언제 끝나는지 모른다.

`entitlements` 를 계정에 뒀다. **코인으로 산 `ownedUnits` 와 갈라 둔다** — 획득 경로가
다르고, 섞으면 나중에 무엇이 유료였는지 구분이 안 된다. 저장 형식 v10.

**함정 하나**: `ownsUnitKind` 가 `price === 0` 을 "기본 제공"으로 본다. 무지개도
`price: 0` 이라 그대로 두면 **누구나 공짜로 입는다.** 유료 종류는 `price` 를 아예
안 보게 갈랐다. 서버도 `UNIT_PRICES` 와 `PREMIUM_UNITS` 를 갈라 뒀다 — 같은 이유다.

### 자산 id는 비워 뒀다 (지어내면 안 된다)

`net/vx.ts` 의 `ASSET_IDS` 가 비어 있다. 어떤 문자열이 "무지개 비어갱"인지는 Verse8에
상품을 등록해야 정해진다. **비어 있으면 상점에 `준비 중` 으로 나가고 버튼이 죽는다** —
이유를 화면에 적어 둔다(`결제 상품이 아직 등록되지 않았습니다`).

배포 후: ① 대시보드에 상품 등록 → ② `ASSET_IDS` 채우기 → ③ `subscribeAsset` 이 그
id를 실제로 내려주는지 콘솔 확인.

### 남은 구멍 — 서버가 결제를 검증하지 못한다

**`server.js` 샌드박스에 자산을 읽는 API가 없다.** $global/$room/$sender/$lock 어디에도
없고 SDK 타입에도 없다. 그래서 `grantEntitlement` 는 **클라이언트를 믿는다** —
콘솔에서 부르면 전투력 4짜리를 공짜로 얻는다.

§-34에서 막은 것과 같은 종류의 구멍이고 해결도 같다. **검사를 넣을 자리를 한 함수로
몰아 뒀다** — 자산을 읽는 방법이 확인되면 `grantEntitlement` 안에서만 고치면 된다.

### 배속은 안 만들었다 (구조상 물어봐야 한다)

**락스텝에서는 판 속도를 한쪽만 바꿀 수 없다.** 양쪽이 같은 틱을 같은 순서로 돌아야
하는데 속도가 갈리면 그 자리에서 데싱크다. 가능한 해석이 셋인데 셋 다 다른 물건이다:

1. **봇전 전용 빨리감기** — PVP에서는 못 쓴다. 편의 기능
2. **생산 속도 강화의 유료 단계** — 지금 `speedLevel` 5단계 위에 하나 더. 이건 전투력
   축이라 PVP 밸런스에 직접 들어간다
3. **판 밖의 시간 단축** — 매칭 대기 등

고르셔야 만든다.

### 검증

- 서버 **127/127** (유료 8개 추가), `tsc` 통과, 빌드 124.98 kB
- 서버 쪽으로 고정한 것: 코인으로 못 삼, 소유 없이 못 입음, 모르는 항목 거부,
  열린 뒤 착용됨, 두 번 열어도 한 칸, 저장본 오염(중복·모르는 값) 정리
- 브라우저(375px): 유료 카드가 `준비 중`·비활성으로 뜨고 안내문이 이유를 적는다.
  아우라 클래스가 붙고, 그림은 `/assets/unit/p1/beergang/run0.png` 를 빌리며,
  높이 53px로 카탈로그 최대. 저장 형식 v10, `entitlements: []`

---

## -35. "봇이 중계를 못 쓴다"는 낡은 관찰이었다 (2026-08-02)

사용자가 다음 작업으로 **봇 개선**을 골랐다. §7의 항목이 이것이다:

> 봇은 재고 20에서 경로를 열어 성장을 멈추므로 60에 잘 못 닿는다.
> 전에는 판당 중계가 사실상 0이었고 지금은 10까지 올랐지만 의도한 전술이 아니라 부산물이다

**고치기 전에 다시 쟀더니 사실이 아니었다.**

### 실측 (봇 대 봇 12판, `sim/` 헤드리스)

| 항목 | 값 |
|---|---|
| 판당 중계 (중앙값) | **284회** (최소 149, 최대 447) |
| 도착 유닛 중 중계를 거친 비율 | **27.9%** (3,275 / 11,740) |
| 중계 노릇을 한 타워 (판당) | 3.9개 |
| 어느 순간에나 상한에 차 있는 타워 | 평균 1.12개 |

`ev.relays` 는 `step()` 마다 새로 만들어지므로(`emptyEvents`) 이 수치는 홉 단위 실집계다.

**도착 유닛의 4분의 1 이상이 중계를 거친다.** "사실상 0"과는 자릿수가 다르다.

### 그래서 봇을 안 건드렸다

고치려던 결함이 없다. 여기서 봇의 보급을 한 곳으로 모으면(중계 허브를 의도적으로
만들면) 봇이 **더 세지는데**, 그건 §-0.75에서 `BOT_SPEED_LAG = 0.5` 로 맞춰 둔
승률 66~68%를 흔든다. 그리고 그 승률은 §-29의 점수 천장(1123)이 서 있는 근거다.

**밸런스 수치는 사용자가 정한다** (§8, 2026-07-29 지시). 결함이 아닌 것을 고치겠다고
그 축을 흔들 이유가 없다. 정말로 봇을 세게 만들고 싶다면 그건 난이도 결정이지
버그 수정이 아니다 — 그때는 `BOT_SPEED_LAG` 도 같이 다시 재야 한다.

### 구조는 기록해 둔다 (나중에 필요하면)

중계는 **상한(60)에 찬 내 타워**에 유닛이 닿으면 흡수되지 않고 그 타워의 경로 중
하나로 계속 가는 것이다 (`relayThrough`, 최대 `RELAY_MAX_HOPS`=3, 라운드 로빈).
**보급선이 그대로 공격선이 된다.**

봇을 의도적으로 중계시키려면 `desiredTargets` 의 후방 보급 대상을 바꾸면 된다 —
지금은 후방 타워마다 `minBy(dist)` 로 **제일 가까운** 최전선을 고르므로 보급이 흩어진다.
적에게 가장 가까운 타워 하나로 **모으면** 그 타워가 상한을 유지하며 중계기가 된다.
한 곳만 고치면 되고 되돌리기도 쉽다.

---

## -34. 점수 치팅 방어선 (2026-08-02, 사용자 결정)

**서버는 승패를 클라이언트 보고로 믿는다** (§7의 "서버 권위 시뮬레이션"). 원래도
그랬는데 §-27~-32에서 점수와 순위표를 붙이면서 **사다리를 통째로 밀어 올릴 수 있게**
됐다. 사용자 결정: **"서버 방어선만"** — 서버 권위 이전은 출시 전으로 미룬다.

### 가장 싼 치팅은 즉시 항복이었다

부계정이 판을 시작하자마자 항복하면 본계정이 이긴다. 항복은 `reportResult` 를 안 부르지만
**이긴 쪽은 정상적으로 부른다.** 판 하나가 몇 초라 무한히 돌릴 수 있었다.

### `MIN_RATED_MS = 20000` — 짧은 판은 점수에 안 센다

거부가 아니라 **점수만 안 준다.** 코인과 전적은 그대로 간다 — 판은 실제로 있었다.

값의 근거(실측, `sim/` 을 헤드리스로 30시드씩):

| 상황 | 최단 결판 |
|---|---|
| 봇 대 봇 (양쪽 공격) | 48.8초 |
| 한쪽이 아무 명령도 안 냄 | **32.4초** |

**20초 밑은 정상 플레이로 안 나온다.** 유일하게 걸리는 것이 초반 항복승인데 그게
막으려는 경로다. 규칙 자체도 납득 가능하다 — "상대가 20초 안에 나가떨어진 판은
점수에 안 센다".

**순위표도 같이 막는다.** 점수가 안 움직인 판이 표를 갱신하면 세운 의미가 없다.

### 나머지 둘

- **`startedAt` 없는 방은 보고를 못 받는다.** `#start`/`#soloStart` 만 이 값을 찍는다 —
  방을 만들어 놓고 곧바로 결과부터 보내는 경로가 끊긴다
- **`eloDelta` 를 `±k` 로 한 번 더 자른다.** 수식상 이미 그 안이지만, 점수 스냅샷이
  어디선가 오염되면 한 판에 사다리가 뒤집힌다. 자르는 비용이 0이다

### 양쪽이 다른 승자를 대면 기록만 남긴다

`reports` (슬롯별 신고) + `resultMismatch`. **지불은 안 바꾼다** — 서버가 시뮬레이션을
안 돌려서 어느 쪽이 맞는지 가릴 방법이 없다. 나중에 서버 권위로 갈 때 이 필드가
"실제로 얼마나 어긋나는가"의 표본이 된다.

### 아직 안 막힌 것 (중요)

**PVP에서 먼저 보고한 쪽이 승자가 된다.** 진 사람이 자기가 이겼다고 먼저 보내면 그대로
먹는다. 막으려면 서버가 판을 돌려야 한다 — Verse8 계정 API가 **보낸 사람의 계정만**
쓸 수 있어서(`updateMyState`) "양쪽 신고가 일치할 때만 정산" 같은 우회로도 안 된다.
`resultMismatch` 는 그 사실을 기록만 한다.

### 검증

- 서버 **119/119** (방어선 9개 추가), `tsc` 통과, 빌드 121.59 kB
- 하네스에 `age(roomId)` 헬퍼를 넣었다. 판을 실제로 안 돌리고 곧바로 보고하므로
  **점수를 보는 검사는 전부 방을 늙혀야 한다** — 안 늙히면 새 규칙에 걸린다

---

## -33. 자체 리뷰에서 나온 두 건 (2026-08-02)

§-27~-32를 한 번에 리뷰했다 (`git diff 1f53917..HEAD`). 리뷰 자체는 결함 0이었고,
**리뷰가 안 본 두 가지를 직접 막았다.**

### `ctx.roundRect` 이 없으면 판이 통째로 안 그려진다

`drawFrontBar` 의 둥근 모서리. **Safari 16 미만에는 이 API가 없다.** 없는 채로 부르면
`TypeError` 가 나는데, 이건 `render()` 안이라 **매 프레임 던지고 화면이 통째로 죽는다.**

`typeof ctx.roundRect === 'function'` 로 갈라 없으면 `ctx.rect` 를 쓴다. 모서리를
포기하는 것과 화면이 검게 죽는 것은 비교 대상이 아니다.

### 항복 연타

`resign()` 이 `state.winner !== null` 만 봤다. **명령은 `inputDelayTicks` 뒤에
적용되므로 그 사이에는 아직 `winner` 가 null 이고**, 두 번째 누름이 그대로 통과했다.

`applyResign` 이 끝난 판을 무시하므로 **데싱크는 없다** — 양쪽이 같은 명령 스트림을
받아 같이 무시한다. 다만 PVP에서 그만큼 명령이 더 나간다. `this.resigned` 를 검사에
추가했다.

---

## -32. 전선 막대를 타워 수 비율로 (2026-08-02, 사용자 지시)

사용자 지시: **"타워 수 비율로 바꿔"**. §-30에서 관찰하고 §-31에 남겨 뒀던 것을 고쳤다.

### 옛 지표는 "누가 이기고 있나"가 아니라 "누가 안 쓰고 쌓아뒀나"였다

```
totalPower = 내 타워에 쌓인 병력 + 길 위의 내 유닛
```

공격하면 **내 유닛도 적 병력도 같이 죽는다.** 그래서 미는 쪽의 숫자가 오히려 줄고,
가만히 있으면 타워마다 초당 `TOWER_GEN`(1.9)씩 `TOWER_CAP`(60)까지 쌓인다.

**실측** — P1은 아무 명령도 안 내고 P2(봇)만 공격한 판(seed 1234):

| 초 | 내 타워 | 적 타워 | 타워 비율 | `totalPower` 비율 |
|---|---|---|---|---|
| 10 | 1 | 3 | **25%** | 45% |
| 20 | 1 | 5 | **17%** | 35% |
| 30 | 1 | 8 | **11%** | 22% |
| 40 | 1 | 10 | 9% | 10% |

**가만히 있는 쪽을 시종일관 2배 가까이 부풀렸다.** §-30에서 봇에게 판을 내주는 동안
막대가 파랑 97%였던 것이 이것이다.

### 타워 수는 승패 판정과 같은 기준이다

`checkEnd` 가 **타워 수 우선**, 동수일 때만 `totalPower` 로 가른다. 막대가 그 기준을
그대로 쓰면 화면과 판정이 어긋날 수가 없다.

중립은 양쪽 어디에도 안 센다 — 아직 아무의 것도 아니라서 색이 없다.
그래서 판 시작에는 1:1로 정확히 가운데다.

**눈금이 부드럽게 안 움직이고 툭툭 뛴다.** 한 번 뛸 때마다 타워가 하나 주인이
바뀌었다는 뜻이라 오히려 읽기 쉽다.

### 정상 대전에서는 나빠지지 않는다

봇 대 봇 8판, 판 중간(45초)에 잰 두 지표:

| seed | 승자 | 타워 | `totalPower` |
|---|---|---|---|
| 11 | 1 | 58% | 58% |
| 33 | 2 | 45% | 41% |
| 44 | 1 | 67% | 62% |
| 77 | 1 | 58% | 59% |

**양쪽이 다 싸우면 두 값이 사실상 같다.** 중간 시점 승자 예측은 타워 5/6, 전투력 6/6
인데, 갈린 한 판(seed 66)은 **타워가 정확히 50:50인 동률**을 전투력이 재고 1% 미만
차이로 가른 것이다 — 신호가 아니라 잡음이다.

### 검증

- `tsc` 통과, 서버 110/110, 빌드 121.53 kB
- 위 두 표는 **브라우저에서 `sim/` 을 직접 돌려 잰 값이다** (렌더링 없이). 캔버스가
  이 시점에 또 안 그려져서(§-30의 rAF 문제) 픽셀 검증 대신 쓴 방법인데, 지표 자체를
  재는 데는 이쪽이 더 곧다

### 남은 것

- **화면으로는 못 봤다.** 눈금이 툭툭 뛰는 것이 실제로 어떻게 보이는지는 안 봤다.
  거슬리면 `drawFrontBar` 에서 목표값을 향해 보간하면 된다 (그러면 "지금 몇 대 몇"이
  잠깐 거짓이 된다 — 그 대가를 알고 고를 것)

---

## -31. HUD에 양쪽 점수 + 판이 HUD를 피하게 (2026-08-02, 사용자 지시)

사용자 지시: **"점수도 나와야해 각 플레이어의 점수도 추가해줘"**.

### 이름 밑에 한 줄

```
(◉) 가나다라마바사…   1:25   검은모래32 (◉)
    1000점                      960점
```

같은 줄에 안 붙인 이유: 375px에서 이름에 남는 자리가 111px인데 점수가 ~38px을 먹으면
**네 글자짜리 이름만 들어간다.** 줄을 하나 늘리는 편이 싸다 (`HUD_H` 56 → 66).

점수는 이름보다 **작고 흐리다** (11px, 알파 0.72). 누구와 붙었는지가 먼저고 점수는
그 다음이다. 고정폭 숫자를 쓰는 것은 시계와 같은 이유고, `점` 을 붙이는 것은 로비·결과
화면과 같은 표기를 쓰기 위해서다 — 숫자만 두면 이 판에서 딴 점수로 읽힌다.

**0이면 안 그린다.** 점수를 못 받은 판(옛 서버·오프라인)에서 `0점` 이라고 쓰면 밑바닥까지
떨어진 사람으로 읽힌다.

### 봇에게도 점수를 준다 — 안 주면 그 자리가 신호가 된다

`botRating(seed, mine)`. 이름·아바타를 만드는 것과 정확히 같은 이유다(§-7). 양쪽 점수가
뜨는데 한쪽만 비면 그걸로 끝난다.

**내 점수 ±60에서 뽑는다.** 매칭이 점수 대역으로 상대를 고르므로(`RATING_BAND_STEPS` 의
즉시 대역이 100) 진짜 사람이었어도 어차피 가까운 값이 나온다. 멀리 떨어진 값을 주면
"이 점수대에 저런 상대가 붙을 리가 없는데" 가 힌트가 된다. 시드로 뽑아 판 안에서 안 변한다.

PVP는 서버가 판 시작에 찍어 둔 `state.ratings` 스냅샷을 그대로 쓴다 — **판이 끝난 뒤
Elo 계산의 기준값과 같은 값이다**(§-27). 그래서 화면의 점수와 결과 화면의 `before` 가
반드시 일치한다.

### 판이 HUD 밑으로 들어가고 있었다 (원래 있던 버그)

레이아웃을 화면 **전체**에 맞추고 있었다:

```
scale = min(w/620, h/1000)      oy = (h - 1000·scale) / 2
```

375×812에서는 위아래 여백이 104px씩이라 HUD(56)가 들어갔다. 그런데 **375×667에서는
여백이 31px뿐이다** — HUD가 상단 타워를 덮었다. 점수 줄을 넣어 66이 되면서 겹침이
35px로 커졌고, 스크린샷에서 실제로 적 본진이 막대 밑에 깔린 것이 보였다.

HUD가 먹는 높이를 **먼저 빼고 남은 자리에** 판을 넣게 고쳤다:

```
top    = safeTop + HUD_H(66)
usable = h - top - safeBottom - HUD_BOTTOM(48)
scale  = min(w/620, usable/1000)
oy     = top + (usable - 1000·scale) / 2
```

**세로가 긴 화면에서는 판 크기가 안 바뀐다** — 어차피 가로가 먼저 걸린다
(375×812 → 0.605로 전과 같다). 짧은 화면에서만 판이 작아지고, 대신 다 보인다.

`HUD_BOTTOM` 은 48이다. [항복] 버튼(44px)까지 다 비우지는 않았다 — 구석 하나 때문에
판을 더 줄이면 손해가 크고, 그 자리는 경로를 긋는 손에서 이미 가장 멀다.

### 검증

| 화면 | 배율 | 판 시작 y | HUD 겹침 |
|---|---|---|---|
| 375×812 | 0.605 (전과 같음) | 113 | 0 |
| 375×667 | 0.553 | 66 | 0 |
| 320×568 | 0.454 | 66 | 0 |
| 414×896 | 0.668 | 123 | 0 |
| 768×1024 | 0.910 | 66 | 0 |

- `tsc` 통과, 서버 110/110, 빌드 121.46 kB
- **실기 스크린샷으로 확인했다** (이번엔 화면이 보였다 — `document.documentElement.style.zoom
  = 0.5` 로 줄이면 캡처가 페이지 전체를 담는다. 배율 1에서는 좌상단 1/4만 나온다).
  375×812와 375×667 둘 다에서 상단 타워가 온전히 보이고, 봇 점수가 `960점`(내 1000점
  ±60 안)으로 떴다
- 콘솔 에러 0

### 남은 것

- 봇 점수는 **매 판 새로 뽑힌다.** 같은 봇을 다시 만나는 개념이 없으니 문제는 아니지만,
  연속으로 붙으면 점수가 매번 달라지는 것이 눈에 띌 수는 있다
- §-30의 "막대가 초반에 오해를 부른다"(`totalPower` 가 재고를 세므로 안 쓰고 쌓으면
  부풀어 보인다)는 그대로 남아 있다

---

## -30. HUD 재단장 + 설정 창 제거 (2026-08-02, 사용자 지시)

사용자 지시: **"게임 세로형으로 바꾸면서 ui겹치는거 없는지 확인하고 HUD 게임 분위기에
맞게 꾸며줘 / 우리는 상대가 봇인지 모르게 기획했으니까 설정 창 없애고 항복하기만
만들어줘"**.

### 겹침은 실제로 있었다 (닉네임이 길 때)

375px 세로에서 12자 닉네임일 때 실측:

| | 시작 | 끝 |
|---|---|---|
| 내 이름 | 102 | 282 |
| 상대 이름 | 93 | 273 |
| 시계 | 167 | 207 |

**셋이 통째로 포개졌다.** 짧은 이름에서는 안 보이던 버그다 — 이름을 그리는 자리를
막대 끝(`barX - 4`)에 붙여 두고 폭 제한을 안 걸었기 때문이다.

고친 방법은 **자리 예산을 정해 놓고 넘치면 자르는 것**이다:

```
budget = w/2 − HUD_TIMER_HALF(36) − HUD_PAD(12) − 아바타(22) − 간격(6)
```

`HUD_TIMER_HALF` 를 시계 글자 폭이 아니라 **고정값**으로 둔 이유: 글자 폭으로 잡으면
`9:59`→`10:00` 에서 양쪽 이름이 동시에 줄어들어 화면이 이유 없이 들썩인다.

검증: 폭 5종(320·360·375·414·768) × 이름 4종 = **20개 조합 전부 겹침 0**.
320px 최악 조건에서 내 이름 끝 122 < 시계 135, 상대 이름 시작 198 > 시계 185.

### HUD를 필드 위에 얹히는 것으로 전제를 바꿨다

전에는 **위아래 여백에 HUD가 들어간다고 가정**했다. 620×1000 비율이라 375×812에서는
위아래 104px씩 남는다. 그런데 **375×667에서는 31px밖에 안 남아** HUD가 필드를 덮는다.
글자만 얹혀 있으면 밝은 건물 스프라이트 위에서 안 읽힌다.

그래서 **판때기(`drawHudPlate`)를 깔았다.** 아래로 갈수록 투명해지는 그라데이션이다 —
단색 띠로 자르면 화면이 두 동강 난 것처럼 보인다.

### 전선 막대 (`drawFrontBar`)

세력 비율 막대를 **이 게임의 본론**으로 다시 그렸다. 밀고 밀리는 게임이니 경계가
출발선에서 얼마나 옮겨 갔는지가 보여야 한다. 세 겹이다:

1. 두 색이 만나는 **경계 눈금** (막대 위아래로 튀어나온 흰 선) — 지금 전선
2. 가운데 **출발선 자국** — 잴 기준
3. 그 둘을 잇는 **가는 선** — 밀린 폭, 색은 밀어낸 쪽

**`local` 을 받는다.** 슬롯 번호로 그리면 P2로 배정된 판에서 내 세력이 오른쪽에 붙어
매 판 좌우가 뒤집힌다. 내 색이 언제나 왼쪽이다.

### 그 밖

- **시계가 고정폭 숫자**(`ui-monospace`)다. 비례폭이면 초가 바뀔 때마다 가운데
  정렬한 시계가 좌우로 흔들린다. 남은 10초는 붉게 — 유일하게 "서둘러라"인 자리다
- **이름 색을 진영색에서 회백색(`#e6eef7`)으로.** 파랑·빨강 글자는 어두운 판때기 위에서
  대비가 모자라고, 어느 편인지는 아바타 테두리가 이미 말한다
- **조작 안내가 6초 뒤 사라진다** (`HINT_HOLD`/`HINT_FADE`). 상태가 아니라 힌트인데
  세로 화면에서 한 줄을 계속 먹는다. `state.elapsed` 로 재므로 판마다 다시 뜬다

### 설정 창이 봇인지를 알려 주고 있었다

없앤 진짜 이유가 이것이다. 설정 창은 **봇전에서만 판을 멈췄고**(`paused = !this.pvp`),
안내문도 `판이 멈춰 있습니다` / `대전 중에는 판이 멈추지 않습니다` 로 갈렸다.
**§-7이 안 알리기로 한 것을 UI가 두 방향으로 흘리고 있었다.**

창째로 지웠다. `#btn-gear`·`#settings`·`paused`·`openSettings`·`closeSettings` 전부 제거.
**이제 판을 멈추는 경로가 없다.**

항복은 캔버스 위 DOM 버튼 하나로 남았다(`.resign-btn`). 우하단인 이유: 위쪽 여백은
HUD가 쓰고, 아래 구석이 경로를 긋는 손에서 가장 먼 자리다. 호박색인 이유는 그 색이
이미 "되돌릴 수 없다·취소된다"를 뜻하기 때문이다(`CUT_COLOR`) — 붉은색은 P2 진영색이라
여기 쓰면 적 표시와 섞인다.

### 검증

- `tsc` 통과, 서버 110/110, 빌드 120.66 kB
- 겹침: 위 20개 조합 계산 검증 (실제 `measureText` 값)
- 브라우저: 항복 버튼 `297,754` 64×44 — 필드 아래 여백(707~812) 안, 상단 HUD와 안 겹침.
  `#settings`·`#btn-gear` DOM에서 사라짐. 항복 한 번에 즉시 종료, `항복 — 보상 없음`,
  점수 줄 안 뜸(보고를 안 하므로 맞다), 버튼도 결과창 위에서 숨음
- 콘솔 에러 0

### 남은 것 — **그림을 눈으로 못 봤다**

**이 세션에서는 캔버스를 한 번도 못 봤다.** 브라우저 패널이 안 떠서
`requestAnimationFrame` 이 아예 안 돌았고(캔버스 픽셀이 전부 투명), 스크린샷 도구도
같은 이유로 실패했다. 그래서:

- **겹침·좌표·잘림은 계산으로 검증됐다** (실측 `measureText` 기반). 이건 믿을 만하다
- **판때기 그라데이션·전선 눈금·색 조합이 실제로 어떻게 보이는지는 안 봤다.**
  다음 사람이 먼저 할 일은 실기에서 한 판 띄워 보는 것이다
- `ctx.roundRect` 를 썼다. Safari 16 이상이라 Verse8 대상 기기에서는 되지만
  **아주 오래된 iOS에서는 막대가 안 그려질 수 있다**

---

## -29. 봇전도 점수를 움직인다 + 결과 화면에 점수 표시 (2026-08-01, 사용자 지시)

사용자 지적: **"봇전이 점수 안올라가면 플레이어 입장에선 엥 이겻는데 왜 안올라? 되지않을까"**.
맞는 지적이고, **문제가 둘이었다.**

### 1. 코인은 PVP와 같이 주는데 점수만 안 줬다

§-7이 **봇인 것을 화면에 안 알리기로** 했고 **봇전 보상은 PVP와 같게** 했다. 매칭 화면도
결과 화면도 PVP와 구분되지 않는다. 그 상태에서 점수만 안 움직이면 앞뒤가 안 맞는다.

### 2. 순위표가 죽어 있었다 (이쪽이 더 컸다)

`SOLO_FALLBACK_MS = 12000`. **인구가 적으면 거의 매 판이 봇전이다.** 봇전이 점수를
안 건드리면 아무의 점수도 안 움직이고 §-28에서 만든 순위표가 **영구히 빈다.**
출시 전에는 100% 그렇다. 기능이 태어나면서 죽어 있었던 셈이다.

### 봇에 가상 점수를 준다 — 천장이 저절로 생긴다

```
BOT_RATING   = 1000   (= DEFAULT_RATING)
RATING_K_SOLO = 8     (사람전은 32)
```

파밍을 막는 장치를 따로 안 만든 이유: **Elo가 알아서 멈춘다.** 봇 승률은 66~68%로
고정돼 있고(§-0.75의 `BOT_SPEED_LAG` 60판 실측), Elo는 실제 승률과 기대 승률이
같아지는 지점에서 균형을 잡는다.

```
0.67 = 1 / (1 + 10^((1000 - R) / 400))   →   R ≈ 1123
```

**봇만 잡아서는 1123점 언저리가 끝이다.** 그 위로 가려면 사람을 이겨야 한다.
반올림 때문에 **1470점을 넘으면 봇전 승리가 +0** 이 된다 (`8 × (1 − E) < 0.5`) —
하네스에서 1500점으로 확인해 뒀다.

`RATING_K_SOLO` 를 32로 두지 않은 이유: 승률이 고정이라 큰 폭이면 몇 판 만에 천장에
붙고, 그 뒤로는 매 판 널뛰는 것만 보인다.

### 봇전은 스냅샷을 안 쓴다

사람전은 `state.ratings` 스냅샷으로만 잰다(§-27). 봇전은 반대로 **계정의 지금 값**을
쓴다 — 상대가 상수고 보고하는 사람이 하나뿐이라 섞일 자리가 없다. 스냅샷을 고집하면
`#soloStart` 에도 `ratings` 를 채워야 하는데 상수 하나 때문에 룸 상태를 늘리는 것뿐이다.

### 전적은 여전히 갈라 센다

`wins` 와 `soloWins` 는 그대로 나뉘어 있다(§-7). **점수는 하나인데 전적은 둘이다** —
점수는 플레이어가 보는 값이라 갈리면 안 되고, 전적은 나중에 밸런스를 볼 때 표본이
사람이었는지 봇이었는지 알아야 해서 갈려 있어야 한다.

### 결과 화면에 점수를 띄운다

§-28의 "남은 것"이었다. 점수가 움직여도 결과 화면에 안 나오면 **로비에 돌아갈 때까지
모른다** — 사용자가 지적한 바로 그 증상이 반만 고쳐진다.

**서버 왕복이라 보상 줄보다 늦게 온다.** 보상을 먼저 띄우고 점수 줄은 도착하면 채운다.
`resultRound` 로 번호를 매겨 늦게 온 응답이 다음 판의 화면을 덮어쓰지 않게 했다.

**안 움직였으면 줄 자체를 안 띄운다.** `AccountStore.grantReward` 가 오프라인·이미 받은
판·보고 실패에서 `null` 을 준다. `0` 으로 뭉뚱그리면 화면이 `±0` 을 띄우는데,
**안 움직인 것과 못 잰 것은 다르다.**

### 검증

- 서버 **110/110**, `tsc` 통과, 빌드 120.45 kB
- **브라우저에서 실제 봇전을 끝까지 돌렸다** (로컬 백엔드가 진짜 `server.js` 를 돌린다).
  자동 매칭 → 12초 폴백 → 봇전 → 패배:

  ```
  패배
  +30  (기본 30 · 타워 0개 0)
  점수 1000 → 996  (-4)      ← round(8 × (0 − 0.5)) = −4
  ```

  로비 `996점`, 전적은 `0승 0패`(봇전이라 solo로 갔다), **순위표에 `구계정 996점`
  한 줄이 올라왔다** — 봇전 한 판으로 표가 채워지는 것까지 확인
- 콘솔 에러 0

### 남은 것

- **1123 천장은 산술이지 실측이 아니다.** 봇 승률 66~68%를 그대로 믿고 푼 값이라,
  실제로 여러 판 돌려서 어디에 수렴하는지는 안 재 봤다
- 봇전 패배가 −5, 승리가 +3인 구간이 있다(1100 근처). 봇보다 점수가 높으면 이기는 게
  당연한 판이라 Elo상 맞지만, **플레이어에게는 "져서 더 많이 깎인다"로 보인다.**
  불만이 나오면 봇전만 패배 폭을 줄이는 선택지가 있다

---

## -28. 티어를 걷어내고 순위표를 붙였다 (2026-08-01, 사용자 지시)

사용자 지시: **"티어말고 그냥 점수대로 가자 그러고 순위에 10등 점수만 띄워주고"**.
"10등 점수만"이 커트라인 한 줄인지 상위 10명 목록인지 갈려서 물었고, **상위 10명 목록**을
골랐다.

### 티어는 되살리지 말 것

§-27에서 만든 `game/src/rating.ts` 를 통째로 지웠다. `DEFAULT_RATING` 만
`account/account.ts` 로 옮겼다. **구간이 있으면 같은 티어 안의 변동이 안 보인다** —
한 판 ±32인데 실버 폭이 200이라 대부분의 판에서 화면이 그대로다. 점수를 봐도 올랐는지
내렸는지 모르면 점수를 보여 주는 의미가 없다.

### 순위표는 전체를 훑지 않는다

Verse8 유저 상태에는 **"전 계정 읽기"가 없다.** 있더라도 계정 수에 비례해 느려진다.
그래서 글로벌 상태(방 코드표 `codes` 옆)에 10칸짜리 표를 두고 판이 끝날 때마다 고친다.

```
#recordOnBoard(a)   내 칸을 빼고 → 다시 넣고 → 정렬 → 10개로 자른다
```

**최고 기록이 아니라 지금 점수다.** 최고 기록으로 두면 한 번 올라간 사람이 안 내려와
표가 굳는다. 점수가 내려가면 밀려난다.

### 순위표 락을 계정 락 안에서 잡으면 안 된다

`#grantReward` 가 `$lock('acct:…')` 안에서 계정을 저장하고, **락이 풀린 뒤에**
`#recordOnBoard` 를 부른다. 안에서 부르면 계정 락을 쥔 채로 `board` 락을 기다리게 되고,
두 사람이 동시에 보고하면 서로를 막는다.

### 내 줄은 `me` 로 찾는다 — 이름으로 찾으면 안 된다

**닉네임은 안 겹치는 값이 아니다.** 서버가 `$sender.account` 와 대조해 `me` 를 세워
준다. 계정 id는 안 내려준다 — 순위 표시에 필요 없고, 내려주면 남의 계정 주소가 퍼진다.

### 봇전은 안 올라간다

점수를 안 건드리는 것과 같은 이유다(§-27). 이름이 빈 계정도 안 넣는다 — 빈 칸이
순위에 끼면 무엇인지 알 수가 없다.

### "서버 없음"과 "표가 빔"은 다르다

`AccountStore.leaderboard()` 가 오프라인이면 **`null`**, 붙었는데 비면 **`[]`** 를 준다.
빈 배열로 뭉뚱그리면 화면이 "아직 아무도 안 올랐다"로 읽어 버린다. 각각
`서버에 연결되지 않아 순위를 볼 수 없습니다` / `아직 순위에 오른 사람이 없습니다` 다.

### 검증

- 서버 **107/107** (순위표 10개 추가), `tsc` 통과, 빌드 **119.88 kB**
- 브라우저: 로비가 `1000점`(티어 없음), 순위 화면이 실제 `getLeaderboard` 를 타고
  빈 표 문구까지 뜬다. 목록 렌더는 `import('/src/app/board-scene.ts')` 로 실제 씬을
  붙여 확인 — 등수 번호(`ol`), 내 줄만 `board-row-me`, 12자 닉네임에서도 가로 스크롤 0
- 콘솔 에러 0

### 남은 것

- **여러 사람이 동시에 판을 끝내는 경우를 실물로 못 재 봤다.** `$lock('board')` 가
  하네스에서는 단일 스레드라 경합이 안 재현된다 (§-5의 스텁 가정 항목과 같은 한계)
- 순위가 로비 버튼 뒤에 있다. 결과 화면에서 "몇 점이 됐는지"는 아직 안 보여준다

---

## -27. PVP 점수(Elo)와 실력 기반 매칭 (2026-08-01)

`wip` 커밋(1f53917)이 **절반만 놓고 멈춰 있었다.** `eloDelta`·`RATING_BAND_STEPS`·
계정의 `rating` 필드·시작 시 스냅샷(`state.ratings`)까지는 있는데 **판이 끝나도 점수가
안 움직였다** — `#grantReward` 가 `rating` 을 그대로 두고 있었다. 클라이언트 쪽
`game/src/rating.ts` 는 서버 주석이 가리키는데 파일 자체가 없었다. 그 둘을 이었다.

### 점수는 시작 시 스냅샷으로만 잰다

`#ratingAfter(state, slot, outcome)` 가 `state.ratings` 만 읽는다. **계정의 지금 값을
읽으면 안 된다** — 두 사람이 각자 `reportResult` 를 부르는데, 먼저 보고한 쪽이 이미
점수를 옮겨 놓은 뒤라 나중 쪽이 다른 상대 점수로 계산하게 되고 **합이 0이 안 된다.**

실측(하네스 36번): 1200 대 1000, 1200 쪽 승 → `1208` / `992`. 합 **2200 보존**.
지금 값을 읽었다면 진 쪽이 1208을 상대로 봐서 -9가 나온다.

### 자물쇠는 `rewarded` 하나면 된다

`wip` 이 `reports`·`ratingApplied` 를 룸 상태에 만들어 뒀는데 **둘 다 아무도 안 읽었다.**
`rewarded[account]` 가 이미 계정당 한 번을 보장하고, 점수는 보상과 같은 호출에
붙어 있다. 죽은 상태를 남기면 나중에 "이건 왜 있지"가 된다 — 지웠다.

### 봇전은 안 건드린다

solo 전적을 갈라 둔 것과 같은 이유다(§-7). 오프라인 경로(`applyReward`)는 언제나
봇전이라 로컬에서는 `rating` 이 아예 안 바뀐다.

### 티어 구간은 클라이언트 한 곳에만 있다

`game/src/rating.ts`. 서버는 숫자만 안다. **양쪽에 두면 한쪽만 고쳤을 때 보이는 티어가
갈라진다.** 브론즈/실버(900)/골드(1100)/플래티넘(1300)/다이아(1500) — 시작 1000이
실버 한가운데 오게 잡았다. 한 판 최대 ±32라 첫 판에 티어가 튀면 숫자가 안 읽힌다.

### 저장 형식 v9

`rating` 이 붙었다. **`num()` 을 쓰면 안 된다** — 0은 "많이 져서 0점"과 "한 번도 안
쟀음"을 구분 못 한다. `ratingOr()`(클라) / `numOr()`(서버)가 없을 때 1000으로 떨어진다.
v8 저장본 실측: 코인 500·3승 1패 그대로 살고 점수만 `실버 1000` 으로 시작.

### 검증

- 서버 **97/97** (Elo 8개 추가), `tsc` 통과, 빌드 **118.68 kB**
- 브라우저: 로비에 `실버 1000` 표시, v8 저장본 마이그레이션 확인, 콘솔 에러 0

### 남은 것

- **매칭 대역은 실제 사람 둘로 못 재 봤다.** 하네스에서만 확인했다 (점수 차 600은
  즉시 안 붙고, 11초 기다린 방은 열린다). `RATING_BAND_STEPS` 의 숫자는 잠정치다
- 티어가 로비에만 뜬다. 매치 중 HUD나 결과 화면에는 없다

---

## -26. 방향 히스테리시스 + 베이커 경로를 이식 가능하게 (2026-07-31, 사용자 지시)

### 먼저: 프레임마다 깜빡이지는 않았다

§-24에 "45°에 가까우면 톡톡 바뀔 수 있다"고 적어 뒀는데, **시간에 따른 깜빡임은 없다.**
`dx`/`dy` 가 `from`·`to` 타워 좌표에서 나오고 그 값이 한 구간 동안 고정이기 때문이다.

방향이 실제로 바뀌는 곳은 **중계**다 (`relayThrough`) — 상한에 찬 타워를 통과하며
`fromId`/`toId` 가 갈아끼워질 때. 45°에 걸친 두 구간을 오가면 거기서 톡톡 바뀐다.
히스테리시스는 그 자리에서 값을 한다.

### `DIR_HYSTERESIS = 1.35`

이미 쓰던 방향의 점수에 가산점을 준다. 1.0이면 없는 것과 같고, 1.35는 약 **8° 폭의
불감대**다.

```ts
const sideScore = |dx| * (prev === 'run' ? 1.35 : 1);
const vertScore = |dy| * (prev 가 up|down ? 1.35 : 1);
```

`Renderer.unitDir` 이 유닛 id → 방향을 든다. **`drawUnits` 가 매 프레임 살아 있는 것만
담아 통째로 갈아 끼운다** — 유닛 id는 계속 증가하고 재사용되지 않아서, 지우지 않으면
한 판 내내 쌓인다. 새 판(`state.tick` 되감김)에서는 `frontMemory` 와 같이 비운다.

### 베이커에 이 PC 경로가 박혀 있었다

```
const PROJECT_ROOT = 'C:/Users/anjsh/OneDrive/Desktop/TowerWar';   ← 지웠다
```

**저장소를 GitHub에 올린 뒤로는 실제로 터지는 문제다** — 다른 PC에서 클론하면 베이커가
통째로 죽는다. `vite.config.ts` 에 `/__bake/root` 를 붙여 dev 서버가 알려 주게 했고
(`apply:'serve'` 라 프로덕션에는 없다), `fsUrl` 이 **프로젝트 루트 기준 상대 경로**를
받게 바꿨다. 절대 경로도 그대로 받는다.

`DEFAULTS.animation` 이 이제 `Walking.fbx` 다 (전에는 `null`). `facing()` 의 기본값도
같이 맞췄다 — **굽는 것과 같은 클립으로 재야 의미가 있다.** 모델 안의 idle 로 재면
보폭이 없어 발끝-발목 벡터가 흐려진다 (실측: Walking `1.0088` vs 모델 기본 클립 `1.3749`).

### 검증

- **히스테리시스 실측**: 45°에 걸친 두 구간(`dy` 105↔95, `dx` 100 고정)을 6번 오갔을 때
  원시 비교는 `down·run·down·run·down·run`(**전환 6회**), 히스테리시스는
  `down` **6회 연속**(전환 0). 4:1로 크게 벗어나면 정상적으로 넘어간다 — 고착 아니다
- **이식성**: `/__bake/root` 가 루트를 돌려주고, 인자 없는 `inspect()`·`facing()` 이
  상대 경로 기본값으로 열린다 (머티리얼 6종 확인)
- 소스에 남은 절대 경로 **0건** (주석 한 줄 제외)
- `tsc` 통과, 서버 **89/89**, 빌드 **118.17 kB**

---

## -25. GitHub 저장소 (2026-07-31, 사용자 지시)

사용자 지시: **"이거 깃허브에 올리고 싶은데 레포지토리만들어서 dev브랜치에서 작업하게 해줘"**.

```
https://github.com/Jujeongmin/TowerWar   공개
기본 브랜치: dev   (main 도 있고 같은 커밋에서 갈렸다)
```

**작업은 `dev` 에서 한다.** `main` 은 그대로 뒀다 — 나중에 릴리스 자리로 쓸 수 있다.

### 에셋을 저장소에 안 넣는다 — 라이선스 때문이다

사용자가 **"공개 — 에셋은 빼고"** 를 골랐다. `.gitignore` 에 둘 다 들어 있다:

| 경로 | 왜 |
|---|---|
| `assets-src/` | 51MB. **Tiny Swords 원본 팩 재배포 금지**, BeerGang 3D 권한 미확인 |
| `game/public/assets/` | 뽑아낸 PNG지만 **재배포인 것은 같다** |

**뽑아낸 것도 재배포다.** 원본이 아니라 추출·가공한 PNG라도 Tiny Swords 조항
("재배포·재판매·재포장 금지")에 걸린다. BeerGang 은 애초에 상용 권한이 미확인이다.

**클론하면 게임이 그림 없이 돈다.** 렌더러의 원·도형 폴백 경로가 그대로 산다 —
§7에서 "지우지 말 것"이라고 적어 둔 그 폴백이 여기서 값을 한다.
그림을 되살리는 방법은 `README.md` 와 §-16 · §-24 · §7에 있다.

### 히스토리를 다시 썼다 (중요)

처음 커밋에 에셋을 넣어 버렸다가 공개 결정을 듣고 뺐다. **새 커밋으로 지우기만 하면
히스토리에 그대로 남아 공개 저장소에서 누구나 받아 갈 수 있다.**
`git rm --cached` + `commit --amend` 로 커밋 자체를 다시 만들었고,
**푸시 전이라 유출은 없었다.**

확인: `git rev-list --all --objects | grep -c "game/public/assets"` → **0**,
원격 트리에서도 0건 (`gh api .../git/trees/dev?recursive=1`).

에셋을 다시 넣어야 할 일이 생기면 `.gitignore` 를 고치는 것으로 끝나지 않는다 —
**저장소를 비공개로 돌리는 것이 먼저다.**

### §0이 하나 낡았다

"git 저장소가 아니다. 되돌릴 방법이 없으니 큰 수술 전에 파일 복사본을 떠 둘 것"은
이제 유효하지 않다. 커밋이 그 자리를 대신한다.

---

## -24. 유닛 정면·후면 (2026-07-31, 사용자 지시)

사용자 지시: **"유닛도 정면 후면 구워서 방향 맞게 나오게 해줘"**.

§-23에서 세로형으로 돌린 뒤 남겨 둔 숙제다. 유닛이 대부분 위아래로 움직이는데 옆모습
한 장뿐이라 위로 가는 유닛도 오른쪽을 보고 있었다.

### 각도는 전부 `facing()` 으로 재서 정했다

**눈으로 고르면 틀린다** — §-16에서 실제로 270으로 정했다가 유닛이 뒤로 걸었다.
정면/후면은 좌우보다 더 안 갈린다. `facing()` 이 이제 z 성분도 돌려준다:

| yaw | forwardX | forwardZ | 정체 | 화면 |
|---|---|---|---|---|
| 0 | +0.26 | **+1.01** | 정면(카메라 쪽) | `down` 아래로 |
| 90 | **+1.01** | -0.26 | 오른쪽 | `run` 옆모습 |
| 180 | -0.26 | **-1.01** | 후면 | `up` 위로 |
| 270 | **-1.01** | +0.26 | 왼쪽 | (좌우 반전으로 대신한다) |

카메라가 +Z에서 원점을 보므로 **월드 +z = 카메라 쪽 = 정면**이다.

### 파일 이름이 곧 화면 이동 방향이다

```
assets/unit/{p1,p2}/{종류}/run{0..7}.png    옆모습 (59×91)
                          up{0..7}.png     후면   (37×91)
                          down{0..7}.png   정면   (38×91)
```

**`run` 이라는 이름을 바꾸지 말 것** — 상점 썸네일이 `run0.png` 를 직접 부른다
(`previewSrc`). 그래서 옆모습만 옛 이름을 그대로 쓴다.

정면/후면이 옆모습보다 **좁다**(37~38 vs 59). 높이는 셋 다 91로 같아서 화면에 그려지는
키가 안 변한다 — 렌더러가 `w = (sprite.w/sprite.h) * h` 로 폭만 맞춘다.

### 베이커가 세 방향을 한 번에 굽는다

`BakeOptions.directions` 가 `{prefix, yawDeg}[]` 다. **방향이 바깥 루프**라 모델·클립·
머티리얼을 한 번만 준비해 재사용한다 — GLB가 21MB라 방향마다 다시 읽으면 세 배 걸린다.

`span` 계산에 **z를 넣었다** (`max(size.x, size.y, size.z)`). 회전하면 지금 x였던 것이
z가 되므로 x·y만 보면 정면·후면에서 팔이 잘린다.

### 렌더러 — 이동 벡터의 큰 성분으로 고른다

```ts
const vertical = Math.abs(dy) > Math.abs(dx);
const dir = vertical ? (dy > 0 ? 'down' : 'up') : 'run';
const facingLeft = !vertical && dx < 0;
```

**회전은 여전히 안 건다** (§7 — 사람 모양이라 돌리면 옆으로 눕는다). 좌우 반전은
옆모습일 때만 건다. 정면·후면을 반전하면 조명이 반대편에서 오는 그림이 된다.

### 스프라이트를 지연 로딩으로 바꿨다

종류 5 × 진영 2 × 방향 3 × 8프레임 = **240장**이다. 전에는 `Sprites` 생성자가 전부
미리 받았는데 그대로 두면 **첫 로딩이 세 배**가 된다.

`Sprites.loadUnit(owner, kind)` 를 `Renderer.setUnitKinds` 가 부른다. 한 판에 실제로
쓰는 것은 (내 진영·내 종류) + (상대 진영·상대 종류) = **48장**뿐이다.
`render/profiles.ts` 가 아바타에 같은 판단을 해 뒀다 (§-15).

건물 3장은 그대로 미리 받는다. `ready` 는 이제 "지금까지 **요청한** 것이 다 왔는가"다.

### ⚠ 굽는 중에 소스를 고치지 말 것

`bake` 가 도는 동안 `sprites.ts` 를 고쳤더니 **Vite HMR이 페이지를 리로드해
진행 상황(`window.__ship`)이 통째로 날아갔다.** 파일은 이미 다 써진 뒤라 결과는 멀쩡했지만
로그를 못 봤다. 굽기는 5종 × 3방향에 몇 분 걸린다 — 끝나고 고칠 것.

### 검증

- **각도 실측**: 위 표 4개 전부 `facing()` 으로 측정 (`forwardZ` 신규)
- **세 방향이 서로 다른 그림**: `up !== down`, `up !== run`, p1 `!==` p2 (dataURL 직접 비교).
  같은 그림이 다른 이름으로 구워진 게 아니다
- **240경로 전부 `image/png`**. 크기 `run 59×91` / `up 37×91` / `down 38×91`
- **렌더러 방향 선택 실측** (`drawImage` 가로채기, 봇 대 봇 600틱, 유닛 44기):
  실제 이동 방향 분포 **위 13 / 아래 13 / 옆 18**,
  4프레임 동안 그려진 그림 **52 / 52 / 72** — **정확히 4배로 일치**.
  P1(아래 진영)이 `up*`, P2(위 진영)가 `down*` 를 쓴다
- `tsc` 통과, 서버 **89/89**, 빌드 **118.00 kB**
- `dist` **1.54MB / 260파일** (직전 0.67MB / 100파일). 유닛 0.41 → **1.28MB**.
  단 지연 로딩이라 **한 판에 받는 것은 48장**이다

### 남은 것

- 대각선 이동은 큰 성분으로만 가른다. 45°에 가까우면 옆모습과 정면이 톡톡 바뀔 수 있다.
  거슬리면 8방향으로 늘리거나 임계값에 히스테리시스를 넣을 것

---

## -23. 세로형으로 돌렸다 + 항복 재확인 제거 (2026-07-31, 사용자 지시)

사용자 지시: **"항복하기 누르면 재확인하지말고 바로 항복해주고 / 세로형 게임으로
만들어줘 모바일 대응해주고"**.

### 항복 — 확인 창을 없앴다

`#confirm-resign` 을 통째로 지웠다. [항복하기] 한 번이면 끝난다.

**대가는 버튼 바로 아래 문구가 알린다** (`.resign-warn`: "누르면 즉시 패배로 끝나고
코인을 받지 못합니다"). 되돌릴 수 없고 보상도 0인데 안 알리면 그게 더 나쁘다 —
확인 단계를 되살릴 거면 이 문구도 같이 옮길 것.

### 세로형 — 진영이 위아래로 마주본다

```
FIELD_W 1000 × FIELD_H 620   →   FIELD_W 620 × FIELD_H 1000
좌: P1  우: P2               →   아래: P1  위: P2
```

**배치를 다시 재지 않았다.** `maps.ts` 는 여전히 옛 좌표계에서 생성하고 **마지막 한 줄에서
돌린다**:

```ts
const LONG = FIELD_H;    // 1000 — 진영이 마주보는 축 (옛 FIELD_W)
const SHORT = FIELD_W;   // 620  — 폭            (옛 FIELD_H)
push = (u, v, ...) => towers.push({ x: v, y: LONG - u, ... });
```

`LONG`·`SHORT` 가 옛 `FIELD_W`·`FIELD_H` 와 같은 값이라 `BASE_MARGIN`(105)·
`MIN_SPACING`(112)·유형별 좌표·`ring` 반지름이 **전부 옛날 실측 그대로의 의미**다.
§-11의 유형별 수치도 그대로 산다. `Spot` 의 필드를 `x`/`y` 에서 **`u`/`v` 로 바꿨다** —
회전한 좌표계에서 축 이름이 헷갈리는 것이 바로 버그가 되는 지점이다.

**이게 순수 강체변환이라는 증거**: 봇 대 봇 12판의 **판당 점령이 23.0** 으로 §-17에 기록된
값과 정확히 같다. 락스텝 비교 틱 수도 8,709로 동일하다.

#### 같이 돌려야 하는 두 곳 (안 돌리면 검증이 거짓말이 된다)

1. **`maps-check.ts` 의 `mirrorProblem`** — `FIELD_W - t.x` 로 짝을 찾던 것을
   `FIELD_H - t.y` 로 바꿨다. 안 바꾸면 **세로 대칭인 맵을 가로 대칭으로 검사한다**
2. **`renderer.drawField` 의 중앙선** — 세로선에서 가로선으로. `maps.ts` 가
   `y = FIELD_H/2` 를 축으로 거울을 찍으므로 같은 선이어야 한다

`ai/bot.ts` 는 좌표축을 가정하는 코드가 하나도 없어 안 건드렸다 (`.x`/`.y` 참조 0건).
`routeBlockedBy` 의 `distToSegment` 도 회전 불변이라 그대로다.

### 모바일 대응

- **`viewport-fit=cover` + `env(safe-area-inset-*)`** — 노치·홈 인디케이터 밑으로
  버튼이 안 들어가게 `.screen`·`.account-bar`·`.gear`·`.screen-scroll` 을 밀었다
- **캔버스는 `env()` 를 못 읽는다.** `:root` 에 `--safe-top`/`--safe-bottom` 을 두고
  `Renderer.resize` 가 `getComputedStyle` 로 읽어 간다. 안 하면 HUD의 이름·아바타가
  노치에 가린다. **변수 이름을 바꾸면 렌더러도 같이 고칠 것**
- **`height: 100dvh`** — 모바일 주소창이 접혔다 펴지면 `100vh` 가 실제 높이와 어긋난다
- **HUD 막대를 톱니만큼 좁혔다** (`GEAR_CLEARANCE = 58`). 375px 폭에서 상대 이름
  오른쪽 끝이 370px, 톱니가 332~376px라 **겹치고 있었다.** 양쪽을 같이 줄여야 막대가
  가운데 남는다 — 오른쪽만 줄이면 시간 표시와 중심이 어긋난다
- 톱니를 40px → **44px** 로 키웠다 (iOS 최소 터치 크기)
- `-webkit-touch-callout: none`, `overscroll-behavior: contain`(스크롤 화면),
  `overflow-x: hidden` — 가로 스크롤이 생기면 드래그 조작과 헷갈린다

`#stage` 의 `touch-action: none` 과 `overscroll-behavior: none` 은 원래부터 있었고,
입력도 이미 포인터 이벤트라 터치가 그대로 먹는다.

### 검증

- **맵 3000시드 문제 0건.** 유형 분포 543~642로 §-11과 동일
  (`layoutFor` 를 안 건드렸다)
- **본진 위치 실측**: P1 `(310, 895)` 아래 / P2 `(310, 105)` 위
- **375×812 실측**: 판이 **가로를 꽉 채우고**(ox=0) 세로 605px, oy=104.
  타워 12개 전부 화면 안. P1 본진 화면 `(188, 645)` / P2 `(188, 167)`
- **HUD 충돌 없음**: 상대 이름 오른쪽 끝 **273px** vs 톱니 시작 **317px**
- **가로 넘침 0**: 닉네임(아바타 9장 2줄, 카드 59px)·로비(버튼 320×53)·상점(카드 5장 101px)
  전부 `scrollWidth === innerWidth`
- **봇 대 봇 12판 전부 승부남**, 남은 중립 0, **판당 점령 23.0** (§-17과 동일)
- **락스텝 10/10**, 8,709틱 불일치 0. 결정론 유지
- **항복 실측**: [항복하기] **한 번**에 `winner 2` / 제목 `항복` / `항복 — 보상 없음`,
  `grantReward` 호출 **0건**, `#confirm-resign` **DOM에 없음**
- `tsc` 통과, 서버 **89/89**, 빌드 **117.61 kB**

### 남은 것

- **화면이 세로가 됐지만 유닛은 여전히 옆모습이다.** 위아래로 움직이는 유닛에
  좌우 반전만 걸리므로(§7) 수직 이동에서는 전부 오른쪽을 본다. 어색하면
  `bake-units.ts` 에서 정면/후면을 따로 굽고 렌더러가 방향으로 고르게 해야 한다
- 가로 화면(태블릿·데스크톱)에서는 판이 좌우로 크게 레터박스된다. 폭을 넓히려면
  `FIELD_W`(620)를 올리되 §-11 수치가 같이 흔들린다

---

## -22. 매치 중 설정 + 항복 (2026-07-31, 사용자 지시)

사용자 지시: **"게임중에 설정눌러서 항복버튼도 만들어서 나올수있는거 만들어줘"**.

```
매치 화면 우상단 [⚙]
   ↓
설정 — [항복하기] [계속하기]
   ↓ 항복하기
확인 — "판은 즉시 패배로 끝나고 코인을 받지 못합니다" [항복] [돌아가기]
```

> **2026-07-31에 확인 창을 없앴다 (§-23, 사용자 지시).** [항복하기] 한 번이면 끝난다.
> 대가는 버튼 아래 문구가 알린다.

### 항복은 **sim 커맨드**다 (`Command 'resign'`)

§-17 이후 명령이 `toggleRoute` 하나뿐이었는데 두 번째가 생겼다.

**씬에서 직접 `state.winner` 를 박으면 안 된다.** PVP는 락스텝이라 내 쪽에서만 판을
끝내면 두 시뮬레이션이 갈라지고, 그냥 방을 나가면 상대가 내 입력을 기다리며 멈춘다.
`toggleRoute` 와 같은 파이프로 흘려야 **양쪽이 같은 틱에** 같은 결과를 낸다.

공짜로 따라오는 것들:
- `hashState` 가 이미 `state.winner` 를 넣고 있어 데싱크 검사가 그대로 산다
- 서버는 명령을 해석하지 않고 중계만 하며 `cmd.player` 를 덮어쓴다(§-5) —
  **남 대신 항복시키는 것이 막힌다.** `server.js` 는 한 줄도 안 고쳤다
- 봇은 이 명령을 안 낸다 (`ai/bot.ts` 는 `toggleRoute` 만 만든다)

`applyResign` 은 **이미 끝난 판에는 안 먹는다.** `sim.ts:86` 의 `step` 조기 반환이 대부분을
막지만, **같은 틱에 양쪽이 동시에 항복하는 경우**는 이 가드가 필요하다 — 한 배열 안에서
처리되기 때문이다. 명령이 플레이어 번호 순이라(§-6) 양쪽 클라이언트가 같은 결과를 낸다.

### 항복은 0코인 (사용자 결정)

패배 보상 그대로(30 + 타워×8) 주면 **판 시작 직후 항복을 반복해 38코인씩** 벌 수 있다 —
끝까지 하는 것보다 분당 3~4배 빠르다. 그래서 안 준다. 확인 창에 명시한다.

구현은 **아무것도 안 하는 것**이다. 서버가 보상을 계산하므로(§-10) `reportResult` 를
안 부르는 것이 곧 0이다 — **서버에 "항복" 개념을 넣을 필요가 없다.**
`MatchScene.resigned` 플래그는 클라이언트에만 있고 **내 화면과 내 보상에만 쓴다.**
`MatchState` 에 넣으면 순수 표시용 값이 서버 권위 PVP의 검증 대상이 된다.

**PVP 상대는 정상적으로 승리 보상을 받는다** — 자기 클라이언트에서 정상 승리로 보고한다.

### 일시정지는 봇전에만 (사용자 결정)

**PVP에서는 멈출 수가 없다.** 상대가 계속 두는 것도 그렇지만, 결정적으로 `pump()` 가
멈추면 내 `ackTick` 이 안 나가 **상대까지 교착에 빠진다** (§-6).

그래서 `paused` 는 `!this.pvp` 일 때만 참이 되고, 설정 창에 어느 쪽인지 적는다
(`판이 멈춰 있습니다.` / `대전 중에는 판이 멈추지 않습니다.`). 멈춘 줄 알고 있다가
밀리면 그게 더 나쁘다.

멈춘 동안에도 **렌더는 돈다.** 화면이 얼면 멈춘 것인지 죽은 것인지 구분이 안 된다.
닫을 때 `accumulator` 를 비운다 — 안 그러면 멈춰 있던 벽시계가 그대로 들어와
닫는 순간 수십 틱이 한 프레임에 몰린다 (`restart` 와 같은 이유).

`resign()` 이 `closeSettings()` 를 먼저 부르는 것이 중요하다. 멈춘 채로 두면 명령이
적용될 틱까지 시뮬레이션이 안 굴러 **아무 일도 안 일어난다.**

### 전부 DOM이다 (§4.5)

톱니도 두 창도 DOM이다. 캔버스에 버튼을 그리면 그 뒤에서 절단 스와이프가 시작되는 버그를
따로 막아야 한다 — 매치 내 상점(`renderer.shopHit`)을 지운 이유가 그것이다.

**`#settings` 는 `pointer-events: auto` 로 `.screen-overlay` 의 `none` 을 되돌린다.**
결과 화면은 판이 끝난 뒤라 그대로 통과시켜도 되지만, 설정은 **판이 살아 있는 위**에 뜬다 —
안 되돌리면 창 너머 캔버스로 드래그가 들어가 설정을 보는 동안 경로가 열린다.

`restart()` 에서 `resigned`·`paused` 를 되돌린다. 안 지우면 [다시 하기]로 들어간 새 판을
이기고도 `항복 — 보상 없음` 이 뜬다.

### 검증

- **sim**: P1 항복 → winner 2 / P2 항복 → winner 1 / 이미 끝난 판(winner 1)에 항복 → **1 유지**
- **락스텝 성질**: 두 독립 시뮬레이션에 **같은 명령 스트림**(틱 300에 항복 포함)을 먹여
  700틱 매 틱 해시 비교 → **불일치 0**, 양쪽 다 **틱 301에 winner 2**.
  같은 시드를 항복 없이 302틱 돌리면 `winner: null` — 항복이 실제로 끝낸 것이 맞다
- **락스텝 회귀**: `runAll()` **10/10**, 8,709틱 (§-20과 동일)
- **UI 전 구간** (씬을 직접 세우고 `frame()` 을 손으로 돌림, rAF 정지 — §0):
  톱니 노출 → 열면 `판이 멈춰 있습니다.` + **120프레임 동안 틱 60 고정** →
  [항복하기] 확인창 → [돌아가기] 설정 복귀 → [계속하기] 뒤 **틱 60 → 90 재개** →
  [항복] → **winner 2, 제목 `항복`, 보상 `항복 — 보상 없음`, 톱니 숨김**,
  `grantReward` 호출 **0건**
- **톱니가 매치 밖으로 안 샌다**: 부팅·닉네임·로비에서 전부 숨김
- **설정창 스타일 실측**: `pointer-events: auto`, 배경 `rgba(6,10,15,0.82)`, `z-index 30`
  (톱니는 `fixed / top 14px / right 14px / z 20`)
- `tsc` 통과, 서버 **89/89**, 빌드 **117.70 kB**, 콘솔 에러 0

---

## -21. 접속이 닉네임을 지우고 있었다 (2026-07-31, 사용자 신고)

사용자 신고: **"게임중에 상단에 내닉네임하고 아이콘이 안떠"**.

**§-20 작업이 만든 것이 아니다. 원래 있던 버그다.** 다만 §-19에서 아바타가
"캐릭터 고정 + 배경색"이 되면서 아이콘이 안 뜨는 것이 더 눈에 띄었을 뿐이다.

### 왜 둘이 같이 사라지나

`drawHud` 가 **아바타를 이름 조건 안에서 그린다**:

```ts
if (this.names[ui.local]) {
  const x = this.drawAvatar(ui.local, ...);   // ← 아바타가 이 블록 안에 있다
  ctx.fillText(this.names[ui.local], x, 42);
}
```

이름이 비면 줄을 통째로 안 그리는 것은 의도된 동작이다(§-14, 빈칸을 남기면 레이아웃이
흔들린 것처럼 보인다). 그래서 **원인은 언제나 "이름이 비었다" 하나다** — 렌더러를 보지 말 것.

### 진짜 원인 — 부팅 순서 경합

```
showFirstScreen()          동기. 로컬 계정을 보고 닉네임 화면을 띄운다
  ↓ (사람이 이름을 입력)
store.setName()            this.net === null → **로컬에만** 저장
  ↓ (8초 뒤)
store.connect() 완료        this.account = fromRemote(getAccount())
                           서버 계정엔 이름이 없다 → **로컬 이름이 통째로 날아간다**
```

`bootAccount` 는 일부러 안 기다린다(첫 화면이 로컬 사본으로 먼저 떠야 한다). 그 간격이
정확히 닉네임을 입력하는 시간이다. 배포 후에는 접속이 빨라 창이 좁아지지만 **사라지지는
않는다** — 게다가 오프라인으로 이름을 정한 옛 저장본은 접속하는 순간 무조건 걸린다.

### 고친 두 곳

**1. `AccountStore.connect` 가 이름만은 로컬 것을 올려 준다.**

```ts
if (!remote.name && local.name) { await net.setName(local.name); ... }
```

§-10의 "오프라인에서 번 것은 서버에 안 올린다"에 어긋나 보이지만 **재화가 아니라
이름이다.** 코인·강화·소유 유닛을 올리면 콘솔에서 고쳐 두고 접속하는 것으로 서버 검증이
무의미해지지만, 이름은 경제와 무관하고 서버가 `cleanName` 으로 다시 정리한다.
자칭할 수 있는 것이 없다. **서버에 이름이 이미 있으면 서버가 이긴다** — §-10 그대로다.

올리기가 실패해도 접속은 살린다. 이름이 빈 채로 남으면 아래 2번이 되돌린다.

**2. `main.ts` 의 `afterConnect()`** — 접속 뒤 첫 화면 판단을 **다시** 한다.

전에는 `if (current === nameScene && store.current.name) switchTo(lobby)` 로
**"이름이 생긴 경우"만** 봤다. 반대 방향(로비에 있는데 이름이 사라짐)이 없어서
빈 이름으로 로비에 남았다. 매치 중이면 안 건드린다 — 판을 하다 화면이 튕기는 게 더 나쁘다.

### 검증

- **`connect` 3가지 경우** (서버를 흉내낸 스텁으로):
  로컬 `앤지`/pink + 서버 빈 이름 → **로컬 보존 + `setName:앤지`·`setProfile:pink` 올라감** /
  서버에 `서버이름` 있으면 → **서버가 이김, 올리기 호출 0** /
  양쪽 다 비면 → **아무것도 안 올림**
- **실제 앱**: 계정을 지우고 새로 뜬 닉네임 화면에서 이름 입력 → **16.5초 뒤**(dev 백엔드가
  붙고도 남은 시각) `앤지`/gold 그대로. 고치기 전에는 이 시점에 `''`/`slate` 로 바뀌었다
- **빈 이름이면 닉네임 화면으로 되돌아간다**: 서버 계정이 빈 상태로 접속 → 로비로 안 가고
  닉네임 화면 유지 (dev 백엔드는 메모리 스텁이라 새로고침하면 서버 계정도 빈다 — §-14)
- **HUD 실제 출력** (`fillText`/`drawImage`/`fill` 가로채기, PVE 120틱 진행):
  `앤지` + `느긋한노을26`, `profile/beergang.png` **2장**, 아바타 배경색
  `#b83280`(내가 고른 분홍)·`#b7791f`(봇) — **닉네임과 아이콘이 둘 다 나온다**
- `tsc` 통과, 서버 89/89, 빌드 **115.98 kB**

---

## -20. 유닛 종류가 성능을 든다 — 전투력 축이 돌아왔다 (2026-07-31, 사용자 지시)

사용자 지시: **"유닛을 사면 능력치가 좋아졌으면 좋겠어. 유닛마다 체력이 있는거지
기본 유닛 체력은 1이고, 공격력도 1인거야. 다음 유닛은 체력이 1.5, 2 이런식으로 증가"**.

**§-3에 "되살리지 말 것"이라고 적어 둔 축이다.** 그 문장은 사용자 본인의 지시
("전투력강화는 없애")에서 나온 것이고, 이번 지시가 그것을 뒤집는다. 짓기 전에 §-1·§-0.75의
실측치를 그대로 보고했고 사용자가 진행을 결정했다. **§-3의 금지는 이걸로 해제된다.**

### 체력과 공격력을 필드로 나누지 않았다 (수학적으로 같아서)

`Unit.power` 하나가 체력이자 공격력이다. 충돌이 `traded = Math.min(a.power, b.power)` 라,
"각자 자기 power 만큼 때리고 power 만큼 버틴다"와 **죽는 쪽도 남는 값도 한 글자가 안 다르다**:

```
분리 모델: a.hp -= b.atk,  b.hp -= a.atk     a=1.5,b=1 → a 0.5 / b -0.5
min  모델: traded = min(a,b) 를 양쪽에서 뺌   a=1.5,b=1 → a 0.5 / b 0
```

그래서 sim 충돌 코드는 **한 줄도 안 고쳤다.** 필드를 나누고 싶어지면 이 등식을 먼저 깰 것.

### 반 단계가 살아 있는 이유

사용자에게 먼저 알린 사실: **공격력이 1로 고정이면 체력 1.5와 2가 완전히 같다.** 피해가
1씩 들어오므로 둘 다 적 2기를 잡고 죽는다 — 사다리가 `1·2·2·3·3` 으로 뭉개진다.
사용자가 **"체력과 공격력을 같이 올린다"** 를 골라서 분수가 의미를 갖는다.

| 종류 | 체력=공격력 | 가격 |
|---|---|---|
| `beergang` | 1 | 0 |
| `beergang_white` | 1.5 | 400 |
| `beergang_gold` | 2 | 900 |
| `beergang_green` | 2.5 | 1500 |
| `beergang_purple` | 3 | 2400 |

### 흐르는 길 — `sim/` 은 여전히 `units.ts` 를 모른다

```
units.ts (power)  →  account/modsFor  →  PlayerMods.unitPower  →  sim emitUnits
                                      ↘  difficulty/botModsFor (PVE)
server.js kinds{slot} → agent8 setupFrom → MatchScene (PVP)
```

**서버는 힘 수치를 모른다. 이름만 내려준다.** 이름 → 힘 변환은 클라이언트에만 있다
(`unitPowerOf`). §-9에서 `speedMulFor` 에 한 판단과 같다 — 공식을 양쪽에 두면 언젠가
어긋나고, 어긋나는 순간 두 클라이언트가 다른 판을 돈다. 서버가 막아야 하는 것은
**안 산 종류를 자칭하는 것**이고 그건 `cleanAccount` 가 이미 한다.

`hashState` 는 원래부터 `q(u.power)` 를 넣고 있어 데싱크 검사가 그대로 산다.

### 반드시 같이 봐야 하는 세 곳

1. **`setReady` 가 `unitKind` 를 방에 싣고 `#start` 가 `kinds{slot}` 으로 옮긴다.**
   `levels` 와 똑같은 구조다. 안 실으면 양쪽이 자기 계정을 읽어 첫 틱부터 갈라진다
2. **`lockstep-check.ts` 의 `FakeClient` 도 `MatchScene` 과 같은 식으로 `createMatch` 를
   부른다.** 전에는 보정 없이 불렀는데 그대로 뒀으면 `unitPower` 경로가 검증에서
   한 번도 안 밟힌다 (§-6의 "둘이 어긋나면 이 검증은 아무것도 증명하지 못한다")
3. **PVE에서 봇을 내 종류로 그린다.** 전에는 상대가 항상 `DEFAULT_UNIT_KIND` 였는데,
   봇은 `botModsFor` 로 나와 **정확히 같은 힘**을 갖는다. 기본 생김새로 그리면
   약해 보인다 — 색이 곧 세기가 된 뒤로 화면이 거짓말을 하는 것이다

### 봇은 유닛 힘에서 사람과 정확히 같다

`botModsFor` 가 공속만 반 단계 뒤지고 `unitPower` 는 그대로 복사한다. §-0.75 실측
(사람 전투력 2 기준, 봇을 올려 가며 잰 사람 승률):

```
봇 1.0 → 100%   1.5 → 100%   1.7 → 100%   1.85 → 95%   1.95 → 95%   2.0 → 45%
```

1.95에서도 사람이 95% 이긴다. **봇 공속을 +20%까지, 판단주기를 0.5→0.1로 줄여도 전부
100%였다** — 다른 어떤 것으로도 못 메운다. 대가로 **비싼 유닛을 사도 PVE에서는 이득이 0이다.**

### 알고 있어야 할 대가 (실측 근거는 §-1·§-0.75)

- **PVP가 유료 우위다.** 2400짜리를 산 쪽이 안 산 쪽을 거의 항상 이긴다.
  §1의 "완만한 곡선" 완화책은 이 게임에서 성립하지 않는다(§-1)
- **중계가 터진다.** `power` 가 타워 예치량도 겸해서(`troops += u.power`) 재고가 상한(60)에
  빨리 닿고, 상한에 닿은 타워는 중계기가 된다. 양쪽 전투력 2에서 판당 중계 6.5 → 208
- **손잡이는 `units.ts` 의 `power` 표 하나다.** 좁히면(예: 1 / 1.1 / 1.2) 완만해진다.
  전투만 세지고 경제는 그대로이길 원하면 `resolveImpact` 의 `troops += u.power` 를
  `+= 1` 로 바꾸면 된다 — 사용자에게 선택지로 제시했고 이번엔 안 고르셨다

### 검증

- **힘이 실제로 붙는다**: 5종 각각으로 200틱 돌려 P1 유닛의 `power` 가
  `[1] / [1.5] / [2] / [2.5] / [3]`, 상대는 `[1]`
- **봇이 정확히 따라온다**: 5종 전부 `matchModsFor` 의 `mods[1].unitPower === mods[2].unitPower`.
  공속 3단계 + 보라 → 사람 `{1.12, 3}` / 봇 `{1.10, 3}` (반 단계 지연 유지)
- **오염 계정 차단**: 안 산 보라를 착용해 둔 저장본 → 양쪽 다 `unitPower 1`
- **구매→힘 연결**: 3000코인 → 보라 2400 구매 → `power 3`, 잔액 600에서 초록 1500 **거부**,
  흰 400 구매 → 1.5, 보라 재착용 **공짜**, 안 산 초록 착용 시도는 **무시**
- **락스텝 10/10**, 8,709틱 전수 비교 불일치 0. `kinds{1:기본, 2:보라}` 로 힘을 갈라 놓고
  돌렸고 승자가 전부 P2 — `kinds` 가 sim 까지 실제로 흘렀다는 뜻이다
- **결정론**: `{1: 2.5, 2: 1.5}` 로 같은 시드를 두 번 → 최종 상태 JSON 완전 일치
- **PVE 실물**: `MatchScene` 을 직접 세워 `frame()` 을 손으로 돌림(rAF 정지, §0) →
  `state.mods` 가 `{1:{unitPower:3}, 2:{unitPower:3}}`, 봇 유닛 `power 3`,
  **봇이 `p2/beergang_purple` 로 그려짐**
- **서버 89/89** (`kinds` 6건 추가: 내려온다 / 슬롯별 / 안 산 것 자칭 불가 /
  힘 수치는 서버가 안 내려준다 / 모르는 종류는 기본값 / `'toString'` 같은 상속 키도 기본값)
- **상점 카드**: `흰 비어갱 체력 1.5 · 공격력 1.5 흰 하의 ◈ 400`, 제목도
  `유닛 — 비쌀수록 체력과 공격력이 높다` 로 바꿈
- `tsc` 통과, 빌드 **115.71 kB**, 콘솔 에러 0

**승률은 안 쟀다** (§8). 한 시드(3) 한 판에서 기본끼리는 6:6 무승부, 체력 1.5부터
12:0 전멸이 나왔지만 **이건 정확성 검증의 부산물이지 밸런스 측정이 아니다.**
표 조정은 직접 해 보고 판단할 것.

---

## -19. 유닛 색 변형 4종 + 아바타는 배경색만 (2026-07-31, 사용자 지시)

사용자 지시: **"비어갱 말고 다른 아이콘 없애고 비어갱 캐릭터 고정으로 간다 주변 배경 색만
변하게. 유닛도 비어갱 색을 다르게 해서 추가"**.

§-18이 기다리던 "다른 캐릭터 파일"은 **결국 안 들어왔다.** `GangHouse/` 에는 지금도
`BeerGang/` 하나뿐이다. 위 지시가 그 계획을 대신한다 — 캐릭터를 늘리는 대신 색을 늘린다.

### 재킷은 건드리면 안 된다 — 진영색의 유일한 창구다

§-18에 "색만 바꾸는 방식은 쓰지 말 것"이라고 적어 뒀는데, 그건 **재킷을 바꾸는 경우**의
이야기다. `drawUnits` 는 유닛에 배지도 테두리도 안 그린다 — **스프라이트 색이
"누구 편인가"의 유일한 신호다.** 재킷이 그 신호를 들고 있다.

그래서 변형색은 `pants_MAT` · `hair_Wu_MAT` 에만 넣는다. 사용자가 이 방식을 골랐다.

- `sneakers_white_MAT` 은 **일부러 뺐다.** 발치에 밝은 것이 하나 남아야 26px에서
  다리와 발이 안 뭉친다
- `Lyquid_body_MAT` · `base_head_MAT`(피부·얼굴)도 안 건드린다
- **변형색은 진영 색조를 피해야 한다.** 파랑 `#3fbdf1` · 빨강 `#f2555f` 근처 색을 하의에
  칠하면 26px에서 재킷과 하의가 서로 다른 편처럼 읽힌다. 흰·금·초록·보라로 고른 이유다

한 판에 나오는 종류는 최대 둘이고(`match-scene` 이 상대를 항상 `DEFAULT_UNIT_KIND` 로
그린다) 그 둘은 항상 서로 다른 진영이다. 그래서 변형끼리 헷갈릴 일은 구조적으로 없다.

### 베이커에 축이 하나 늘었다

`tools/bake-units.ts` 에 `accentMaterials` / `accentColor` 를 붙였다. `accentColor` 가
`null` 이면 원본 텍스처 그대로다 (= 기본 종류). 진영색과 달리 **진영마다 안 바뀌므로
한 번만 칠한다.**

**대상을 못 찾으면 던진다.** 조용히 넘어가면 기본과 똑같은 그림이 다른 이름으로 구워지고,
상점에서 돈을 낸 뒤에야 같은 것임을 알게 된다. 진영색 쪽과 같은 이유다.

```js
const B = await import('/src/tools/bake-units.ts?v=' + Date.now());
await B.bake({ animation: '<Walking.fbx 절대경로>', frames: 8,
               kind: 'beergang_gold', accentColor: '#f0b429' });
```

실측한 머티리얼 이름 (`inspect()`):
`Lyquid_body_MAT` · `pants_MAT` · `sneakers_white_MAT` · `base_head_MAT` ·
`bomberJacket_MAT` · `hair_Wu_MAT`. 모델 안의 클립은 `AFK`·`Angry`·`Fun`·`Hi`·`idle` 뿐이라
**걷기는 여전히 `anim/Walking.fbx` 를 붙여야 한다** (52/59 바인딩, §-16과 동일).

`facing(90)` = `forwardX +1.0088` = right. **`yawDeg: 90` 이 맞다** (270은 -1.0088 = left).

### 다섯 종

| 종류 | 라벨 | 하의·머리 | 가격 |
|---|---|---|---|
| `beergang` | 비어갱 | 원본(어두운 색) | 0 |
| `beergang_white` | 흰 비어갱 | `#e9eef5` | 400 |
| `beergang_gold` | 금 비어갱 | `#f0b429` | 900 |
| `beergang_green` | 초록 비어갱 | `#3fa96b` | 1500 |
| `beergang_purple` | 보라 비어갱 | `#9061f2` | 2400 |

다섯 종이 같은 모델·같은 클립이라 **추출 상자가 전부 59×91로 같다** — `scale` 도 1.3으로 같다.
(§-16이 `60×91` 이라고 적어 뒀는데 디스크의 실제 값은 처음부터 `59×91` 이었다.)

`units.ts` 세 곳(`UNIT_KINDS`·`UNIT_KIND_META`·`SHOP_UNIT_ORDER`)과 `server.js` 의
`UNIT_PRICES` 를 같이 고쳤다. **어긋나면 "상점엔 보이는데 못 입는"이 된다.**

### 아바타 — 캐릭터 고정, 배경색 9종

캐릭터 그림 9장을 고르던 것이 **BeerGang 한 장 + 배경색 9종**이 됐다.
`PROFILE_IDS` 가 색 이름이다: `slate`(기본)·`blue`·`cyan`·`green`·`gold`·`orange`·
`red`·`purple`·`pink`. `PROFILE_BG` 가 DOM(`--avatar-bg`)과 캔버스 HUD에 같은 값을 준다.

**라이선스 미해결 건이 이걸로 정리됐다.** §-15·§-18이 남겨 둔 `ryan`(카카오)·`bayc`(Yuga
Labs)·`clonex_w`·`clonx_b`(RTFKT/Nike) 가 배포물에 실려 있었는데, 이제 배포물에 남는
그림은 BeerGang 하나뿐이다. 지운 게 아니라 `assets-src/profile-128-unused/` 로 옮겼다
(908px 원본은 `assets-src/beergang/2D/mvp_profile/` 에 그대로).

`botProfile` 이 **더는 기본값을 제외하지 않는다.** 옛 `default` 는 빈 실루엣이라 "안 고른
사람"으로 읽혔지만 지금은 안 고른 것과 고른 것이 그림으로 구분되지 않는다. 오히려 한 색만
빼 두면 그 색이 절대 안 나오는 것이 봇의 신호가 된다.

#### ⚠ `beergang.png` 는 알파가 없었다 — 배경을 따내야 했다

§-15에 "알파 있음"이라고 적혀 있지만 **`beergang.png` 에는 투명 픽셀이 0개였다.**
회색 배경이 그림에 구워져 있어서, 뒤에 색을 깔아도 하나도 안 보였다.
**CSS `background` 가 9종 다 다른 것까지 확인했는데도 화면은 9장이 똑같았다** —
DOM 속성만 재고 픽셀을 안 보면 이걸 놓친다.

배포 사본만 따냈다 (`assets-src` 원본은 그대로). 방법과 함정:

1. **가장자리에서 이어진 영역만 지운다.** 색만 보고 지우면 **흰 머리카락이 같이 날아간다**
2. **배경이 단색이 아니라 세로 그라데이션이다** — 위 `194,194,193` → 아래 `245,241,213`.
   단색으로 잡으면 위쪽 42%만 따이고 아래는 회색으로 남는다 (실제로 그렇게 나왔다).
   행마다 좌우 끝 6px 중앙값을 떠서 y에 대한 1차식으로 맞췄다
3. **맨 아랫줄은 통째로 어깨다.** 흉상이라 아래 두 귀퉁이가 캐릭터다 —
   귀퉁이 평균으로 배경색을 잡으면 `154,154,136` 이 나온다(실제로 그랬다).
   씨앗은 **위·좌·우에서만** 심는다
4. 경계는 배경색과의 거리로 부분 알파를 줘서 계단을 없앴다. 그 뒤 908 → 128 축소

결과: 908px에서 58.2% 제거, 128px에서 투명 8747 / 부분 1292 / 불투명 6345.

#### `/@fs/` 는 fetch·XHR 로 못 읽는다

베이커는 three 로더로 `/@fs/` 를 읽지만, **직접 `fetch`/`XMLHttpRequest` 를 걸면 실패한다**
(`Failed to fetch`). 브라우저에서 `assets-src` 의 그림을 손보려면 `game/public/` 에 잠깐
복사했다 지우는 편이 빠르다.

**PowerShell 로는 아예 시작도 하지 말 것.** 스크립트 안의 한글이 PS 5.1의 코드페이지에
깨져 파서가 죽는다 — §0의 그 사고가 소스가 아닌 임시 스크립트에서도 똑같이 난다.

### 검증

- **베이커 픽셀 실측**: 금 변형에서 재킷 p1 `#5bbbd4`(파랑)·p2 `#ed665c`(빨강) 유지,
  하의는 양쪽 다 `#deac3a`. 기본은 하의 `#3b3b3d`
- **렌더러 실제 출력** (`drawImage` 가로채기, 봇 대 봇 500틱, 유닛 46기):
  `p1/beergang_purple/run{0..7}` + `p2/beergang_gold/run{0..7}` — **8프레임 전부** 나옴.
  옛 경로·다른 종류 **0건**
- **아바타 배경색 실제 출력**: `fill` 가로채기로 `#b83280`(pink, P1)·`#178a9c`(cyan, P2)가
  프레임마다 한 번씩. 그림은 `profile/beergang.png` 한 장만
- **닉네임 화면**: 카드 9장, `distinctSrc: 1` / `distinctBg: 9`
- **상점**: 카드 5장, 가격·라벨·썸네일 정상. **1500코인으로 흰 비어갱 400 구매 →
  1100, 자동 착용**, 2400짜리는 `disabled`
- **마이그레이션**: v8 저장본(`profile:'bayc'`, `unitKind:'lancer'`,
  `ownedUnits:['warrior','pawn']`, 코인 1500) → `slate` / `beergang` / `[]` / **코인 보존**
- 유닛 **80경로 전부 `image/png`**, 지운 아바타 8종 전부 `text/html`(= 실제로 없음)
- 서버 **83/83** (유닛 구매 6건 추가 — §-18이 남긴 "변형이 들어오면 다시 넣을 것" 이행)
- `tsc` 통과, 빌드 **115.30 kB**, 콘솔 에러 0
- `dist` **0.67MB / 100파일** (직전 0.49MB / 44파일).
  유닛 0.083 → **0.411MB**(80장), 아바타 0.17 → **0.023MB**(1장)
- dist 에서 `THREE`·`GLTFLoader`·`bake-units`·`mixamorig`·개발 백엔드·옛 아바타 id
  검색 **0건**

### 남은 것

- **타워는 그대로 Tiny Swords 다.** 유닛만 BeerGang이라 화풍이 여전히 섞인다.
  `BeerGang_HQ`·`genesis` 를 받아야 풀린다 (§-16)
- 밸런스는 안 건드렸다. 유닛 종류는 순수 외형이라 `sim/` 에 안 들어간다

---

## -18. 상점 유닛을 BeerGang 하나로 (2026-07-30, 사용자 지시)

사용자 지시: **"우선 비어갱만 남기고 다른 에셋을 넣고싶어"**.
Tiny Swords 5종(전사·일꾼·궁수·수도사·창기병)을 카탈로그에서 걷어냈다.
픽셀아트와 3D 렌더가 한 판에 섞이던 것이 없어졌다.

### 지운 게 아니라 옮겼다

```
game/public/assets/unit/{p1,p2}/{warrior,pawn,archer,monk,lancer}/
  → assets-src/tiny-swords-units/{p1,p2}/…      52파일. 빌드에 안 실린다
```

`assets-src/` 는 `game/` 밖이라 `dist` 에 안 들어간다. 되살리려면 도로 옮기고
`units.ts` 의 세 곳(`UNIT_KINDS`·`UNIT_KIND_META`·`SHOP_UNIT_ORDER`)과
`server.js` 의 `UNIT_PRICES` 를 복구하면 된다.

### 같이 손본 곳

- **`server.js` 의 `UNIT_PRICES` 가 `{ beergang: 0 }` 하나다.** 클라이언트 표와
  어긋나면 "상점에는 보이는데 못 입는"이 된다. **`buyUnitKind` 배관은 안 지웠다** —
  걸음걸이 변형이 들어오면 그대로 다시 쓴다
- **옛 저장본은 조용히 정리된다.** 카탈로그에 없는 종류는 `migrateUnitKind` 가 기본값으로
  떨어뜨리고, `ownedUnits` 에서도 걸러진다. **거기 쓴 코인은 환불하지 않는다** (§-3과 같은 처리)
- 서버 테스트의 "일꾼 400 구매" 검사는 태울 대상이 없어져 **거부 경로 검사로 바꿨다**.
  변형이 들어오면 구매 검사를 다시 넣을 것

### 다음 종류는 **다른 캐릭터**로 간다 (사용자 결정, 2026-07-30)

내가 먼저 제안한 것은 "같은 캐릭터에 걸음걸이만 다르게"였다. 사용자가 거부했다 —
**"유닛은 그대론데 애니메이션만 다른 건 별로"**. 되살리지 말 것.

색만 바꾸는 방식도 쓰지 말 것. 화면에서 유닛은 26px(`UNIT_SPRITE_H` 20 × scale 1.3)라
바지·머리색 차이는 아예 안 보인다.

**넣는 곳**: `assets-src/beergang/GangHouse/<캐릭터>/glb/*.glb` (또는 `*.fbx`).

#### 그래서 베이커를 넓혔다

1. **FBX 모델도 읽는다.** `loadModel` 이 확장자로 갈린다. Mixamo 에서 캐릭터를
   "with skin" 으로 받으면 FBX 로 나오고 걷기가 그 안에 들어 있어 별도 클립이 필요 없다
2. **`factionMaterials: 'auto'`** — 캐릭터마다 머티리얼 이름이 달라서(BeerGang 은
   `bomberJacket_MAT`) 처음 보는 모델도 바로 구울 수 있어야 한다. 표면적이 가장 넓은
   머티리얼에 진영색을 칠한다
3. 대상이 하나도 안 잡히면 **던진다.** 진영색 없이 구워지면 양쪽이 같은 색으로 나와
   판이 읽히지 않는데, 조용히 넘어가면 그걸 나중에야 발견한다

**`auto` 를 삼각형 개수로 세면 안 된다.** 처음에 그렇게 짰더니 BeerGang 에서
`sneakers_white_MAT`(운동화)가 뽑혔다 — 작지만 촘촘하게 나뉘어 있어서다.
**월드 좌표 실제 표면적**으로 바꾸자 `bomberJacket_MAT` 가 나왔다.

`auto` 는 어디까지나 첫 시도용이다. `inspect()` 로 이름을 보고
`factionMaterials: ['이름']` 로 못 박는 것이 항상 더 정확하다 — 출하본은 그렇게 굽는다.

### 지금 멈춰 있는 지점 (다음 세션이 이어받을 것)

사용자가 **다른 캐릭터 파일을 직접 받아 넣기로 했다.** 넣기 전까지 상점 유닛은 1종이다.

넣을 곳: `assets-src/beergang/GangHouse/<캐릭터>/glb/*.glb` (FBX 도 됨).

**라이선스가 아직 안 풀렸다.** 후보 4종이 전부 남의 IP다 — CloneX(RTFKT/Nike),
zepeto(네이버 Z), ReadyPlayerMe. CloneX 는 폴더 이름이 `CloneX_#1291` 처럼 토큰 번호라
BeerGang 쪽이 그 토큰을 보유했을 가능성이 높지만, **권리는 파일이 아니라 토큰 보유자에게
붙으므로 드라이브를 준 쪽에 확인해야 한다.**

**이건 유닛과 무관하게 이미 미해결이다** — 프로필 아바타 9종에 `clonex_w`·`clonx_b`·
`bayc`·`ryan` 이 **이미 배포물에 실려 있다**(§-15). `ryan`(카카오)은 NFT 보유자 권리
구조가 아니라 더 걸린다. 한 번에 같이 정리할 것.

권리가 안 풀리면 **Mixamo 캐릭터 라이브러리**가 대안이다. 리그가 mixamorig 라 100% 붙고
실루엣도 갈린다. `Characters` 에서 고른 뒤 `Walking` 을 얹어
`FBX Binary / With Skin / 30fps / In Place` 로 받으면 캐릭터+걷기가 한 파일로 나온다.

확인이 늦어지면 **구워는 두되 `units.ts` 에 안 올리는** 방법도 있다 — 스프라이트는
`game/public/` 에 생기지만 카탈로그에 없으면 로드도 안 되고, 켤 때 한 줄이면 된다.
(단 `game/public/` 은 통째로 `dist` 에 복사되므로 **파일 자체는 배포물에 실린다.**
정말 빼야 하면 `assets-src/` 에 굽고 나중에 옮길 것.)

### 검증

- 상점에 유닛 카드 **1장**(`비어갱`, 착용 중), 썸네일 `/assets/unit/p1/beergang/run0.png`
- 옛 경로 `p1/warrior/run0.png`·`p2/lancer/run0.png` → **`text/html`** (Vite 폴백 = 실제로 없음)
- 옛 저장본(`unitKind:'lancer'`, `ownedUnits:['pawn','lancer']`, 코인 1500) →
  **`beergang` / `[]` / 코인 1500 보존**
- `tsc` 통과, 서버 **77/77**, 빌드 **114.66 kB**
- `dist` **0.49MB / 44파일** (유닛 16장 0.083MB). 직전 0.57MB / 96파일

---

## -17. 레벨업 제거 — 레벨 개념이 통째로 사라졌다 (2026-07-30, 사용자 지시)

사용자 지시: **"레벨업을 없애줘"**, 그리고 확인 질문에 **"레벨 개념 통째로 제거"**.

### 무엇이 사라졌나

| 있던 것 | 지금 |
|---|---|
| `Tower.level` (1~5) | **없음** |
| `Command 'upgrade'` | **없음.** 명령은 `toggleRoute` 하나뿐 (2026-07-31에 `resign` 이 붙어 둘 — §-22) |
| `LEVELS` 표 (gen/emit/비용 5단계) | `TOWER_GEN = TOWER_EMIT = 1.9` **한 값** |
| `towerRadiusOf = 16 + level*3` (19~31) | **고정 22** |
| `CAPTURE_LEVEL_PENALTY` | 없음 (점령해도 깎을 레벨이 없다) |
| `TickEvents.upgrades` / `upgradeRejects` | 없음 |
| 건물 5종 사다리 (집→큰집→병영→석탑→성채) | **`lv3`(병영) 한 장** |
| 탭 = 레벨업 | **탭은 아무 일도 안 한다** |
| 봇의 `decideUpgrade` | 없음 (봇도 경로만 낸다) |

`1.9` 는 **옛 레벨 2 값**이다 — 본진과 중앙 타워가 쓰던 값이라 초반 속도가 그대로다.
반지름 `22` 도 옛 레벨 2 값이고, 맵의 `MIN_SPACING`(112)이 그 시절 기준으로 잡혀 있어
그대로 두는 것이 안전하다.

**`TOWER_GEN` 과 `TOWER_EMIT` 을 따로 움직이지 말 것.** 둘이 같아야 "경로 1개 = ±0"
이라는 경제 균형점(§4)이 성립한다.

건물은 `sprites.ts` 의 `TOWER_ART = 'lv3'` 한 줄이 정한다. 나머지 네 장은
`game/public/assets/tower/` 에 그대로 있으니 바꾸려면 그 줄만 고치면 된다.

### 같이 손본 곳

`net/hash.ts` 에서 `t.level` 을 뺐다 — 상태 해시에 없는 필드가 남아 있으면 데싱크
검사가 의미를 잃는다. `maps-check.ts` 의 "만렙 반지름에서 안 갇히는가" 검사도 지웠다.
반지름이 고정이라 기존 "길 있음" 검사 하나가 모든 경우를 덮는다.

### ⚠ 측정된 부작용 — 중계가 27배로 터졌다

**레벨업이 재고를 쓰는 유일한 창구였다.** 그게 사라지자 타워가 상한(60)에 눌러앉고,
상한에 닿은 타워는 중계기가 된다(`relayThrough`).

봇 대 봇 12판(시드 1~12) 실측:

| 지표 | 레벨업 있던 때(§-2) | **지금** |
|---|---|---|
| 승부남 | 10/10 | **12/12** |
| 남은 중립 | 0 | **0** |
| 판당 점령 | 45.4 | **23.0** |
| 판당 중계 | 10.1 | **272.9** |
| 판당 충돌 | 198.2 | 533.4 |
| 평균 최대 유닛 | 67.0 | 73.5 |

**판은 정상적으로 끝난다** (12/12 승부, 중립 0). 하지만 점령이 반으로 줄고 중계가
10 → 273으로 뛰었다. 이건 §-11·§-0.75에서 "판을 망가뜨리는 증상"으로 기록해 둔
바로 그 패턴이다 — 타워가 상한에 붙어 유닛이 튕겨 다니는 상태.

**밸런스 조정은 하지 않았다** (§8: 밸런스는 사용자가 직접 플레이해 판단한다).
직접 해 보고 거슬리면 손댈 곳은 둘 중 하나다:

- `TOWER_CAP`(60)을 올린다 — 상한에 덜 닿아 중계가 준다. 단 방어력도 같이 커진다
- `TOWER_GEN`/`TOWER_EMIT`(1.9)을 내린다 — 재고가 천천히 차서 상한 도달이 늦어진다.
  **반드시 둘을 같은 값으로** 내릴 것

### 검증

- 맵 **3000시드 문제 0건**, 유형 분포 그대로 (543~642)
- 락스텝 **10/10**, 매 틱 해시 전수 비교 불일치 0
- 결정론: 같은 시드 두 번 → 최종 해시 동일
- 봇 대 봇 12판 **전부 승부남**, 남은 중립 0
- `Tower` 에 `level` 필드 없음, 반지름 22 고정 확인
- 렌더러가 그리는 건물이 `p1/lv3.png`·`p2/lv3.png` **두 장뿐**
- `tsc` 통과, 서버 **77/77**, 빌드 **115.13 kB** (117.38 → 줄었다)
- 큰 수술이라 사본을 떠 뒀다: `C:\Users\anjsh\TowerWar-backup-20260730-levels\`

---

## -16. BeerGang 유닛 — 3D를 2D로 굽는다 (2026-07-30, 사용자 지시)

§-15에서 "타워도 유닛도 없다"고 적었던 것이 **유닛에 한해 해결됐다.** 사용자가 Drive 링크
3개를 줬고, 전수 확인해 GangHouse 안의 BeerGang 3D를 받았다.

### Drive 3개 전수 조사 결과

| 링크 | 정체 | 판정 |
|---|---|---|
| `prop` | 모자·날개·재킷 등 장신구 3D 50종 | 유닛/타워 아님 |
| **`GangHouse`** | `2D`(= 이미 받은 프로필) + `3D`(캐릭터 8종) | **여기가 알맹이** |
| `textures` | §-15의 1.44GB zip 안에 있던 그 텍스처 | 중복 |

`GangHouse/3D` 는 캐릭터만 8종이다 — BeerGang / zepeto / Clone_X / ReadyPlayerMe /
AlphaCrewz / Vroidgirl / ryan / CHUN-SIK. **타워는 여기에도 없다.**
`BeerGang_HQ` · `genesis` 는 이 세 링크에 안 들어 있다.

**라이선스 주의**: 이 폴더의 캐릭터 상당수가 남의 IP다 — `ryan`·`CHUN-SIK`(카카오),
`Clone_X`(RTFKT/Nike), `bayc`(Yuga Labs), `zepeto`(네이버 Z). §1의 "실제 출시" 목표 때문에
**BeerGang 만 쓴다.** 다른 캐릭터를 유닛 종류로 쓰려면 권리 확인이 먼저다.

### zip은 프로젝트 밖으로 옮겼다

```
C:\Users\anjsh\TowerWar-assets-raw\    zip 4개 3.0GB. OneDrive 바깥이라 동기화 안 된다
```

`assets-src` 3.0GB → **45.6MB**. `TinySwords_FreePack.zip` 은 현재 타워 에셋 원본이라 남겼다.

### 배포에 에셋이 딸려 올라가지 않는다 (소스로 확인)

사용자 질문에 답하려고 `@agent8/deploy@1.5.5` 를 받아 `bin/agent8.js` 를 직접 읽었다.
**업로드 대상은 두 가지뿐이다**:

1. `./server.js` (없으면 `./server/dist/server.js`)
2. `./dist/` 밑 전부 (재귀)

그 외 경로는 읽지도 않는다 — `assets-src` 는 코드에 등장조차 안 한다.
파일마다 MD5를 떠 `.deployed` 와 비교해 바뀐 것만 올리고, `--prod` 면 전부 다시 올린다.

**단 "v8 에디터 리모트"는 다른 경로다.** 그건 CLI가 아니라 에디터의 워크스페이스 동기화라
이 저장소의 자료만으로는 확인할 방법이 없다. **모른다.** 워크스페이스를 통째로 올리는
방식이면 `assets-src` 가 그대로 올라가므로, 위에서 zip을 밖으로 뺀 것이 그 대비이기도 하다.

### 베이커 — `game/src/tools/bake-units.ts`

GLB를 옆에서 렌더해 유닛 스프라이트를 굽는다. **게임 코드가 아니다.** 아무 데서도
import 하지 않으므로 빌드에 안 실린다 (`maps-check.ts`·`lockstep-check.ts` 와 같은 자리).

```js
const B = await import('/src/tools/bake-units.ts?v=' + Date.now());
await B.inspect();                                   // 모델 안에 뭐가 있나
await B.checkAnimation('<fbx 절대경로>');              // 이 리그에 붙나
await B.bake({ animation: '<fbx>', frames: 8, kind: 'beergang' });
await B.preview({ frames: 6 });                      // 디스크에 안 쓰고 화면에만
```

`three` 는 **devDependency**다. 게임 코드가 이걸 import 하면 번들이 통째로 무거워진다.

**`vite.config.ts` 에 `/__bake/write` 를 붙였다.** `apply:'serve'` 라 프로덕션 빌드에는
존재하지 않는다. 브라우저는 파일을 못 쓰고, 프레임을 다운로드로 받으면 수십 장이
Downloads 로 흩어진다. dev 서버가 `host:true` 라 같은 망에 열려 있으므로 **경로 탈출을 막았다.**

#### 실측으로 정한 값 둘 (감으로 고르지 않았다)

- **`yawDeg = 270`** — 0/90/180/270 을 다 구워 보고 정했다. 270이 화면 오른쪽을 본다.
  렌더러가 **왼쪽 이동에만 좌우 반전**을 걸기 때문에 이 방향이어야 한다
- **`tintMode = 'flat'`** — 봄버재킷 원본이 올리브색이라 진영색을 곱하면 파랑이 초록으로
  나왔다. 텍스처를 떼고 단색으로 칠하고 명암은 조명이 만들게 했다.
  유닛은 화면에서 **20px**(`UNIT_SPRITE_H`)로 그려진다 — 무늬보다 어느 편인지가 훨씬 중요하다

#### Mixamo 클립을 붙일 때 반드시 지킬 것

GLB의 뼈 이름이 `mixamorig:Hips` 형태라 **리타게팅이 필요 없다.** three 의 GLTFLoader와
FBXLoader가 양쪽 다 콜론을 떼서 `mixamorigHips` 로 만들기 때문에 이름이 그대로 맞는다.

**위치 트랙은 전부 버린다** (`retargetToRig`). 이유가 둘 겹친다:

1. Mixamo FBX는 단위가 **cm라 값이 100배다.** 그대로 넣으면 캐릭터가 화면 밖으로 날아간다
2. 다른 본의 위치 트랙은 뼈 길이를 Mixamo 표준 체형으로 늘려 몸을 찌그러뜨린다

회전만 가져오면 이 캐릭터 체형 그대로 같은 동작을 한다. 어차피 제자리 사이클이고
**유닛의 이동은 시뮬레이션이 만든다.**

Mixamo 다운로드 설정: `Walking` / **In Place 체크** / FBX Binary / **Without Skin** / 30fps.

`Walking.fbx` 실측: 트랙 59개 중 **52개가 붙었다.** 못 붙은 7개는 Mixamo 캐릭터의 옷자락 본
6개(이 캐릭터엔 없다) + 의도적으로 버린 Hips 위치 1개다. 발 Z좌표가 -0.44~+0.39로 움직여
보폭이 실제로 나오는 것까지 좌표로 확인했다.

**진단하다 헛짚은 것**: 프레임 0과 3이 똑같아 보여 바인딩 실패를 의심했는데,
걷기 사이클은 한 주기에 다리가 **두 번** 교차하므로 8프레임 중 0·3이 둘 다 중간자세인 게
정상이었다. 눈으로 두 장만 보고 판단하면 안 된다 — 뼈 좌표를 찍어 확인할 것.

### 기본 생김새가 됐다 (2026-07-30, 사용자 지적 "유닛 안바뀐거같은데?")

처음엔 상점의 새 종류로만 넣었다. 그러자 화면이 안 바뀌어 보였고, 원인이 **둘**이었다:

1. 저장본의 `unitKind` 가 `warrior` 로 굳어 있어 직접 착용해야 했다
2. **상대편은 언제나 `DEFAULT_UNIT_KIND` 로 그려진다** (`match-scene.ts` 의
   `setUnitKinds({ [local]: …, [other]: DEFAULT_UNIT_KIND })`). 그래서 내가 갈아입어도
   상대는 계속 Tiny Swords 전사였다

**`DEFAULT_UNIT_KIND` 를 `beergang` 으로 옮겼다.** 이 한 줄이 새 계정·상대편·폴백을
전부 덮는다. 전사를 포함한 기존 5종은 상점에 그대로 있다.

되돌리려면 `units.ts` 의 `DEFAULT_UNIT_KIND` 한 줄이면 된다.
단 **`server.js` 의 `DEFAULT_UNIT_KIND` 도 같이 고칠 것** — 온라인에서는 서버 계정이
진짜라(§-10) 두 값이 어긋나면 접속하는 순간 서버 값이 클라이언트를 덮어써서
"상점에서 고른 것이 되돌아간 것처럼" 보인다. `tools/server-harness.mjs` 에도 같은 상수가 있다.

#### 저장 형식 v8 — 굳은 기본값만 옮긴다

v7까지 저장본의 `unitKind: 'warrior'` 는 **고른 것이 아니라 기본값이 굳은 것**이다.
그대로 두면 기존 플레이어만 계속 옛 그림을 본다.

`migrateUnitKind` 가 **v8 미만이고 아무것도 안 산 계정에 한해서만** 새 기본값으로 옮긴다.
하나라도 샀으면 상점을 열어 본 사람이라 `warrior` 가 진짜 선택일 수 있어 안 건드린다.
옮긴 뒤 v8로 저장되므로 계정당 한 번만 탄다 — 그 뒤에 전사를 고르면 그대로 남는다.

**`server.js` 의 `UNIT_PRICES` 를 같이 고쳐야 한다.** 서버가 소유·착용을 판정하므로(§-10)
거기 없는 종류는 착용이 거부된다. 클라이언트 표(`units.ts`)와 서버 표가 어긋나면
"상점에는 보이는데 못 입는" 증상이 된다.

**프레임 수 8은 원본 시트가 아니라 우리가 정한 값이다.** 다시 구울 때 프레임 수를 바꾸면
`UNIT_KIND_META.beergang.frames` 도 같이 고칠 것 — 안 그러면 없는 파일을 부르거나
사이클이 잘린다.

#### `scale` 은 다시 구울 필요가 없다 (2026-07-30, 사용자 지시 "scale 1.3으로 다시 구워줘")

`scale` 은 **그릴 때 곱하는 값**이지 굽기 인자가 아니다. `UNIT_KIND_META` 한 줄만 고치면
되고 PNG는 그대로다 — 다시 구워도 완전히 같은 파일이 나온다.

사용자 지시대로 **1.1 → 1.3** 으로 올렸다. 실측 그린 크기:

| | 그려진 크기(논리 px) |
|---|---|
| `warrior` (scale 1) | 20.44 × 20 |
| `beergang` (scale 1.3) | 17.14 × **26** |

원본 해상도를 올릴 이유도 없다. 원본이 91px인데 화면에는 26px로 그려진다.

```
game/public/assets/unit/{p1,p2}/beergang/run{0..7}.png   60×91, 16장 0.09MB
assets-src/beergang/GangHouse/BeerGang/glb/…_05.glb      21.76MB (원본)
assets-src/beergang/GangHouse/BeerGang/anim/Walking.fbx  0.38MB (Mixamo)
```

### 매칭 시간 표시 (2026-07-30, 사용자 지시)

자동 매칭에서 "상대를 찾는 중…" 아래에 경과 시간(`0:07`)이 흐른다.
`#pvp-timer` + `PvpScene.frame()`.

**자동 매칭에서 상대를 찾는 동안에만 흐른다.** 친구랑 하기에는 안 나온다 — 그건 서버가
재는 대기가 아니라 사람을 기다리는 것이고, 코드를 준 쪽은 얼마나 기다렸는지 스스로 안다.
시작·정지를 `show()` 한 곳에 뒀다. 화면 전환이 전부 그 함수를 거치기 때문이다.

#### 벽시계로 재야 한다 — dt 누산은 틀렸다

처음엔 rAF 의 dt 를 누산했다. **탭이 뒤로 가면 rAF 가 통째로 멈추는데**
(실측: 숨긴 탭에서 1.8초 동안 **0프레임**) 서버의 대기 시간은 계속 흐른다.
dt 를 쌓으면 돌아왔을 때 화면 숫자가 서버가 센 시간보다 한참 적어 거짓말이 된다.
`Date.now()` 기준으로 바꿨다. 이 값은 **표시 전용이고 어떤 판단에도 안 쓴다.**

`font-variant-numeric: tabular-nums` 를 넣었다. 비례폭이면 1과 8의 폭이 달라
초가 바뀔 때마다 줄이 좌우로 떤다.

#### §-7 결정을 조금 갉아먹는다 — 알고 있을 것

봇 대체를 화면에 안 알리기로 했는데(§-7), 시간이 보이면 **상대가 없는 판이 항상
같은 초에 시작하는 것**이 눈에 띈다 (연결 실패 8초 / 서버 폴백 12초).
문구로 알리는 것은 아니지만 규칙적인 시각 신호가 생긴 것은 사실이다.
거슬리면 표시를 초 단위가 아니라 "찾는 중 · · ·" 같은 무단계 표시로 되돌리면 된다.

### HUD 상단에서 타워 수를 뺐다 (2026-07-30, 사용자 지시)

전에는 양 끝이 두 줄이었다 — 위에 아바타+닉네임(12px), 아래에 `타워 4`(15px).
사용자 지시로 **한 줄로 합치고 타워 수를 없앴다.** 이제 아바타 + 닉네임만 15px로 나간다.

**타워 개수는 이제 화면 어디에도 없다.** 위쪽 막대가 남지만 그건 타워 수가 아니라
`totalPower`(재고+유닛) 비율이라 **같은 값이 아니다.** 개수를 다시 보여줘야 하면
`drawHud` 의 그 자리에 되살릴 것. `towerCount` import 도 같이 지웠다.

### `— Prototype` 문구 제거 (2026-07-30, 사용자 지시)

`game/index.html` 의 `<title>` 이 `TowerWar` 다. 소스 다른 곳에는 이 문구가 없다
(`account.ts` 주석에 "프로토타입 단계" 라는 말이 남아 있지만 화면에 안 나온다).

### 검증

- **기본 생김새 실측**: 봇 대 봇에서 양쪽 다 `p1/beergang`·`p2/beergang` 8프레임,
  Tiny Swords 경로 **0건**. 그린 크기 17.14×26 (scale 1.3)
- **v8 마이그레이션**: v7 저장본(`warrior`, 산 것 없음) → `beergang`,
  코인 1234·이름·공속 2단계 보존. **뭔가 산 계정(`ownedUnits:['pawn']`)은 `warrior` 유지**
- **HUD 실제 출력**: `fillText` 에 `타워` 문구 **0건**, `테스트유저`·`빠른등불46` 만 나옴.
  아바타 두 장 그대로 그려짐
- `document.title` 이 `TowerWar`
- 매칭 시간: 씬을 직접 세우고 `frame()` 을 손으로 돌려 확인 (rAF 가 멈춰 있어서 —
  §0). `0:00` → 흐름 → `1:14`(분 자리 넘어감), `starting` 에서 멈추고 숨김,
  친구랑 하기의 `idle`·`hosting` 에서는 **아예 안 나옴**
- **`scale` 실측**: `beergang` 17.14×**26**, `warrior` 20.44×20
- Mixamo 클립 바인딩 **52/59**, 발 좌표로 보폭 확인
- 유닛 에셋 **16경로 전부 `200 image/png`**
- **렌더러 실제 출력 확인** (`drawImage` 가로채기, 봇 대 봇 400틱 진행 후 유닛 22기):
  `{1:'beergang', 2:'warrior'}` → `p1/beergang/run{0,2,4,6}` + `p2/warrior/run{0,2,4}`,
  좌우를 바꾸면 정확히 뒤집힌다. **8프레임과 6프레임이 각자 제 주기로 감긴다**
- 상점에 `비어갱` 카드 + 썸네일 `/assets/unit/p1/beergang/run0.png`, `[착용하기]` 노출
- `tsc` 통과, 서버 **77/77**, 빌드 **117.38 kB**
- dist 에서 `THREE`·`GLTFLoader`·`bake-units`·`__bake/write`·`mixamorig` 검색 **0건**

### 남은 것

- **타워가 없다.** `BeerGang_HQ`·`genesis` 폴더를 받아야 한다. 없으면 Blender 렌더가 필요한데
  **이 PC에 Blender가 설치돼 있지 않다** (PATH·Program Files 확인). `.blend` 는 지금 못 연다
- **화면에서 크기 확인.** `scale` 은 사용자 지시로 **1.3**(26px). 더 조정하려면 그 한 줄이다
- **화풍이 섞인다.** 픽셀아트와 3D 렌더가 한 판에 같이 나온다. BeerGang으로 완전히 갈면
  타워까지 다 바뀌어야 해소된다

---

## -15. BeerGang 에셋 — 프로필 아바타 (2026-07-30, 사용자 지시)

사용자가 BeerGang 에셋으로 갈아타겠다고 했다. **"안에는 수정하지마"** — 원본을 고치지 말라는 것.

### 받은 것 전수 조사 (2.9GB)

`assets-src/beergang/` 에 zip 3개.

| zip | 크기 | 내용 | 쓸 수 있나 |
|---|---|---|---|
`2D-…001.zip` | 35MB | `2D/mvp_profile/` — PNG 9장 + PSD 1장 | **아바타로 쓸 수 있다** |
`Final Files For 1st…002.zip` | 1.44GB | `Blender/textures/` — PNG 437장 | **못 쓴다** |
`Final Files For 2nd…003.zip` | 1.44GB | 같음 | **못 쓴다** |

큰 zip 둘은 **Blender PBR 텍스처**다 — `Officer_Metalness.png`, `Suit_Gold_Normal`,
`BaseColor`, `Roughness`, `.hdr` 환경맵. 3D 재질 맵이라 Canvas 2D에서 쓸 데가 없다.
**압축을 풀지 않았다** (5.6GB가 OneDrive 동기화로 올라간다).

**타워도 유닛도 없다.** Drive 폴더에는 `GangHouse`·`BeerGang_HQ`·`genesis` 가 있었지만
zip으로 오지 않았다 — 건물·캐릭터 아트가 거기 있을 것이다.

### 그래서 아바타를 붙였다

프로필 9장(908×908, 알파 있음)이 유일하게 쓸 수 있는 것이었고, 마침 §-14의 닉네임과
맞는 자리였다.

```
game/public/assets/profile/{default,beergang,ryan,animeGirl,bayc,clonex_w,clonx_b,RPM,RPM_female}.png
```

- **원본을 안 건드렸다.** 크롭·리사이즈 없이 그대로 복사만 했다. 파일명도 그대로라
  카탈로그 id = 파일명이다
- 닉네임 설정 화면에서 이름과 함께 고른다
- 로비 상단바 + **매치 HUD 상단**(이름 왼쪽, 원형, 진영색 테두리)
- 저장 형식 **v7**. v1~v7 전부 읽어 살린다
- 아바타도 **서버 계정에서 읽는다** — `setReady` 가 `profile` 을 `players` 에 넣고
  `#start` 가 슬롯 번호로 `profiles: {1,2}` 를 내려준다. 클라이언트가 보내면 남의 것을 자칭할 수 있다

### 용량 — 128px로 다시 뽑았다 (2026-07-30, 사용자 허락)

처음엔 908px 원본을 그대로 복사해 `dist` 가 6.51MB였다. 사용자가 허락해서
**배포 사본만 128px로 다시 뽑았다** — `assets-src` 원본은 그대로다(908px 확인).
HighQualityBicubic, 알파 유지. **6.20MB → 0.17MB**, `dist` 0.48MB.

추출 스크립트는 세션 스크래치패드라 사라진다. 다시 필요하면: System.Drawing 으로
908→128 축소, `InterpolationMode = HighQualityBicubic` + `CompositingMode = SourceCopy`
(SourceCopy 가 없으면 투명 배경에 검은 테두리가 낀다).

로딩 구조는 그대로 유지한다:

1. **매치 중에는 필요한 두 장만 불러온다** (`render/profiles.ts`)
2. 아직 안 불러온 아바타 자리에는 진영색 원을 그린다. 자리를 비우면 이름이 좌우로 튄다

이제 장당 2~31KB라 다 불러도 가볍지만, 구조를 되돌릴 이유도 없다.

### 봇 아바타도 사람처럼 골라 준다

`botProfile(seed)` 가 `default` 를 뺀 8장 중 하나를 고른다. `default` 는
"아직 안 고른 사람"으로 읽혀서 봇이라는 힌트가 된다. §-14의 `botName` 과 같은 이유이고,
같은 기만이다 — 되돌리려면 그 파일 두 함수만 고치면 된다.

### 검증

- 아바타 **9경로 전부 이미지로 200**
- 첫 화면에서 카드 9장 표시, 고르면 테두리 강조, `테스트유저` + `bayc` 저장 → **v7**,
  로비에 이름·아바타 표시
- **HUD 실제 출력 확인** (`drawImage`/`fillText` 가로채기):
  `/assets/profile/bayc.png` + `/assets/profile/clonex_w.png` 두 장이 그려지고
  이름 `테스트유저`·`뜨거운기사73` 이 함께 나온다
- 서버 77/77, `tsc` 통과, 빌드 성공 (JS 116.87 kB)

### 아직 필요한 것

타워 5단계와 유닛 스프라이트는 **이 드롭에 없다.** 현재 게임은 Tiny Swords 를 그대로 쓴다.
BeerGang 으로 갈려면 `GangHouse`·`BeerGang_HQ`·`genesis` 안의 2D PNG가 필요하고,
없으면 Blender 파일에서 2D 렌더를 뽑는 아트 작업이 필요하다.

**라이선스**: `game/public/assets/LICENSE-tiny-swords.txt` 는 아직 그대로다.
BeerGang 아트의 상용 사용 권한을 확인해야 한다 (§1의 "실제 출시" 목표 때문에).

---

## -14. 닉네임 + 개발용 로컬 백엔드 (2026-07-29, 사용자 지시)

### 개발용 로컬 백엔드 — 방 만들기가 코드를 낸다

**요청**: "방만들기하면 코드생성이 돼야해".

코드 발급은 서버가 하는 일이다(`$lock` + 글로벌 상태). 배포 전엔 접속이 안 되니 코드가
안 나온다. 그래서 `net/local-backend.ts` 를 붙였다 — **진짜 `server.js` 를 원문으로 읽어
그대로 실행하고**(`?raw` + `new Function`), 그 아래 플랫폼만 메모리 스텁으로 흉내 낸다.
탭 사이는 `BroadcastChannel` 로 잇고, 먼저 뜬 탭이 호스트가 되어 서버를 든다.

**프로덕션 빌드에는 안 들어간다.** `main.ts` 가 `import.meta.env.DEV` 안에서 동적 import
로만 부르고, 프로덕션에서는 그 값이 `false` 로 치환돼 모듈째 떨어져 나간다.
빌드 후 `dist` 에서 `towerwar:roomcode`·`SOLO_FALLBACK_MS`·`towerwar.dev.backend` 를
검색해 **0건**인 것을 확인했다. **정적으로 import 하지 말 것** — 그 순간 서버 원문이 실린다.

실서버 접속이 실패한 뒤에만 붙고, 붙을 때 콘솔에 경고를 남긴다:
`[dev] 로컬 백엔드로 붙었습니다 (호스트|손님). 계정 0xDEV… 실제 Verse8 서버가 아닙니다`

이걸로 통과한 것이 배포 후에도 통과한다는 보장은 없다 — 스텁이 문서를 읽고 만든
가정이기 때문이다. 확인해야 할 가정은 §-5에 있다.

### 닉네임

**요청**: "매칭하고 나서 상단 ui에 닉네임이 떳으면 좋겠어 닉네임은 게임시작할때 처음 설정하게 나오고".

- **첫 실행에 설정 화면**(`#name`, `app/name-scene.ts`)이 로비보다 먼저 뜬다.
  이름이 없으면 못 나간다 — 상대에게 빈칸으로 보이면 누구와 붙었는지 알 수가 없다
- **매치 HUD 상단**에 양쪽 이름. 타워 수 위 줄. 이름이 없으면 줄을 아예 안 그린다
  (빈칸을 남기면 레이아웃이 흔들린 것처럼 보인다)
- 로비 상단바에도 표시
- 저장 형식 **v6**. v1~v6 전부 읽어 살린다

**이름은 서버 계정에서 읽는다.** `setReady` 가 클라이언트 인자를 안 받고 서버 계정의
`name` 을 `players` 맵에 넣고, `#start` 가 슬롯 번호로 옮겨 `names: {1,2}` 를 내려준다.
클라이언트가 보내면 남의 이름을 자칭할 수 있다.

정리 규칙 `cleanName` 이 **서버와 클라이언트에 같은 내용으로 두 벌 있다** — 화면에 보이는
것과 저장되는 것이 다르면 왜 이름이 바뀌었는지 알 수가 없다. 고칠 때 양쪽을 같이 고칠 것.
(제어문자 제거 → 공백 축약 → trim → 12자 자르기)

### 봇 이름은 사람 이름처럼 만든다 — 기만이다

`app/bot-name.ts`. 시드로 `커다란까마귀84` 같은 값을 만든다.

**§-7에서 봇 대체를 화면에 안 알리기로 했는데 HUD에 이름이 생겼다.** 상대 자리를 비우거나
'BOT' 이라고 쓰면 그 결정이 그 자리에서 무너진다. 그래서 사람처럼 보이는 이름을 붙였다.
**이건 플레이어를 속이는 것이다.** 정직한 대안은 '연습 상대' 같은 이름이고,
그러려면 §-7 결정을 되돌려야 한다 — `botName` 을 `() => '연습 상대'` 로 바꾸면 끝난다.
다른 곳은 안 건드려도 된다.

서버는 봇전 방에 **사람 이름만** 내려준다 (`names: { 1: 내이름 }`). 서버가 봇 이름을
만들어 내려주면 그 결정이 서버 쪽에도 박힌다.

### 검증

- 서버 **77/77** (닉네임 8건 추가: 초기 빈 이름 / 공백만 거부 / 앞뒤 공백 제거 /
  12자 자르기 / 제어문자·공백 축약 / 슬롯별 이름 내려옴 / 봇전은 내 이름만)
- 첫 화면이 닉네임 설정, 빈 이름 거부(`이름을 입력하세요 (최대 12자)`),
  `'   가나다라마바사아자차카타파하   '` → `가나다라마바사아자차카타` 로 잘려 저장·표시
- **HUD 실제 출력 확인** (`fillText` 가로채기): 이름 없으면 `타워 N` 둘만,
  `setNames` 후 이름 두 줄이 추가로 그려진다
- 봇 이름 결정론 확인, 시드별로 다른 값
- **방 만들기 → 코드 `RR3T` 실제 생성** (로컬 백엔드)
- 프로덕션 번들에 개발 코드 유출 0건, 빌드 114.20 kB
- `tsc` 통과

---

## -13. 자동 매칭 / 친구랑 하기 분리 (2026-07-29, 사용자 지시)

로비 `[대전 시작]` 하나가 **`[자동 매칭]`·`[친구랑 하기]`** 둘로 갈렸다.

```
로비 [자동 매칭] [친구랑 하기] [상점]
        ↓             ↓
     "상대를 찾는 중…"   "방을 만들거나 코드를 입력하세요"
     8초 뒤 조용히 봇전   [방 만들기] / [코드 입력 + 참가]
```

### 화면은 하나를 나눠 쓴다

`#pvp` 하나에 `#pvp-friend` 묶음만 켜고 끈다. 두 길이 결국 같은 판을 만들고,
방·시드·플레이어 번호를 어느 길이든 서버가 정하기 때문이다. 화면을 둘로 쪼개면
그 배관이 두 벌이 된다.

`Scene.enter` 가 인자를 못 받으므로 `pvp.setMode('auto'|'friend')` 를 `switchTo` 전에
부른다 — `plan` 과 같은 패턴이다.

### 두 모드의 차이는 둘뿐

| | 자동 매칭 | 친구랑 하기 |
|---|---|---|
| 들어오면 | 바로 `findMatch()` | **아무것도 안 한다** — 사람이 고를 일이다 |
| 실패하면 | 조용히 봇전 (§-7) | **화면에 오류** |

친구랑 하기에 봇 대체가 없는 이유: 그 사람은 *그 친구와* 하려던 것이다.
조용히 봇으로 넘기면 왜 친구가 안 보이는지 알 수가 없다.

### 같이 고친 회귀

**접속을 하나로 합치면서(§-10) 오류가 8초 뒤가 아니라 16초 뒤에 떴다.**
`enterRoom` 이 시작할 때 `leaveMatch()` 를 부르는데, 전에는 클라이언트가 매번
새로 만들어져 첫 진입에서 그 호출이 없었다. 접속을 공유한 뒤로는 항상 불리고,
서버가 죽어 있으면 그 호출이 8초 타임아웃을 다 먹은 다음 `connect` 가 또 8초를 먹었다.

`Agent8Client.inRoom` 을 보고 **방에 없으면 `leaveMatch` 를 건너뛴다.**

### 검증 (배포 전, 브라우저)

| 경로 | 결과 |
|---|---|
| 로비 버튼 | `자동 매칭` / `친구랑 하기` / `상점` |
| [친구랑 하기] | 제목·안내문 바뀜, 친구 컨트롤 보임, **10초 가만히 둬도 아무 일 없음** |
| [자동 매칭] | 친구 컨트롤 숨김, "상대를 찾는 중…" → 8초 뒤 조용히 봇전 |
| [방 만들기] | 즉시 "친구를 기다리는 중…" → **8초 뒤** 화면에 오류 |
| 코드 [참가] | 즉시 "방에 들어가는 중…" → **8초 뒤** 화면에 오류 |

`tsc` 통과, 서버 70/70, 빌드 111.30 kB.

---

## -12. 봇이 5초 늦게 시작한다 (2026-07-29, 사용자 지시)

`app/command-source.ts` 의 `BOT_START_DELAY_SEC = 5`. 판이 열린 뒤 5초 동안 봇은
아무 명령도 내지 않는다 — 첫 프레임부터 상대 유닛이 밀려오면 어디가 내 타워인지
보기도 전에 전선이 생긴다.

**시뮬레이션 시간이다** (벽시계가 아니다). `Bot.think` 가 받는 dt 로 재므로
프레임레이트가 흔들려도 정확히 같은 틱(150틱)에 풀린다.

**매치 시간 90초는 그대로 흐른다.** 봇은 90초 중 5초를 스스로 버리는 셈이고 그만큼
약해진다 — 그걸 감수한 값이다. 줄이거나 늘리려면 이 상수만 고치면 된다.

### 기본값을 0으로 둔 이유

`Bot` 생성자의 `startDelaySec` 기본값은 **0**이고, 5초는 `BotSource.forPlayer` 가 준다.

측정·검증 코드(`lockstep-check`, 맵 검증, 헤드리스 스크립트)는 `new Bot(...)` 로
봇을 직접 만든다. 거기에 지연을 기본으로 넣으면 §6의 기준 수치와 §-11의 유형별 수치가
통째로 흔들린다. **게임에 나가는 지연과 측정용 봇을 갈라 두는 것이 핵심이다.**

### 검증

- 게임 경로(`BotSource.forPlayer(2)`) 첫 명령이 **틱 150 = 정확히 5.00초**
- 지연 0으로 만들면 틱 0 — 기존 동작 그대로
- `new Bot(2)` 직접 생성도 틱 0 — 측정 코드 영향 없음
- 지연 뒤에 봇이 멈춰 있지 않은지: 사람이 아무것도 안 하는 8판에서
  지연 0과 지연 5초의 결과가 **동일** (판당 점령 10.6, 봇 최종 타워 11.6, 8/8 승부남)
- 락스텝 10/10, 맵 0건/1500시드, 서버 70/70, `tsc` 통과, 빌드 110.68 kB

---

## -11. 맵 배치 유형 5종 (2026-07-29)

전에는 맵이 한 종류였다 — 무작위 흩뿌리기 하나. 시드를 바꿔도 위치만 흔들릴 뿐
어디를 먼저 먹고 어디서 막을지가 늘 같아서 사실상 "같은 게임"이었다.

**유형도 시드가 정한다.** 결정론은 그대로다. 유형 추첨에는 `seed ^ 0x9e3779b9` 로
따로 뽑은 난수를 쓴다 — 같은 스트림을 이어 쓰면 유형을 하나 더하는 것만으로
기존 모든 시드의 배치가 통째로 흔들린다.

| 유형 | 모양 | 타워 | 판당 점령 | 판당 중계 |
|---|---|---|---|---|
| `scatter` | 무작위 흩뿌리기 (기존) | 12 | 35.6 | 15 |
| `lanes` | 위아래 두 갈래 + 가운데 연결 | 12 | 39.2 | 11.8 |
| `choke` | 가운데 큰 타워가 길목 | 11 | 21.7 | 79 |
| `ring` | 중앙의 큰 상을 고리가 둘러쌈 | 11 | 30.8 | 115.6 |
| `outpost` | 본진 옆 싼 것 + 앞쪽 비싼 것, 중앙선 비움 | 12 | 50.5 | 0 |

(봇 대 봇 12판씩. **승률이 아니라 판이 굴러가는지**를 본 값이다 — `outpost` 는 난전,
`choke` 는 지구전으로 실제로 다르게 흘러간다.)

### 유형을 추가할 때 반드시 지킬 것

1. **왼쪽 절반에만 놓고 거울로 찍는다.** 오른쪽에 직접 놓지 말 것.
   중앙선(x = FIELD_W/2) 위 타워는 자기 자신이 거울상이라 그대로 둔다
2. **타워끼리 `MIN_SPACING`(112) 이상.** 붙으면 서로의 경로를 막아 그 자리가 통째로 죽는다
3. **본진에서 막히지 않은 목표가 최소 하나** — 만렙 반지름(31px)에서도

셋 다 `sim/maps-check.ts` 가 검사한다. 게임 코드가 아니라 빌드에 안 실린다:

```js
const K = await import('/src/sim/maps-check.ts?v=' + Date.now()); K.runAll(3000);
```

### 이 작업에서 잡은 진짜 버그

**흩뿌리기가 중앙선 타워를 간격 검사에서 빼먹고 있었다.** 중앙 타워를 나중에 놓느라
`scatterSpots` 가 그걸 못 봤다 — 3000시드 중 **406건**에서 중앙 타워에 붙은 중립이 생겼고
(최소 거리 67.6px, 기준의 60%), 둘이 서로의 경로를 막아 그 자리가 죽어 있었다.
**기존 맵 생성기에 원래 있던 버그다.** 중앙선 타워를 먼저 정하고 `reserved` 로 넘기게 고쳤다.

### `lanes` 를 한 번 고쳤다

처음엔 중앙 타워를 통로와 **같은 높이**에 뒀다. 그러니 각 통로가 1차원 줄이 되어
전선이 한 지점에 고착되고 뒤에서 재고가 상한까지 밀렸다 —
**점령 10.2 / 중계 190.8** (다른 유형은 점령 21~50, 중계 0~115).
중앙 타워를 두 통로 **사이**로 옮겨 H자로 잇자 **점령 39.2 / 중계 11.8** 로 정상화됐다.

### 검증

- 맵 **3000시드 문제 0건**: 결정론 / 완전 대칭(짝·진영 뒤집힘까지) / 최소 간격 /
  본진에서 길 있음 / 만렙 반지름에서도 안 갇힘 / 판 안
- 유형 분포 고름 (3000시드에서 543~642)
- 봇 대 봇 유형별 12판 **전부 승부남**, 남은 중립 0
- 유형 5종 **실제 렌더 통과** (오프스크린 캔버스, 예외 0)
- 락스텝 10/10 (24,886틱), 서버 70/70, `tsc` 통과, 빌드 110.64 kB
- `MAX_MAP_TOWERS`(12) = `server.js` 의 `MAX_TOWERS` — 보상 상한이 정상 맵을 자르지 않는다

---

## -10. 계정이 서버로 갔다 (2026-07-29)

재화·강화·소유 유닛의 **판정이 서버에 있다.** 전에는 localStorage 가 유일한 저장소라
콘솔에서 코인을 고쳐 쓸 수 있었고, PVP 강화도 클라이언트가 보낸 숫자를 그대로 믿었다.

### 누가 진짜인가

| | 온라인 | 오프라인 |
|---|---|---|
| 판정 | **서버** (`$global.getMyState`/`updateMyState`) | localStorage |
| localStorage | 사본 | 유일한 저장소 |
| 보상 | 서버가 `reportResult` 에서 계산 | 로컬 계산 |

분기는 전부 `account/store.ts`(`AccountStore`) 안에 있다. **씬들은 이 차이를 모른다** —
`current` 를 읽고 콜백으로 사기만 한다.

**오프라인에서 번 코인은 서버에 안 올린다.** 올리면 콘솔에서 코인을 고쳐 두고 접속하는
것으로 서버 검증이 통째로 무의미해진다. 그럴 바엔 안 올리는 게 낫다.

### 유저 상태는 덮어쓰기다 (글로벌 상태와 다르다)

문서 확인: `updateGlobalState` 는 **병합**, `updateUserState`/`updateMyState` 는 **덮어쓰기**.
그래서 계정을 저장할 때마다 **객체 전체**를 쓴다 — 일부 필드만 넘기면 나머지가 날아간다.

### 서버가 막는 것 / 아직 못 막는 것

**막는다**: 코인 잔액, 강화 상한, 유닛 가격, 안 가진 유닛 착용, `setReady` 의 강화 단계
(이제 클라이언트 인자를 안 받고 서버 계정을 읽는다), 판당 보상 중복 수령(`rewarded` 맵).

**못 막는다**: 승패와 타워 수는 **클라이언트가 보고한 값**이다. 서버가 시뮬레이션을
안 돌려서 검증할 방법이 없다 — 타워 수를 `MAX_TOWERS = 12` 로 자르는 정도가 전부다.
제대로 막으려면 `sim/` 을 서버에서 돌려야 한다.

### 가격표가 두 곳에 있다

`server.js` 의 `UPGRADE_COSTS`/`UNIT_PRICES`/`REWARD_*` 와 `account.ts`·`units.ts` 의
같은 값. **서버가 진짜이고 클라이언트 표는 화면 표시용이다.** 어긋나면
"보이는 값과 깎이는 값이 다름"으로 나타난다. 값을 고칠 때 양쪽을 같이 고칠 것.

### 그 밖에 고친 것

- **결과 보고 경로를 하나로 모았다.** `MatchTransport.reportResult` / `Lockstep.reportResult` /
  `CommandSource.finish` 를 전부 없앴다. 보상이 서버 계정을 바꾸므로 계정을 든 쪽
  (`main.ts`)이 한 번만 보고해야 한다 — 두 경로가 있으면 두 번 받는다
- **끝난 판에서도 결과 보고를 받는다.** 전에는 먼저 온 보고가 방을 `finished` 로 바꾸고
  두 번째 사람의 보고를 거절했다 — 보상이 서버로 오면서 **진 쪽이 한 푼도 못 받는** 버그가 된다
- **접속을 하나로.** `PvpScene` 이 자기 `Agent8Client` 를 만들던 것을 앱이 만든 하나를
  주입받게 바꿨다. 둘이면 서버가 보는 계정과 화면이 보는 계정이 갈릴 수 있다
- **봇전 전적 분리** (`soloWins`/`soloLosses`/`soloDraws`). 봇 대체는 화면에 안 알리지만(§-7)
  전적은 갈라 둔다 — 안 그러면 나중에 표본이 사람이었는지 봇이었는지 알 수 없다
- 저장 형식 **v5**. v1~v5 전부 읽어 살린다

### 검증

- 서버 **70/70** (계정 20건 추가: 기본 계정 생성 / 잔액 부족 거부 / 300·700 차감 /
  유닛 구매·착용 / 안 가진 유닛 거부 / 손으로 고친 계정 정규화 /
  **위조한 강화 단계가 안 먹음** / 양쪽 다 보상 받음 / 중복 수령 차단 /
  타워 수 9999 → 12로 잘림 / 봇전은 solo 전적)
- **위조 차단 실측** (브라우저에서 진짜 `server.js` 에 붙여):
  localStorage 에 `coins: 999999, speedLevel: 5, unitKind: 'lancer'` 를 심고 접속 →
  **서버 값 `0 / 0 / warrior` 로 갈아엎힘.** 잔액 0에서 구매 시도 → 변화 없음.
  서버 코인 1200 → 300 차감 → 900. 안 가진 창기병 착용 시도 → 무시
- 오프라인 경로 회귀 없음: v4 저장본 → v5 마이그레이션(코인·전적·강화·유닛 보존),
  공속 구매 900 → 200
- 락스텝 **10/10**, 23,737틱. `tsc` 통과, 화면 전환 5종, 빌드 109.41 kB

---

## -9. PVP가 상점 강화를 반영한다 + 대기 표시 (2026-07-29)

### 강화 단계는 서버를 거친다

전에는 PVP가 양쪽 다 강화 0으로 붙었다. 상점이 PVP에서 아무 의미가 없었다.

**고칠 때 각자 자기 계정을 읽게 하면 안 된다.** 두 클라이언트가 서로 다른 `PlayerMods` 로
시뮬레이션하면 **첫 틱부터 갈라진다.** 그래서 서버를 거친다:

```
setReady(ready, speedLevel)  →  서버가 0~5로 자름
#start                       →  방 상태에 levels: { 1: n, 2: n } (슬롯 번호 기준)
클라이언트                     →  levels 를 읽어 speedMulFor 로 배수 생성
```

슬롯 번호로 담는 이유: 양쪽이 상대 계정 주소를 몰라도 "P1은 몇 단계, P2는 몇 단계"만으로
똑같은 보정을 만들 수 있어야 한다.

**배수 공식은 서버에 복사하지 않았다.** 서버는 정수 단계만 자르고 `speedMulFor` 는
클라이언트에만 있다 — 공식을 양쪽에 두면 언젠가 어긋나고, 어긋나는 순간 두 클라이언트가
다른 판을 돈다. 서버가 아는 것은 상한 `SPEED_LEVEL_MAX = 5` 하나뿐이고
이건 `sim/config.ts` 와 같아야 한다.

**아직 위조는 못 막는다.** 클라이언트가 보낸 숫자를 그대로 믿는다. 제대로 막으려면
계정을 Verse8 글로벌 유저 상태로 옮겨 서버가 직접 읽어야 한다 — §4.5에서 미뤄 둔 그 작업이다.

### 대기 표시

상대 입력이 늦으면 시뮬레이션이 멈춘다. 전에는 화면이 **이유 없이 얼어붙었다** —
플레이어는 자기 조작이 씹혔는지 게임이 죽었는지 구분할 수 없었다.

`Renderer.setWaiting(bool)` + HUD 하단에 `상대를 기다리는 중…`.
점 개수를 시간으로 돌려 "멈춘 화면"이 아니라 "기다리는 중"으로 읽히게 했다.
`MatchScene` 이 매 프레임 `source.waiting` 을 넘긴다 — 봇전에서는 항상 false다.

### 검증

- 서버 **47/47** (강화 5건 추가: `levels` 내려옴 / 슬롯별 단계 / 99→5 잘림 / 음수→0 / 문자열→0)
- 락스텝 **10/10**, 23,737틱 전수 비교 (회귀 없음)
- 대기 표시: 렌더러를 직접 띄워 `fillText` 를 가로채 확인 —
  꺼짐 `[]`, 켜짐 `["상대를 기다리는 중."]`
- `levels {1:3, 2:5}` → `mods {1:{speedMul:1.12}, 2:{speedMul:1.2}}` 변환 확인
- `tsc` 통과, 화면 전환 정상, 빌드 107.41 kB

---

## -8. 방 코드 (2026-07-29)

친구와 붙는 길. 매칭 화면에 `[방 만들기]` 와 `[코드 입력 + 참가]` 가 붙었다.

### 코드를 방 ID로 쓰지 않았다

`$global.joinRoom()` 에 **없는 ID를 넘겼을 때 방이 만들어지는지가 문서에 없다.**
미검증 동작에 설계를 걸지 않고, **코드 → 실제 방 ID 표**를 글로벌 상태(`codes`)에 둔다.
등록되는 것은 코드 방뿐이라 표가 크게 자라지 않는다.

배포 후 `joinRoom('ABCD')` 가 방을 만들어 주는 것이 확인되면 표를 걷어내고
코드를 그대로 방 ID로 쓸 수 있다. 지금 구조가 더 안전할 뿐 더 옳은 건 아니다.

### 알파벳과 정규화

`ABCDEFGHJKLMNPQRSTUVWXYZ23456789` — **`I`·`O` 를 뺐다.** `1`/`l`, `0` 과 섞인다.
숫자도 `2~9` 만. 32자 4칸 = 약 105만 조합. 코드는 **살아 있는 방에서만** 유일하면 된다.

입력은 서버가 정규화한다: 대문자화 → 알파벳 밖 문자 제거 → 4자 자르기.
`"ab3k "` 도 `"AB3K"` 로 들어간다.

### 발급은 반드시 락 안에서

`$lock('towerwar:roomcode', …)`. 두 사람이 같은 코드를 동시에 잡으면
한쪽 친구가 남의 방으로 들어간다. 표를 읽고 → 안 쓰는 코드를 고르고 → 쓰는 것까지가
한 덩어리여야 한다.

**코드는 세 지점에서 회수한다**: 판이 시작될 때(`#start`/`#startSolo`), 방이 버려질 때.
안 걷으면 글로벌 상태가 죽은 코드로 계속 커지고 코드 공간도 마른다.

### 코드 방은 다르게 다룬다

| | 무작위 매칭 방 | 코드 방 (`private: true`) |
|---|---|---|
| 매칭 후보 | 예 | **아니오** — 친구 기다리는 방에 낯선 사람이 들어오면 안 된다 |
| 봇 폴백 | 12초 뒤 | **없음** — 판이 시작되면 코드를 준 의미가 없다 |
| 실패 처리 | 조용히 봇전 | **화면에 오류** — 그 사람은 *그 방*에 가려던 것이다 |

### 같이 고친 진짜 버그

**`$global.countRoomUsers` 는 Promise를 돌려준다.** `findMatch` 가 `await` 없이
`>= ROOM_MAX_USER` 로 비교하고 있었다 — Promise와 숫자 비교는 **항상 false**라
**꽉 찬 방을 그대로 통과시켰다.** 3명째가 방에 들어갈 수 있었다는 뜻이다.
하네스의 스텁도 async로 바꿔 이 실수가 다시 통과하지 못하게 했다.

### 클라이언트 쪽에서 물린 것 둘

1. **`leaveMatch()` 에 타임아웃이 없었다.** 방을 옮길 때 이걸 먼저 기다리는데,
   서버가 없으면 안 끝나서 화면이 통째로 멈췄다. [참가]를 눌러도 아무 반응이 없었다.
   `withTimeout` 으로 감싸고, **화면 갱신을 `await` 앞으로 옮겼다** —
   누른 즉시 상태가 바뀌어야 눌린 걸 안다
2. **시도가 겹치면 늦은 실패가 엉뚱한 일을 했다.** 무작위 매칭이 도는 중에 [참가]를
   누르면, 8초 뒤 돌아온 매칭 실패가 *그때의* 침묵 플래그를 보고 **봇전을 시작해 버렸다.**
   사용자는 코드 참가를 눌렀는데. `attempt` 번호를 시도마다 매기고 번호가 다르면 버린다.
   방 상태 구독도 같은 번호로 거른다

### 두 창 실측 (2026-07-29)

배포 전이라 Verse8에 못 붙는다. 대신 **진짜 `server.js` + 진짜 앱 UI**를 두 브라우저 탭에
띄우고 Verse8 전송 계층만 `BroadcastChannel` 로 대체해 붙였다.
`server.js` 는 `/@fs/…/server.js?raw` 로 원문을 읽어 `new Function` 에 스텁 전역
(`$global`/`$room`/`$sender`/`$lock`)을 물려 그대로 돌렸다 — **파일은 안 고쳤다.**

확인된 것:

| | 결과 |
|---|---|
| 호스트 [방 만들기] | 코드 `SWA7` 발급, 화면 표시, `codes` 표에 매핑 |
| 게스트가 `swa7` (소문자) 입력 | 같은 방 참가 — 서버 정규화 동작 |
| 둘 다 준비 → `$roomTick` | `playing`, 슬롯 GUEST=1 / HOST=2, 시드 배정 |
| 시작 직후 | **코드 표가 비었다** (회수 확인) |
| 두 탭 화면 | 둘 다 매치 화면으로 전환 |
| 무작위 매칭 방 | 코드 방과 별개로 생성되고, 비워지자 닫힘 |

**락스텝 실측**: 두 탭에서 각자 시뮬레이션을 돌리고 명령을 서버로 릴레이해
**틱 226에서 매 틱 해시의 XOR 사슬이 `ef12acdf` 로 완전히 일치**, 늦은 배치 0.
서버 `players` 에 양쪽 `ackTick`(GUEST 270 / HOST 225)이 찍혀 배치가 실제로 서버를
거친 것도 확인했다. 같은 시드를 **명령 없이** 226틱 돌린 해시(`83fc647c`)와 달라서,
릴레이된 명령이 실제로 적용됐다는 것도 확인됐다.

**테스트하면서 알게 된 두 가지 (제품이 아니라 환경 문제):**

1. **숨겨진 탭에서는 `setInterval` 도 rAF처럼 조여진다.** `$roomTick` 을 인터벌로 돌렸더니
   거의 안 돌았다. 틱을 손으로 불러야 했다
2. **수동 스테핑은 `PEER_TIMEOUT_MS`(10초)보다 느리다.** 호출 간격이 몇 초라
   서버가 방을 무응답으로 닫아 버렸다. 릴레이만 보려는 것이라 `seenAt` 만 되살려 진행했다.
   타임아웃 로직 자체는 `npm run test:server` 가 검증한다

**이 테스트가 증명하지 않는 것**: Verse8 실제 전송·지연·재연결. 그건 배포 후에만 된다.

### 검증 — 서버 42/42

코드 관련 12건 추가: 4자 / 헷갈리는 글자 없음 / 락 안에서 발급 / 매핑 저장 /
`private` 표시 / 소문자·공백 정규화 / 무작위 매칭이 코드 방을 안 집음 /
코드 방은 봇 폴백 없음 / 없는 코드·짧은 코드는 던짐 / 시작하면 코드 회수 /
꽉 찬 방 거부 / 버려지면 코드 회수.

클라이언트 (배포 전, 브라우저에서 확인):

| 경로 | 결과 |
|---|---|
| [대전 시작] 후 대기 | 8초 뒤 **조용히** 봇전 (`stage`) |
| [방 만들기] | 즉시 "친구를 기다리는 중…" → 8초 뒤 **화면에 오류** |
| 코드 [참가] | 즉시 "방에 들어가는 중…" → 8초 뒤 **화면에 오류** |
| 매칭 중 [참가] 겹침 | 늦게 실패한 매칭이 **봇전을 시작하지 않음** |

`tsc` 통과, 콘솔 에러 0, 빌드 106.88 kB.

---

## -7. 매칭 실패 → 봇전 자동 대체 (2026-07-29, 사용자 지시)

사용자 결정: **"pvp매칭을해서 매칭할 사람이 없으면 pve로 자동으로 넘어가는 구조"**
그리고 **봇인 것을 화면에 안 알린다**, **봇전 보상은 PVP와 같다**.

이걸로 PVE 캠페인(스테이지 목록·진행 저장·난이도 곡선)은 **안 만든다.**
PVE는 독립된 모드가 아니라 매칭의 대체재가 됐다.

### 화면이 하나로 합쳐졌다

```
로비 [대전 시작] [상점]
        ↓
   대전 매칭 — "상대를 찾는 중…" → "곧 시작합니다" → 매치
        ↑ 사람이든 봇이든 이 화면은 똑같다
```

`[PVE 시작]`·`[PVP 대전]` 두 버튼이 `[대전 시작]` 하나가 됐다.
**로비는 봇/사람을 가르지 않는다** — 갈래를 두 곳에서 판단하면 반드시 어긋난다.

### 폴백 판정은 서버가 한다 (중요)

`server.js` 의 `SOLO_FALLBACK_MS = 12000`. 혼자 준비된 채로 12초가 지나면
`$roomTick` 이 방을 `{ phase:'playing', solo:true, seed }` 로 확정한다.

**클라이언트가 판정하면 안 된다.** "아무도 없네" 하고 나가는 그 순간 상대가 들어올 수 있고,
그러면 `leaveMatch` 가 상대 판을 부전승으로 끝내 버려 상대는 시작하자마자 영문 모를 승리
화면을 본다. 서버 한 곳에서 정하면 경합이 없다.

타이머 기준은 방의 `createdAt` 이 아니라 **그 사람의 `joinedAt`** 이다.
방 나이로 재면 오래된 방에 뒤늦게 들어온 사람이 기다리지도 않고 바로 봇전으로 떨어진다.

### 봇인 것을 숨긴다 — 대가를 알고 있을 것

`pvp-scene.ts` 에는 봇/사람을 가르는 문구가 하나도 없다. 대기·데싱크·서버 장애가
전부 "그냥 판이 시작됨"으로 보인다.

**판별 창구는 둘뿐이다:**
1. 콘솔 경고 `[net] 매칭 실패, 봇전으로 대체합니다: …`
2. 서버 방 상태의 `solo` 필드

전적도 승률도 PVP와 섞인다. 나중에 밸런스를 볼 때 이 표본이 뭐였는지 알 수 없다는 뜻이다.
가르려면 `solo` 를 계정에도 남겨야 한다.

**예외 하나**: 방 코드를 손으로 넣어 참가한 경우에는 실패를 화면에 띄운다.
그 사람은 *그 방*에 들어가려던 것이라, 조용히 봇으로 넘기면 왜 친구가 없는지 알 수가 없다.

### 배포 전 현재 동작 (확인함)

[대전 시작] → "상대를 찾는 중…" → 8초(연결 타임아웃) 후 **조용히 봇전 시작.**
콘솔에만 두 줄이 남는다:

```
[GameServer] VITE_AGENT8_VERSE is required for preview environment.
[net] 매칭 실패, 봇전으로 대체합니다: 서버 연결 응답이 없습니다 (8000ms)
```

즉 **배포 전에도 게임은 끝까지 플레이된다.** 배포 후에는 이 경로가 실제 매칭으로 바뀐다.

### 그 밖에

- `MatchPlan` 의 `pve` 가 `seed?` 를 받는다. 봇전도 서버가 준 시드로 시작한다 —
  시작 경로를 하나로 두어야 한쪽에만 있는 버그가 안 생긴다
- **[다시 하기]는 시드를 새로 뽑는다** (`restart(true)`). 서버 시드를 그대로 쓰면
  같은 맵만 반복된다
- 봇전은 배치를 안 보내 `seenAt` 이 안 갱신된다. `$roomTick` 이 **전원 무응답**인 방을
  닫도록 했다 — 안 그러면 방이 `playing` 인 채로 영원히 남는다

### 검증 — 서버 28/28

`npm run test:server` 에 폴백 6건 추가:
얼마 안 기다렸으면 대기 유지 / 오래 기다리면 봇전 확정 / 봇전 시드는 서버가 준다 /
봇전 슬롯 1번 / 봇전 방은 매칭 후보에서 빠진다 / 버려진 봇전 방은 닫힌다 /
방금 들어온 사람은 곧바로 안 떨어진다.

클라이언트: `tsc` 통과, 로비 버튼 2개(`대전 시작`·`상점`), [대전 시작] → 10초 후
매치 화면 진입, 폴백 경고 2줄 확인, 콘솔 에러 0, 빌드 105.67 kB.

---

## -6. PVP 클라이언트 — 락스텝 계층 (2026-07-29)

**진입 UI만 빼고 다 됐다.** 배포 없이 전 구간을 검증했다.

```
game/src/net/
  types.ts          InputBatch / MatchTransport / MatchSetup
  lockstep.ts       핵심. 명령 예약·상대 대기·에코 폐기
  hash.ts           상태 해시 (데싱크 감지)
  loopback.ts       검증용 트랜스포트 (두 클라이언트를 한 프로세스에)
  agent8.ts         실물 트랜스포트 (@agent8/gameserver)
  lockstep-check.ts 검증 하네스. 게임 코드가 아니라 빌드에 안 실린다
game/src/app/
  command-source.ts BotSource / NetSource — MatchScene은 상대가 봇인지 사람인지 모른다
```

### 락스텝 규칙 세 가지

1. **명령을 `inputDelayTicks`(12틱 = 0.4초) 뒤에 예약한다.** 벽시계가 아니라 틱 번호로
   맞추므로 양쪽 시작 시각이 어긋나도 결과가 안 갈라진다
2. **내 명령도 똑같이 예약한다.** 즉시 적용하면 상대는 그 명령을 다른 틱에 적용하게 된다.
   한 틱만 어긋나도 끝이다. 조작이 0.4초 굼떠 보이는 것이 이 방식의 값
3. **서버 에코를 기다리지 않는다.** 보낼 때 내 쪽에도 같이 예약하고 돌아온 내 배치는 버린다.
   기다리면 입력 지연이 두 배가 된다. 순서가 갈릴 걱정은 없다 —
   한 틱의 명령을 항상 **플레이어 번호 순**으로 붙이므로 도착 순서와 무관하다

`Lockstep.pump()` 는 **멈춰 있을 때도 계속 불러야 한다.** 내 약속(`ackTick`)이 안 나가면
상대도 멈춰 교착이 된다. `MatchScene.frame` 이 틱 루프 앞뒤로 두 번 부르는 이유다.

### 검증 — 10/10, 23,737틱 전수 비교

```js
const K = await import('/src/net/lockstep-check.ts?v=' + Date.now()); K.runAll();
```

두 클라이언트를 `LoopbackHub` 로 붙이고 **매 틱 상태 해시를 기록해 전수 비교**한다.
불일치 0, 늦은 배치 0, 승자·최종 틱 완전 일치.

| 조건 | 결과 |
|---|---|
| 시드 1~5 / 지연 0ms | 전부 일치 (1453~2700틱) |
| 지연 250ms | 일치, 대기 0프레임 |
| 지연 500ms | 일치, **대기 6,800프레임** |
| 지연 1000ms | 일치, 대기 13,571프레임 |
| 지연 300ms / 입력지연 3틱 | 일치, 대기 16,730프레임 |
| 지연 700ms / 입력지연 6틱 | 일치, 대기 17,168프레임 |

지연이 입력 지연을 넘는 조합을 일부러 넣었다. **대기 경로가 안 밟히면 검증이 헐겁다** —
실제로 0/250ms 조합만으로는 stall 코드가 한 번도 안 돌았다.

**검증 중에 두 번 헛발질했다. 둘 다 하네스 버그였다:**
1. 명령을 프레임 단위로 냈더니, 대기 후 따라잡기 구간에서 여러 틱을 건너뛰어
   양쪽이 다른 명령을 냈다 → 명령은 **틱 루프 안에서** 내야 한다
2. 해시를 30틱마다만 비교했더니 대기 구간에서 검사가 2회밖에 안 됐다 → 매 틱 전수 비교로 바꿈

`lockstep-check.ts` 의 프레임 루프는 `MatchScene.frame` 을 그대로 옮긴 것이다.
**둘이 어긋나면 이 검증은 아무것도 증명하지 못한다. `MatchScene` 을 고치면 여기도 고칠 것.**

### `MatchScene` 이 바뀐 점

- `MatchPlan`(`{mode:'pve'}` | `{mode:'pvp', setup, transport}`)을 받아 판을 구성한다
- `InputController` 를 `restart()` 에서 만든다 — PVP는 내가 P2일 수 있어 `local` 이 판마다 다르다
- 시뮬레이션이 못 돌 때(`commandsFor` 가 `null`) 루프를 빠져나오고 시간을 누산기에 남긴다
- 누산기 상한 0.5초. 오래 기다린 뒤 수백 틱을 한 프레임에 몰아 돌면 화면이 얼어붙는다

### 진입 UI — 붙였다 (`app/pvp-scene.ts`)

```
로비 [PVE 시작] [PVP 대전] [상점]
                  ↓
           PVP 매칭 화면 — 연결 → 방 참가 → 상대 대기 → 시작
           [방 코드 입력] [참가]  [취소]
```

이 씬은 **아무것도 결정하지 않는다.** 시드도 플레이어 번호도 시작 시점도 서버가 정한다.
방 상태를 구독하다 `phase === 'playing'` 이 되면 `setupFrom(state)` 로 조건을 뽑아
매치 씬에 넘기고 손을 뗀다.

**방 코드로 참가하는 입력을 넣은 이유**: 무작위 매칭은 접속자가 없으면 시험할 수가 없다.
한쪽이 만든 방 코드를 다른 창에 넣으면 둘이 붙는다.

취소하고 나갈 때 `leaveMatch()` 를 부른다. 안 부르면 빈 방이 매칭 후보로 남아
다음 사람이 유령과 짝지어진다.

#### `connect()` 는 실패해도 거부하지 않는다 — 타임아웃이 필수다

`.env` 에 `VITE_AGENT8_VERSE` 가 없으면 `GameServer.connect()` 가 콘솔에 경고만 찍고
**영원히 안 끝난다.** 처음 붙였을 때 매칭 화면이 "연결하는 중…"에 갇혀 원인이 안 보였다.
`agent8.ts` 의 `withTimeout`(8초)이 `connect`/`findMatch`/`setReady` 를 감싼다.

배포 전 현재 동작 (확인함): [PVP 대전] → 8초 후
`연결하지 못했습니다 / 서버 연결 응답이 없습니다 (8000ms)` → [취소]로 로비 복귀.

**PVP는 아직 양쪽 다 강화 없음으로 붙는다.** 계정 강화를 클라이언트가 보내면 만렙을
자칭할 수 있어서다. 서버가 정해 내려주는 구조로 바꿔야 한다.

### 배포 후 가장 먼저 확인할 것

1. `npm run deploy:server` → `.env` 에 `VITE_AGENT8_VERSE` 생겼는지
2. 창 두 개로 [PVP 대전] → 한쪽 방 코드를 다른 쪽에 넣어 참가 → 판이 시작되는지
3. `server.js` 스텁 가정 둘 (§-5): `$global.updateRoomState` 병합 방식,
   `$roomTick` 에서 `$global.getRoomState` 가 `$users` 를 채우는지
4. 실제 왕복 지연을 재서 `INPUT_DELAY_TICKS`(12) 조이기

---

## -5. `server.js` 추가 — PVP 뼈대 (2026-07-29)

루트에 `server.js`. Verse8 게임서버 SDK 규약대로 `class Server` 를 **정의만 하고 export 안 한다.**

### 하는 것 / 안 하는 것

**한다**: 매치메이킹(1v1), 준비/시작, 시드·플레이어 번호 합의, 명령 릴레이, 결과 집계, 데싱크 감지.
**안 한다**: 시뮬레이션. **아직 서버 권위가 아니다.**

지금 구조는 **결정론적 락스텝**이다. 양쪽 클라이언트가 같은 시드로 같은 `sim/` 을 돌리고
서버는 명령만 넘긴다. `sim/` 이 브라우저 API를 안 쓴 이유(§3)가 여기서 값을 한다 —
봇 명령 자리에 릴레이가 그대로 들어간다.

### 문서에서 확인한 제약 세 가지가 설계를 결정했다

1. **remote function 이 초당 약 10회 제한.** 30Hz 매 틱 전송은 불가능하다.
   → 명령을 ~100ms 단위로 모아 보내고, 배치마다 **실행할 틱 번호**를 붙인다.
   벽시계가 아니라 틱 번호로 맞추므로 양쪽 시작 시각이 어긋나도 결과가 안 갈라진다.
   상대를 기다려야 하므로 `INPUT_DELAY_TICKS = 12` (0.4초) 만큼 앞선 틱에 예약한다.
   **명령이 없어도 빈 배치를 보내야 한다** — 그게 "이 틱까지 내 명령 없음" 신호다.
2. **`setTimeout`/`setInterval` 금지.** 시간에 걸린 판단은 전부 `$roomTick` 안에 있다
   (자동 시작, 무응답 타임아웃, 방치된 방 정리).
3. **클래스 변수가 요청마다 초기화된다.** 그래서 이 파일은 상태를 안 든다.

### 왜 준비 상태까지 룸 상태에 넣었나

`$roomTick(deltaMillis, roomId)` 에는 요청 맥락이 없어 `$sender` 도 `$room` 도 못 쓴다.
룸을 지목해 읽는 documented API 는 `$global.getRoomState(roomId)` 뿐이다.
룸 유저 상태(`$room.getAllUserStates()`)는 `$roomTick` 에서 읽을 방법이 문서에 없어서
준비 여부·마지막 수신 시각까지 전부 룸 상태의 `players` 맵에 넣었다.
**룸 상태 갱신을 얕은 병합으로 가정했다** — 중첩 객체는 읽어서 통째로 다시 쓴다(`#patchPlayer`).

### 서버가 정하는 것 (클라이언트를 안 믿는 자리)

- **시드와 플레이어 번호.** 클라이언트가 정하면 둘이 다른 맵을 만들거나 둘 다 P1이 된다.
  번호는 계정 문자열 순 — 접속 순서로 주면 재접속 때 뒤집힌다
- **명령의 `player` 필드.** 클라이언트가 보낸 값을 덮어쓴다. 안 그러면 남의 번호로 명령을 낼 수 있다

### 검증 — 배포 없이 21건 통과

```bash
npm run test:server
```

`tools/server-harness.mjs` 가 `$global`/`$room`/`$sender` 를 메모리 스텁으로 물리고
`vm` 으로 `server.js` 를 평가해 흐름을 돌린다 (파일은 export 가 금지라 소스를 읽어
노출 한 줄만 붙인다. **원본은 안 건드린다**).

방 생성·합류·진행 중인 방 걸러내기·한쪽만 준비면 미시작·둘 다 준비 시 시작·시드 범위·
슬롯 배정·명령 릴레이·`player` 덮어쓰기·빈 배치 수용·ackTick·해시 일치/불일치·결과 기록·
끝난 판 명령 거부·진행 중 이탈 부전승·무응답 타임아웃 — 21/21 PASS.

**실제 Verse8 배포로는 검증 못 했다.** 스텁이 문서를 읽고 만든 가정이라,
배포 후 `ping()` 부터 확인하고 특히 이 둘을 볼 것:
`$global.updateRoomState` 의 병합 방식, `$roomTick` 안에서 `$global.getRoomState` 가
`$users` 를 채워 주는지.

### 클라이언트 — 바닐라로 붙는다 (2026-07-29 실물 확인)

**React 없이 된다.** `@agent8/gameserver@1.10.2` 는 `GameServer` **클래스**를 패키지 루트에서
export 한다. `react`/`react-dom` 은 peerDependency지만 훅에만 필요하고,
`dist/src/server/GameServer.js` 는 React를 전혀 import 하지 않는다
(lz4js / lodash / @msgpack/msgpack / ws 뿐).

문서의 시작하기 예제(`useGameServer` 훅)만 보고 React가 필수라고 판단하면 안 된다.
VXShop 문서의 "Vanilla" 페이지는 **다른 패키지**(`@verse8/platform`) 이야기라 참고가 안 된다.

확인한 것 (`import { GameServer } from '@agent8/gameserver'` 한 줄을 넣고 빌드·실행):

- `npm run build` 통과. React 미설치 상태에서 번들된다
- 번들이 32.45 kB → **98.61 kB** (gzip 12.18 → 32.19 kB). SDK 값이다
- 브라우저에서 `typeof GameServer === 'function'`. ESM 파일 안에 섞인 `const ws = require("ws")`
  가 브라우저에서 터지지 않는 것까지 확인
- 콘솔 경고: `[GameServer] VITE_AGENT8_VERSE is required for preview environment.`
  → **`npm run deploy:server` 가 만드는 `.env` 가 있어야 접속이 시작된다. 이게 다음 관문이다**

확인 후 probe 파일은 지웠다. 지금 소스에는 SDK를 부르는 코드가 없다 (번들도 32.45 kB로 복귀).

붙일 때 쓸 API (`GameServer.d.ts` 에서 확인한 실제 시그니처):

```ts
GameServer.getInstance(config?) / new GameServer(config?)   // config: { verse, account, remoteUrl, auth, onDisconnect, onError }
connect(config?): Promise<boolean>
remoteFunction(fn, args?, { needResponse?, throttle?, throttleKey? }): Promise<any>
onRoomMessage(roomId, type, cb): UnsubscribeFunction
subscribeRoomState(roomId, cb) / subscribeRoomAllUserStates(roomId, cb)
onRoomUserJoin(roomId, cb) / onRoomUserLeave(roomId, cb)
```

남은 작업:

- `npm run deploy:server` → `.env` 의 `VITE_AGENT8_VERSE` 확보 → `ping()` 으로 왕복 확인
- 접속 래퍼 모듈 (`game/src/net/`)
- `MatchScene` 의 봇 자리에 릴레이를 꽂고, 상대 `ackTick` 이 밀리면 시뮬레이션을 멈추는 대기 로직
- 상태 해시 함수 (`DESYNC_CHECK_TICKS = 30` 마다 1회)
- 실측 후 `INPUT_DELAY_TICKS` 조이기

---

## -4. 상점을 별도 화면으로 분리 + 유닛 생김새 (2026-07-29)

사용자 지시: **"상점란으로 따로 빼주고 상점에는 생산속도와, 유닛종류들이 여러가지있어
유닛종류는 우선 너가 마땅한 에셋만 넣어놔"**

### 화면 구조가 하나 늘었다

```
로비 [자동 매칭] [친구랑 하기] [상점] → 매치 → 승부 → [다시 하기] | [로비로]
         ↓             ↓          ↕
    상대를 찾는 중   [방 만들기]  상점[돌아가기]
    (실패 → 봇전,   [코드 참가]
     화면은 동일)   (실패 → 오류)
```

PVP는 [다시 하기]가 없다 — 같은 상대와 같은 시드로 다시 시작할 수 없다.

`app/shop-scene.ts` 신규. 로비는 재화·전적만 보여주고 갈 곳만 고른다.
상점은 `.screen-scroll` 이라 항목이 늘어 화면을 넘기면 스크롤된다.

### 파는 것 두 가지 — 성격이 다르다

| | 영구 강화 | 유닛 생김새 |
|---|---|---|
| 무엇 | 생산 속도 (`UpgradeKind`) | 전사·일꾼·궁수·수도사·창기병 |
| 매치 결과 | **바꾼다** (`PlayerMods`로 sim에 들어감) | **안 바꾼다. 순수 외형** |
| 사는 방식 | 단계별 누적 | 한 번 사면 소유, 언제든 갈아입기 |
| 어디까지 감 | `sim/` | `render/` 까지만 |

**유닛 종류는 지금 성능이 전부 같다.** 카드에도 "생김새만 바뀐다"고 써 뒀다.
종류마다 성능을 다르게 만들 거면 `render/`에 숫자를 넣지 말고 `sim/`에 규칙을 넣어
`PlayerMods`로 흘려보내야 한다 — 렌더러가 게임 규칙을 들면 서버 권위 PVP에서 화면과 실제가 어긋난다.

### 카탈로그는 `game/src/units.ts` 한 곳

`account/`(가격·소유)와 `render/`(프레임 수·크기 보정)가 같은 표를 봐야 해서 어느 쪽도 아닌
최상위에 뒀다. **`sim/`은 이 파일을 몰라야 한다.**

| 종류 | 가격 | 프레임 | 추출 크기 | scale |
|---|---|---|---|---|
| 전사 warrior | 0 (기본) | 6 | 93×91 | 1 |
| 일꾼 pawn | 400 | 6 | 66×77 | 0.9 |
| 궁수 archer | 900 | 4 | 73×90 | 1 |
| 수도사 monk | 1500 | 4 | 83×71 | 0.92 |
| 창기병 lancer | 2400 | 6 | 72×149 | 1.45 |

**프레임 수가 종류마다 다르다.** 원본 러닝 시트가 그렇다 (Archer·Monk는 4프레임).
없는 프레임을 채워 넣으면 달릴 때 발이 끌린다. `Sprites.unit(owner, kind, frame)`이
종류별 프레임 수로 감는다.

**`scale`이 필요한 이유**: 추출을 전 프레임 합집합 상자로 하기 때문에 종류마다 세로 비율이
다르다. 창기병은 창이 위로 뻗어 72×149라, 높이를 그대로 맞추면 사람이 절반 크기로 보인다.

### 생김새는 `MatchState`에 넣지 않았다

`Renderer.setUnitKinds({1, 2})` 로 렌더러만 든다. 매치 상태에 넣으면 순수 외형이
서버 권위 PVP의 검증 대상이 되어 버린다. 봇(P2)은 항상 기본 생김새라 상대가 뭘 입었는지로
헷갈릴 일이 없다.

### 에셋 추출

`scratchpad/export_units.ps1` (세션 스크래치패드라 사라진다 — 다시 필요하면 재작성).
System.Drawing 으로 시트를 통째로 LockBits 해서 알파 합집합 상자를 구하고 프레임마다 잘라 저장.
**부분 사각형으로 LockBits 하면 AccessViolation 이 난다** — Scan0/Stride 조합이 버퍼 밖을 가리킨다.
시트 전체를 한 번만 잠글 것.

```
game/public/assets/unit/{p1,p2}/{warrior,archer,lancer,monk,pawn}/run{0..N}.png
```

옛 평면 경로 `unit/{p1,p2}/run*.png` 는 지웠다. 52파일 0.076MB.
warrior 추출 크기가 예전과 같은 93×91로 나와 추출 규칙이 그대로임을 확인했다.

### 저장 형식 v4

`ownedUnits: UnitKind[]`, `unitKind: UnitKind` 추가. v1~v4를 전부 읽어 살린다.
읽을 때 소유 목록의 중복을 제거하고, 안 가진 것이 착용돼 있으면 기본 생김새로 되돌린다.

### 검증

- `npx tsc --noEmit` 통과, 콘솔 에러 0
- 유닛 에셋 **52경로 전부 HTTP 200**, 옛 평면 경로는 `text/html`(Vite 폴백) = 실제로 사라짐
- v3 저장본(코인 1500, `speedLevel` 1) → v4 마이그레이션, 코인·전적·강화 단계 보존
- 로비 [상점] → 상점 열림 / [돌아가기] → 로비
- 일꾼 400 구매 → 코인 1500 → 1100, 자동 착용, `ownedUnits: ['pawn']` 저장
- 전사 재착용 → 코인 그대로, 일꾼 재착용 → 코인 그대로 (이미 가진 것은 착용만)
- 못 사는 카드는 `disabled` + 가격 회색, 착용 중 카드는 `is-equipped`로 강조
- **렌더러 실제 출력 확인**: 오프스크린 캔버스에 `Renderer`를 띄우고 `drawImage`를 가로채
  `setUnitKinds({1:'lancer',2:'monk'})` 전후를 비교 —
  `p1/warrior/run2 + p2/warrior/run4` → `p1/lancer/run2 + p2/monk/run0`.
  4프레임짜리 monk가 run4를 요구하지 않고 제대로 감기는 것까지 확인

**매치 화면 스크린샷은 못 찍었다.** 브라우저 패널이 표시돼 있지 않아 `document.hidden`이라
rAF가 멈춘다. 위 렌더러 검증이 그 자리를 대신한다. 실제 화면 확인은 사용자가 패널을 열어야 한다.

---

## -3. 전투력 강화 축 제거 (2026-07-29, 사용자 지시)

**"전투력강화는 없애".** 로비 상점의 축은 이제 **공속 하나뿐이다.**
전투력을 축으로 만들려는 시도는 이걸로 두 번째였다 (매치 내 등급 상점 → 로비 영구 강화).

> ⚠ **2026-07-31에 사용자 지시로 세 번째가 왔다 (§-20).** 여기 있던 "되살리지 말 것"은
> 해제됐다. 다만 **로비 영구 강화로는 되살리지 말 것** — 지금 이 축을 든 것은
> **유닛 종류**(`units.ts` 의 `power`)이고, 같은 축이 둘이 되면 §-0.5에서 매치 내 등급
> 상점을 지웠던 이유가 그대로 재현된다. 아래 실측치와 설명은 지금도 유효하다.

이유는 §-1·§-0.75에 남아 있다. 요약: 충돌이 `min(a.power, b.power)`라 전투력이
조금이라도 높은 쪽 유닛은 정면 교환에서 절대 먼저 안 죽는다. 조절 가능한 축이 아니라 계단이다.

### 지운 것

| 파일 | 지운 것 |
|---|---|
| `sim/types.ts` | `PlayerMods.unitPower`, `Unit.tier`. `PlayerMods`는 이제 `{ speedMul }` 하나 |
| `sim/config.ts` | `UNIT_POWER_MAX` |
| `sim/sim.ts` | `emitUnits`가 항상 `power: 1`로 만든다 |
| `account/account.ts` | `Account.unitPowerLevel`, `UpgradeKind`의 `'unitPower'`, 가격표·라벨 |
| `app/difficulty.ts` | 봇 전투력 일치 로직 (이제 공속 반 단계 지연만) |
| `render/renderer.ts` | `powerMark`, 전투력 2 유닛 확대(1.28배)와 발밑 전투력 점 |
| `index.html` | `#buy-unitPower` 행 |

`Unit.power`는 남는다 — 타워 방어력과 비교하는 양이고 충돌 맞교환의 대상이다. 항상 1로 태어난다.

### 저장 형식 v3

`SCHEMA_VERSION` 3. v1(강화 없음)·v2(전투력+공속)·v3를 전부 읽어서 살린다 —
버전이 다르다고 기본값으로 떨어뜨리면 재화가 날아간다.
**v2의 `unitPowerLevel`은 버린다. 거기 쓴 코인은 환불하지 않는다** (프로토타입이라 실사용자 없음).

### 검증

- `npx tsc --noEmit` 통과, 전 소스에 `unitPower`/`tier` 잔재 0 (마이그레이션 주석만 남음)
- v2 저장본(코인 1200, `unitPowerLevel: 2`, `speedLevel: 1`) → v3로 읽힘, 코인·전적·공속 단계 보존
- 상점 행이 `buy-speed` 하나만 남음, 구매 1200 → 500 / `speedLevel` 2 / 표시 `×1.08 → ×1.12`
- 보정 주입 사람 ×1.08 / 봇 ×1.06 (반 단계)
- **결정론 확인**: 같은 시드를 두 번 돌려 최종 상태 JSON 완전 일치
- `Unit` 필드에서 `tier` 사라짐, 콘솔 에러 0

---

## -2. 배출 쿨다운 버그 수정 — sim 밸런스가 바뀌었다 (2026-07-29)

**`emitUnits`가 쿨다운을 `=`로 재설정해 초과분을 버리고 있었다.** 그래서 실제 배출 간격이
틱 단위로 올림돼(30Hz) 두 가지 문제가 있었다:

1. 배출률이 `emit`보다 항상 낮았다. §4의 "gen = emit, 경로 1개 = ±0"이 실제로는 성립 안 했다
2. **공속 강화가 계단으로 뭉개졌다.** 타워 레벨별 실제 배출 틱 수:

| 타워 레벨 | 속0 | 속1 | 속2 | 속3 | 속4 | 속5 |
|---|---|---|---|---|---|---|
| 1 | 24 | 23 | 22 | 21 | 20 | 20 |
| 2 | 16 | 16 | 15 | 15 | 14 | 14 |
| 3 | 12 | 12 | 11 | 11 | 10 | 10 |
| 4 | 9 | 9 | 9 | 8 | 8 | 8 |
| 5 | **7** | **7** | **7** | **7** | **7** | 6 |

레벨 5 타워에서는 공속 1~4단계가 **아무 효과도 없었다.** 강화 단계별 승률이 요철
(72.5 / 95 / 77.5 / 100 / 65)이었던 원인이 이것이다.

수정: `r.cooldown += emitIntervalOf(...)` — 초과분을 남겨 평균 간격이 정확해진다.
같이 고친 것: `applyToggleRoute`가 첫 쿨다운에 소유자의 `speedMul`을 안 넣고 있었다.

### 같은 시드(1~30) 전후 비교

| 지표 | 수정 전 | 수정 후 |
|---|---|---|
| P1 승 | 14/30 | 12/30 |
| 판당 충돌 | 175.5 | 198.2 |
| 판당 점령 | 46.0 | 45.4 |
| 판당 중계 | 6.5 | 10.1 |
| 평균 최대 유닛 | 65.2 | 67.0 |
| 평균 최고 레벨 | 4.57 | 4.77 |
| 승자 일치 | — | 18/30 |

**§6의 기준 수치는 전부 이 값으로 갈아야 한다.** 배출이 빨라졌으니 충돌·중계가 느는 것은
방향이 맞다. 승자가 30판 중 12판 바뀌었지만 이건 회귀가 아니라 의도한 규칙 변경이다 —
칼날 위 게임이라 배출 한 틱이 승부를 가른다.

수정 후 공속 승률은 단조로워졌다 (1단계 차 86.7~91.7%, 2단계 차 96.7~100%).

---

## -1. 영구 강화 폭이 판을 끝낸다 (2026-07-29)

**로비 상점은 코드로는 다 붙었다.** 충돌 규칙은 사용자 결정대로 그대로 두기로 했다(아래).
봇 난이도는 §-0.75에서 처리했다. PVP 정규화는 아직 착수 안 함.

**아래 첫 표는 §-2 수정 전 측정치다.** 방향(공속 2단계면 90%대, 전투력 1단계면 100%)은
수정 후에도 같지만 숫자는 조금 다르다. 수정 후 값은 §-0.75 표를 볼 것.

### 측정 결과 (시드 1~30 × 좌우 거울 = 60판, 봇 대 봇)

강화를 한쪽에만 주고 **강화받은 쪽의 승률**을 쟀다. §6의 올바른 진영 테스트대로
강화와 명령 우선권을 함께 좌우로 옮겼다. **기준선(양쪽 강화 0)은 45%다.**

| 강화 | 승률 | 평균 타워차 |
|---|---|---|
| 없음 (기준선) | 45% | -0.23 |
| 공속 ×1.02 | 55% | +0.05 |
| 공속 ×1.03 | 68.3% | +0.45 |
| 공속 ×1.05 | 93.3% | +2.93 |
| **공속 1단계 ×1.04** | **66.7%** | +0.58 |
| **공속 2단계 ×1.08** | **93.3%** | +4.90 |
| **공속 5단계 ×1.20** | **100%** | +10.03 |
| 전투력 1.05 | 93.3% | +4.53 |
| 전투력 1.1 | 100% | +7.63 |
| **전투력 2** | **100%** | **+12.0 (전멸)** |
| 전투력 3 | 100% | +12.0 |

**지금 설정된 상한(공속 +20%, 전투력 3)은 사는 순간 판이 끝난다.**
가격으로 막을 수 있는 문제가 아니다 — 살 수 있게 만든 이상 언젠가는 산다.

### 왜 이렇게 가파른가 (원인은 코드에 있다)

**전투력**은 축 자체가 연속적이지 않다. 충돌은 `traded = Math.min(a.power, b.power)` 인데
`power > 0` 이면 유닛이 살아남는다. 전투력이 1.05만 돼도 정면 교환에서 **내 유닛만 살아남는다** —
1.05짜리는 적 1기를 확실히 죽이고 두 번째와 부딪혀야 죽는다. 즉 최소 증분이 이미 교환비 2:1이다.
0.05를 주든 1을 주든 "내 유닛은 1:1로 안 죽는다"는 같은 결과라 미세 조정이 불가능하다.

**공속**은 경제가 칼날 위(§4, `gen === emit` 이라 경로 1개 = ±0)라 3%부터 눈덩이가 된다.
승률 50%대를 유지하려면 총 강화 폭이 +2% 안쪽이어야 하는데, 그건 플레이어가 느낄 수 없는 크기다.
**"완만한 곡선"이라는 완화책(§1)은 이 게임에서 성립하지 않는다.**

### 양쪽에 같은 강화를 걸면 (= 전투력 매칭이 성립하는가, 시드 1~30)

| 강화 (양쪽 동일) | P1 승 | 판당 충돌 | 판당 점령 | 판당 중계 | 평균 최대 유닛 |
|---|---|---|---|---|---|
| 없음 | 14/30 | 175.5 | 46.0 | 6.5 | 65.2 |
| 공속 5단계 | 15/30 | 256.0 | 45.1 | **27.2** | 82.7 |
| 전투력 2 | 12/30 | 485.2 | 44.2 | **208.1** | 97.7 |
| 전투력 3 + 공속 5 | 13/30 | 587.7 | 37.3 | **259.3** | 134.0 |

승패는 어느 조합에서도 반반이라 **전투력 기반 매칭을 하면 PVP는 성립한다.**
다만 전투력 2를 양쪽에 걸면 중계가 6.5 → 208로 32배 터진다 — 매치 내 등급 상점을
비용 40으로 뒀을 때 판을 망가뜨렸던 바로 그 증상(§4)이다. 판이 안 끝나는 게 아니라
"타워가 계속 상한에 붙어 유닛이 튕겨 다니는" 다른 게임이 된다.

### 사용자 결정 (2026-07-29)

**"전투력 2면 일반유닛 2명이랑 부딪혀야 없어지는 걸로 구현."**
충돌 모델을 바꾸자는 제안은 채택되지 않았다. **이 규칙은 이미 그대로 구현돼 있다** —
`resolveCollisions`의 `traded = Math.min(a.power, b.power)`가 정확히 그 동작이다.
전투력 2 유닛은 1기를 죽이고 `power`가 1로 남았다가 두 번째에 함께 사라진다.

실측 (시드 1~5, P1에게만 강화):

| 강화 | P1 유닛 소멸 | P2 유닛 소멸 | 교환비 |
|---|---|---|---|
| 없음 | 720 | 724 | 1.01 |
| 전투력 2 | 144 | 358 | **2.49** |
| 전투력 3 | 46 | 219 | **4.76** |

2.49가 2.00보다 큰 것은 정상이다 — 한 번만 부딪히고 목적지까지 살아 도착하는 유닛이 있어서다.

**따라서 sim 코드는 손대지 않는다.** 대신 §-1 첫 표의 결과(전투력 2 = 승률 100%)는
그대로 남는다. 규칙이 아니라 **강화를 살 수 있게 한 것 자체**가 승률을 100%로 만드는 것이므로,
남은 대응은 둘뿐이다:

- **PVP**: 서버가 양쪽 `PlayerMods`를 정규화하거나, 전투력 단계로 매칭한다.
  §-1의 "양쪽 동일" 표대로 같은 단계끼리 붙이면 승패는 반반으로 돌아온다
- **PVE**: 봇(P2)도 강화 단계에 맞춰 세져야 한다. 지금은 봇이 항상 기본값이라
  전투력 2를 사는 순간 캠페인이 끝난다

### 잠정치로 붙여 둔 가격

`account/account.ts`의 `UPGRADE_COSTS`. 위 결정이 나면 다시 정해야 한다.

```
unitPower: [2500, 8000]
speed:     [300, 700, 1300, 2200, 3500]
```

승리 보상이 100 + 타워×8 이라 판당 대략 150~180이 들어온다.

---

## -0.75. 봇 난이도 — 사람의 강화 단계를 따라간다 (2026-07-29)

`game/src/app/difficulty.ts` 새 파일. `matchModsFor(account)` 가 사람과 봇 몫을 한꺼번에
만들어 `createMatch` 로 넘어간다. 계정 데이터도 시뮬레이션 규칙도 아니라 `app/`에 뒀다 —
스테이지 캠페인이 붙으면 스테이지 번호도 여기로 들어온다.

### 규칙 (두 축을 다르게 다룬다)

**전투력: 봇이 사람과 정확히 같다.** 조절 가능한 축이 아니라 계단이다.
충돌이 `min(a.power, b.power)`라 조금이라도 낮은 쪽 유닛은 정면 교환에서 항상 먼저 죽는다.
사람 전투력 2 기준 실측(사람 승률):

| 봇 전투력 | 1.0 | 1.5 | 1.7 | 1.85 | 1.95 | **2.0** |
|---|---|---|---|---|---|---|
| 사람 승률 | 100% | 100% | 100% | 95% | 95% | **45%** |

1.95에서도 사람이 95% 이긴다. **봇 공속을 +20%까지 올려도, 봇 판단주기를 0.5→0.1로 줄여
최고 실력으로 만들어도 전부 100%였다.** 다른 어떤 것으로도 못 메운다.
그래서 **전투력 구매는 PVE에서 이득이 0이다.** 축의 성질이지 버그가 아니다.

**공속: 봇이 반 단계 뒤처진다** (`BOT_SPEED_LAG = 0.5`).
한 단계(+4%)가 통째로 승부를 가르므로 정수 지연으로는 조준이 안 된다. 시드 1~30 × 좌우 거울 = 60판:

| 봇 지연 | 사람 속1 | 사람 속3 | 사람 속5 |
|---|---|---|---|
| 0 (동등) | 40% | 40% | 40% |
| 0.15 | 43.3% | 61.7% | 50% |
| **0.5** | **66.7%** | **68.3%** | **68.3%** |
| 0.75 | 90% | 81.7% | 81.7% |
| 1 | 86.7% | 91.7% | 90% |

0.5가 목표대(65~70%)에 들어오면서 **강화 단계가 올라가도 안 흔들리는** 유일한 값이다.
봇 보정은 저장되는 값이 아니라 매치마다 계산되므로 소수여도 된다
(`speedMulFor`는 단계를 내림하므로 `1 + SPEED_STEP * level`로 직접 계산한다).

### 출하 규칙 최종 측정 (시드 1~30 × 좌우 거울 = 60판, §-2 수정 반영)

| 계정 | 사람 | 봇 | 사람 승률 | 판당 중계 | 판당 점령 |
|---|---|---|---|---|---|
| 전0/속0 | 전1 ×1.000 | 전1 ×1.000 | 40% | 10.2 | 45.5 |
| 전0/속1 | 전1 ×1.040 | 전1 ×1.020 | 66.7% | 5.2 | 46.0 |
| 전0/속3 | 전1 ×1.120 | 전1 ×1.100 | 68.3% | 28.2 | 46.3 |
| 전0/속5 | 전1 ×1.200 | 전1 ×1.180 | 68.3% | 37.3 | 47.9 |
| 전1/속0 | 전2 ×1.000 | 전2 ×1.000 | 46.7% | **189.4** | 44.9 |
| 전1/속1 | 전2 ×1.040 | 전2 ×1.020 | 81.7% | **172.6** | 43.5 |
| 전1/속3 | 전2 ×1.120 | 전2 ×1.100 | 60% | **227.1** | 44.2 |
| 전1/속5 | 전2 ×1.200 | 전2 ×1.180 | 73.3% | **252.7** | 41.7 |
| 전2/속0 | 전3 ×1.000 | 전3 ×1.000 | 43.3% | **226.1** | 38.6 |
| 전2/속1 | 전3 ×1.040 | 전3 ×1.020 | 75% | **227.5** | 37.0 |
| 전2/속3 | 전3 ×1.120 | 전3 ×1.100 | 70% | **311.0** | 39.9 |
| 전2/속5 | 전3 ×1.200 | 전3 ×1.180 | 51.7% | **284.8** | 34.2 |

**전투력을 안 산 줄(전0)은 깨끗하다** — 40 → 66.7 → 68.3 → 68.3, 단계와 무관하게 평평.

**전투력을 사면 무너진다.** 양쪽 전투력이 2 이상이면 중계가 10 → 190~310으로 20~30배 터진다.
타워가 계속 상한에 붙어 유닛이 튕겨 다니는 다른 게임이 되고, 점령이 45 → 34까지 줄고,
승률도 51.7~81.7%로 널뛴다. 매치 내 등급 상점을 비용 40으로 뒀을 때와 같은 증상이다(§4).

즉 **전투력 축은 사도 PVE에서 이득이 없고 판 모양만 망가뜨린다.** 남은 선택은 §-1과 같다.

---

## -0.5. 로비 상점 — 코드는 완료 (2026-07-29)

사용자 결정: **로비에서 유닛 전투력과 공격속도를 영구 구매한다.**
그에 따라 **매치 내 등급 상점은 제거했다** (축이 겹쳐서). 재고는 다시 레벨업 전용 자원.

공속 방식: `gen/emit × (1 + 0.04 × 단계)`, 최대 5단계 = +20%. **단 §-1 참고 — 이 폭은 못 쓴다.**

### 끝난 것

| 파일 | 변경 |
|---|---|
| `sim/types.ts` | `PlayerMods{unitPower,speedMul}` + `defaultMods()`. `MatchState.unitTier` → `mods`. `Command`에서 `buyUnitTier` 제거 |
| `sim/config.ts` | `UNIT_POWER_MAX=3`, `SPEED_STEP=0.04`, `SPEED_LEVEL_MAX=5`, `speedMulFor(level)`. `emitIntervalOf(t, speedMul=1)` |
| `sim/sim.ts` | `createMatch(towers, mods?)`. `produce`가 `speedMul`을 gen에, `emitUnits`가 emit과 `unitPower`에 적용 |
| `ai/bot.ts` | `decideBuyTier`·`TIER_BUY_MARGIN` 제거 |
| `render/renderer.ts` | 상점 버튼·`shopHit`·등급 이벤트 제거. HUD가 `state.mods[x].unitPower`를 읽는다 |
| `render/input.ts` | `shopHit` 분기 제거 |
| `account/account.ts` | `unitPowerLevel`/`speedLevel`, `UPGRADE_COSTS`, `buyUpgrade`, `modsFor`. `SCHEMA_VERSION` 2 |
| `index.html` + `style.css` | 로비 상점 DOM (`#buy-unitPower`, `#buy-speed`) |
| `app/lobby-scene.ts` | 상점 행 렌더 + 구매 클릭. 계정은 콜백으로만 만진다 |
| `app/difficulty.ts` | **신규.** `botModsFor` / `matchModsFor` — 봇이 사람 강화를 따라간다 (§-0.75) |
| `app/match-scene.ts` | `createMatch(map, getMods())` — 양쪽 보정을 통째로 받는다 |
| `main.ts` | `buy` / `matchModsFor(account)` 콜백 배선 |

**gen과 emit에 같은 배수를 곱하는 것이 중요하다.** emit에만 곱하면
"경로 1개 = ±0"이라는 경제 균형점(§4)이 깨진다.

**저장 형식 v1 → v2는 마이그레이션한다.** 필드가 늘기만 했으므로 버전이 1이어도 읽어서
코인과 전적을 살린다. `loadAccount`의 버전 검사 참고 — 기본값으로 떨어뜨리면 재화가 날아간다.

### 검증 (2026-07-29)

- `npx tsc --noEmit` 통과
- v1 저장본(코인 4200) 로드 → v2로 마이그레이션, 코인 보존 확인
- 공속 3단계 연속 구매: 코인 4200 → 3900 → 1900, `speedLevel` 3, localStorage 반영 확인
- 코인 부족·만렙에서 버튼 `disabled` + 가격이 `최대` / 회색으로 바뀌는 것 확인
- `modsFor(계정)` → `createMatch`에 주입된 `state.mods` 가 `{1:{unitPower:3,speedMul:1.2}, 2:기본값}` 확인
- 저장값이 오염돼도(`unitPowerLevel:99`, `speedLevel:-5`) 상한/0으로 잘리는 것 확인
- 콘솔 에러 0

**화면 스크린샷은 못 찍었다.** 브라우저 패널이 표시돼 있지 않아 `document.hidden === true` 라
rAF가 멈춘다 — 매치 렌더가 한 프레임도 안 돈다. 로비 DOM은 rAF와 무관해서 그대로 검증된다.
매치 화면 확인이 필요하면 사용자가 패널을 열어야 한다 (§0).

### 남은 것

1. **가격 확정** — `UPGRADE_COSTS.speed`는 잠정치 `[300, 700, 1300, 2200, 3500]`
2. **PVP `PlayerMods` 정규화 또는 강화 단계 매칭** — 네트코드 작업에 딸린다
3. **§6 기준 수치 갱신** — §-2 수정으로 값이 바뀌었다

---

## 0. 먼저 읽을 것 (지뢰)

**PowerShell 텍스트 치환을 소스 파일에 절대 쓰지 말 것.**
`(Get-Content -Raw) -replace ... | Set-Content` 를 한 번 썼다가 `renderer.ts` 와 `input.ts` 가 통째로 깨졌다.
PS 5.1이 UTF-8 한글을 시스템 코드페이지로 읽어서 주석이 전부 깨지고 줄이 병합돼 esbuild가
`Unterminated string literal` 로 죽었다. 두 파일을 처음부터 다시 써서 복구했다.
**수정은 Edit / Write 도구로만.**

> **2026-07-28에 같은 사고가 또 났다.** 이번엔 `Get-Content` 로 읽어 줄 배열을 자르고
> `Set-Content -Encoding utf8` 로 되쓰는 방식이었다 — `-replace` 를 안 썼어도 결과는 같다.
> **읽기든 쓰기든 PowerShell을 소스 파일에 대지 말 것.** 줄 삭제 같은 단순 작업도 Edit로 할 것.
> `renderer.ts` 손상은 2026-07-29에 복구됐다 (33,612 바이트). 두 번 다 같은 원인이었다.

**브라우저 패널은 사용자가 앱에서 직접 열어야 스크린샷이 된다.**
패널이 화면에 표시돼 있지 않으면 `computer{action:"screenshot"}` 이 이렇게 실패한다:
```
screenshot failed: the Browser pane is not displayed, so the page is not compositing frames.
```
열려 있지 않으면 사용자에게 열어달라고 요청할 것. 대신 헤드리스 검증은 항상 가능하다 (§6).

**2026-07-31부터 git 저장소다.** 그 전까지는 아니어서 "큰 수술 전에 파일 복사본을 떠 둘 것"
이라고 적혀 있었다 — 이제 커밋이 그 자리를 대신한다. 작업 브랜치는 `dev` 다.

**`assets-src/` 는 `.gitignore` 에 있다.** 51MB인 것도 있지만 **라이선스가 진짜 이유다** —
Tiny Swords 가 에셋 재배포를 금지하고 BeerGang 3D 는 상용 권한이 미확인이다.
저장소를 공개로 돌리기 전에 `game/public/assets/` 안의 뽑아낸 PNG까지 같이 정리할 것
(§-16 · §-18 · §7).

---

## 1. 프로젝트 개요

**무엇**: Tower War - Tactical Conquest (SayGames, `games.vaveda.militaryoverturn`) 를 레퍼런스로 한
웹 실시간 전략 게임. PVP + PVE.

**목표**: 실제 상용 출시. 그래서 서버/매치메이킹/수익화가 범위 안에 있고, 에셋 라이선스도 상용 가능해야 한다.

**결정된 방향** (사용자와 합의 완료):

| 항목 | 결정 |
|---|---|
| 스택 | TypeScript + Canvas 2D. 렌더 라이브러리 없음 |
| 배포 | Verse8 플랫폼 (Vite 구조, `index.html` 이 `game/` 안) |
| PVP | 실시간 1v1, 서버 권위 |
| 깊이 축 | 매치 내 타워 강화 |
| 강화 내용 | **속도만.** 공격력 축은 검토 후 사용자가 거부함 |
| 성장 | 계정 영구 수치 성장. "순수 실력제와 충돌한다"고 알렸으나 사용자가 그대로 진행 결정. 완화책: 전투력 기반 매치메이킹 + 완만한 곡선. **2026-07-29 측정 결과 "완만한 곡선"은 성립하지 않는다 — §-1** |
| PVE | 스테이지 캠페인 |
| 자원 모델 | Plan C 하이브리드 — 레벨업이 병력을 소모 |

---

## 2. 실행

```bash
npm run dev
```

`.claude/launch.json` 에 `towerwar-dev` 로 등록돼 있고 `autoPort: true` 다.
**포트가 고정이 아니다.** 5173이 다른 프로젝트에 잡혀 있으면 다른 포트로 밀린다
(작업 중 54564 → 5173 으로 바뀐 적 있음). `preview_list` 로 실제 포트를 확인할 것.

타입체크:
```bash
npx tsc --noEmit
```

**커밋 전 검증은 이거 하나면 된다** (타입 체크 + 서버 테스트 + 빌드):
```bash
npm run verify
```

> **Windows PowerShell(5.1)에는 `&&` 가 없다.** `npx tsc --noEmit && npm run build` 는
> 거기서 파서 에러다 (§0의 PowerShell 지뢰와 같은 뿌리). `npm run verify` 는 npm이
> 내부적으로 cmd를 태우므로 어느 셸에서든 돈다. 이 PC에는 `pwsh`(7)가 없다 —
> 확인함: `powershell 5.1.26100.8875`, `pwsh 미설치`.

`vite.config.ts` 는 `tsconfig.json` 의 `include` 에서 제외돼 있다 (`@types/node` 없이 통과시키려고).

서버 로직 검증 (배포 없이):
```bash
npm run test:server
```

서버 배포:
```bash
npm run deploy:server
```

### Verse8 구조 — 이미 맞춰져 있다 (2026-07-29 빌드로 확인)

`vite.config.ts` 는 **저장소 루트**에 두고 `root: 'game'` 으로 클라이언트 루트만 옮긴다.
Vite는 CWD에서 설정 파일을 먼저 찾은 뒤 그 안의 `root` 를 적용하므로 설정 파일까지 옮길 필요는 없다.
`publicDir` 과 `build.outDir` 은 **root 기준 상대 경로**다 — 그래서 `outDir: '../dist'` 가
저장소 루트의 `dist/` 로 나간다.

```
vite.config.ts          루트. root:'game', publicDir:'public', outDir:'../dist'
game/index.html         Vite 진입점
game/src/               클라이언트 소스
game/public/assets/     그대로 복사되는 에셋
dist/                   빌드 산출물 (index.html + assets/ 70파일, 0.224MB)
```

`npm run build` → `tsc && vite build` 통과, `dist/index.html` + `dist/assets/` 생성 확인.

루트 `server.js` 는 2026-07-29에 붙였다 (§-5).
**클라이언트 쪽은 아직 안 붙였다** — `@agent8/gameserver` 의존성도 없다.

---

## 3. 파일 구조

```
vite.config.ts          root:'game', build.outDir:'../dist'
tsconfig.json           include: ["game/src"]
server.js               Verse8 게임서버. class Server, export 금지 (§-5)
tools/server-harness.mjs  server.js 로직 검증 (npm run test:server)
.claude/launch.json     towerwar-dev, port 5173, autoPort

game/index.html         #lobby(DOM) + <canvas id="stage"> + #result(DOM)
game/src/main.ts        앱 진입. rAF 루프 하나를 들고 씬 전환만 맡는다
game/src/units.ts       유닛 생김새 카탈로그(가격·프레임 수·크기 보정). sim/은 이걸 모른다
game/src/net/           락스텝 PVP 계층 (§-6). sim/ 은 이 폴더를 몰라야 한다
  local-backend.ts      개발 빌드 전용. 진짜 server.js 를 로컬에서 돌린다 (§-14)
game/src/account/
  account.ts   계정 타입 + 로컬(localStorage) 저장·보상 계산
  store.ts     서버/로컬 중 어디가 진짜인지 정하는 곳 (§-10)
game/src/app/
  scene.ts     Scene 인터페이스 + MatchPlan
  lobby-scene.ts  DOM 로비
  shop-scene.ts   DOM 상점 (영구 강화 + 유닛 생김새)
  difficulty.ts   사람 강화 단계 → 봇 보정
  name-scene.ts   DOM 닉네임 설정. 첫 실행에서 로비보다 먼저 (§-14)
  bot-name.ts     봇 상대 이름 생성. 사람처럼 보이게 만든다 — 기만이다 (§-14)
  pvp-scene.ts    DOM 대전 매칭 화면. 봇 대체를 화면에 안 드러낸다 (§-7)
  command-source.ts  BotSource / NetSource — 봇과 원격 상대를 같은 인터페이스로
  match-scene.ts  고정 타임스텝 루프. 명령의 출처를 모른다
game/src/sim/
  types.ts     (104줄) 도메인 타입. DOM 의존 없음
  config.ts    (124줄) 밸런스 상수 전부. 튜닝은 이 파일만
  geometry.ts   (67줄) 순수 기하. dist / clamp / segmentsIntersect /
                       segmentIntersectionPoint / distToSegment
  sim.ts       (467줄) 결정론적 시뮬레이션
  maps.ts       시드 기반 좌우 대칭 맵 생성 (mulberry32). 배치 유형 5종 (§-11)
  maps-check.ts 맵 검증. 게임 코드 아님 — 빌드에 안 실린다
game/src/ai/bot.ts     (148줄) 사람과 동일한 Command 만 낸다
game/src/render/
  renderer.ts  Canvas 2D. 상태를 읽기만 한다
  sprites.ts   스프라이트 로딩 / 여백 자동 제거 / 'color' 합성 진영색 입히기
  input.ts     (210줄) 포인터 → Command

game/public/assets/     배포되는 에셋. Vite publicDir이라 URL이 경로 그대로다
                        tower/{p1,p2,neutral}/lv1~5.png
                        unit/{p1,p2}/{warrior,archer,lancer,monk,pawn}/run{0..N}.png
assets-src/             에셋 원본. game/ 밖이라 빌드에 안 실린다
docs/assets-research.md 에셋 선정 근거 + 측정값 + 통합 기록
```

**`sim/` 은 브라우저 API를 일절 안 쓴다.** 서버 권위 PVP로 갈 때 Verse8 `server.js` 에서
그대로 돌리기 위해서다. 이 규칙을 깨지 말 것.

**봇은 사람과 똑같은 `Command` 객체만 낸다.** 시뮬레이션은 명령이 봇에서 왔는지
네트워크에서 왔는지 구분 못 한다. 그 자리에 원격 플레이어 입력 스트림을 끼우면 PVP가 된다.

---

## 4. 게임 규칙 (현재 확정 상태)

### 경제 — 쌓기 vs 흘리기

- 타워는 재고(`troops`)를 쌓는다. **경로가 하나라도 열려 있으면 그 타워는 재고가 안 는다.**
  생산분이 재고 적립 대신 유닛이 되어 경로로 빠져나간다.
- 재고를 깎는 것은 적의 공격과 레벨업뿐.
- `gen` 과 `emit` 이 레벨마다 같은 값이다. 경로 1개 = ±0, 2개째부터 순이득.
  근데 2번째 경로는 재고 10, 3번째는 20이 필요하고 경로가 열린 동안은 재고가 안 는다. 이게 딜레마의 전부다.
  **이 등식은 2026-07-29(§-2)부터 실제로 성립한다.** 그 전에는 배출 쿨다운이 틱 단위로
  올림돼 emit이 gen보다 조금씩 느렸다.

### 경로 슬롯

`routeSlotsFor(troops) = min(3, floor(troops/10) + 1)` → 1~9: 1개, 10~19: 2개, 20+: 3개.
재고가 줄면 `pruneRoutes` 가 오래된 것부터 강제로 닫는다.

**같은 타워쌍에 경로를 두 개 겹칠 수 없다.** 다시 드래그하면 토글로 꺼진다.
즉 "경로 3개"는 언제나 "목적지 3곳"이지 "한 곳에 3배 화력"이 아니다.

### 상한과 중계

`TOWER_CAP = 60`, 모든 타워 동일. 60에 닿은 타워에 유닛이 도착하면 흡수 못 하고
**그 타워의 경로 중 하나로 그대로 통과한다** (`relayThrough`). 라운드 로빈(`relayCursor`),
돌아온 길 제외, `RELAY_MAX_HOPS = 3`.

### 경로 차단

출발지-목적지 직선에 제3의 타워가 걸치면 개설 불가.
판정: `distToSegment(t, from, to) <= towerRadiusOf(t) + ROUTE_CLEARANCE(6)`.
**개설 시점에만** 검사한다 — 이미 깔린 경로는 나중에 타워가 커져도 유지된다.
`routeBlockedBy()` 를 sim / bot / renderer 가 공유한다. 판정이 두 곳에 있으면 화면과 실제가 어긋난다.

### 충돌 — 최근에 크게 바뀜

```ts
if (a.fromId !== b.toId || a.toId !== b.fromId) continue;
if (dist(pos[i], pos[j]) > COLLISION_RADIUS) continue;
```

**정면 맞물림에서만 충돌한다.** A가 B로 경로를 긋고 B도 A로 그은 그 통로 위에서만.
X자 교차는 아무리 가까워도(실측 0.3px) 그냥 통과한다. 기하학적 교차 판정과 캐시는 전부 제거됐다.

모든 유닛이 전투력 1이라 마주친 둘은 언제나 함께 사라진다. 순수 1:1 소모전.

판정 기준이 "경로"가 아니라 "유닛이 달리는 구간"이라, **경로를 끊어도 이미 뜬 유닛끼리는
마주보면 부딪힌다** (실측 912건 중 145건). 의도된 동작.

### 유닛 등급 (매치 내 상점) — **제거됨** (2026-07-28 추가 → 2026-07-29 제거)

로비 영구 강화와 축이 겹쳐서 통째로 제거했다. `Command 'buyUnitTier'`, `MatchState.unitTier`,
`UNIT_TIER_COST`/`UNIT_TIER_MAX`, `renderer.shopHit`, 봇의 `decideBuyTier` 전부 없다.
**되살리지 말 것** — 재고는 다시 레벨업 전용 자원이다.

아래 기록은 **방법론과 측정치가 지금도 쓸모 있어서** 남긴다. 지금 살아 있는 전투력 축은
`PlayerMods.unitPower`(로비 상점)이고, 그쪽 측정은 §-1에 있다.

- 비용은 새 자원이 아니라 **재고**에서 걷었다. Plan C(성장이 병력을 소모)와 같은 결
- **재고가 많은 타워부터** 걷었다. 적은 쪽을 먼저 비우면 그 타워의 경로 슬롯이 1로 떨어져
  전선이 즉시 무너진다. 동점은 id 순 — 정렬이 흔들리면 서버와 갈라진다
- 충돌 코드는 원래부터 `Math.min(a.power, b.power)` 라 손댈 필요가 없었다.
  등급 2는 등급 1과 부딪혀도 1만 잃어 **두 번 맞아야 죽는다**.
  **이 성질이 §-1에서 전투력 축을 못 쓰게 만든 원인이다**

#### 비용 140을 정한 방법 (방법론 — 지금도 유효)

**수치 표를 보고 감으로 고르지 않았다.** "살 수 있는 즉시 사는 쪽 vs 아예 안 사는 쪽"을
좌우 바꿔가며(명령 우선권도 함께 이동) 시드 1~30 × 2 = 60판씩 돌려 **구매하는 쪽 승률**을 쟀다.
봇의 구매 휴리스틱은 걷어내고 직접 주입했다 — 안 그러면 휴리스틱 임계값이 비용 효과와 섞인다.

| 비용 | 구매 측 승률 | 구매 가능 비율 | 평균 구매 시점 |
|---|---|---|---|
| 40 | **100%** | 100% | 13.6초 |
| 100 | 90% | 93.3% | 40.0초 |
| 120 | 80% | 86.7% | 52.2초 |
| **140** | **53.3%** | 71.7% | 62.2초 |
| 180 | 45% | 21.7% | 80.6초 |

40은 사는 게 무조건 이득이라 선택이 아니라 세금이었다. 180은 90초 안에 거의 못 사서 기능이 죽는다.
**140은 승률이 반반이면서 대부분의 판에서 도달은 가능한 지점**이다.

#### 비용 140에서의 밸런스 (시드 1~30, 봇 양쪽, 구매 켬/끔)

| 지표 | 상점 끔 | 켬 (비용 140) | 참고: 켬 (비용 40) |
|---|---|---|---|
| 판당 중계 | 6.5 | **5.7** | 78.2 |
| 판당 충돌 | 175.5 | 209.5 | 274.6 |
| 판당 점령 | 46.0 | 46.4 | 34.8 |
| 평균 최대 유닛 | 65.2 | 61.4 | 83.6 |
| 평균 최고 레벨 | 4.57 | 4.27 | 4.97 |

비용 40에서 판을 망가뜨리던 **중계 폭증(12배)이 사라졌다.** 등급 2 유닛이 재고를 2씩 넣어
타워를 상한으로 밀어붙이는 것은 같지만, 구매가 60초 이후라 그럴 시간이 없다.

최고 레벨이 4.57 → 4.27로 내려간 것이 이 축의 진짜 대가다. 재고를 등급에 쓰면 레벨업을 못 한다.
**속도(레벨)와 전투력(등급) 중 택일**이 성립한다.

#### 선택이 되었는가

| 결과 | 비용 40 | 비용 140 |
|---|---|---|
| 양쪽 다 구매 | 30/30 | 18/30 |
| 한쪽만 구매 | 0 | 8/30 |
| 아무도 안 삼 | 0 | 4/30 |

판마다 갈렸다. 평균 첫 구매 60.9초.

**진영 대칭성**: 승패 거울 **40/40** (시드 1~40). 비용 40에서 39/40으로 어긋났던 것은
비용을 올리자 사라졌다.

봇의 `TIER_BUY_MARGIN` 도 1.8 → **1.0** 으로 내렸다. 비용 140에서 임계값 252가
한쪽이 실제로 모으는 최대 재고(평균 174.5)를 넘어 봇이 아예 안 샀기 때문이다.
1.0이 정당한 근거는 위 표다 — 즉시 구매 자체가 승률 53.3%라 이미 반반 도박이다.

### 레벨 — **제거됨** (2026-07-30, §-17)

> 아래 표와 설명은 **옛 규칙이다.** 지금은 레벨도 레벨업도 없고 모든 타워가
> `TOWER_GEN = TOWER_EMIT = 1.9`, 반지름 22로 같다. 방법론과 수치는 참고용으로 남긴다.

레벨은 **속도만** 올린다. 유닛 1기는 어느 레벨에서 나왔든 전투력 1
(로비에서 전투력 강화를 사면 그 값이 오른다 — §-1 참고).
고레벨의 우위는 "더 센 유닛"이 아니라 "더 많은 유닛"으로만 나타난다.

```
Lv  gen/emit  다음레벨비용
1   1.3       10
2   1.9       18
3   2.6       30
4   3.4       48
5   4.3       —
```

누적 비용 106인데 상한이 60이다. **만렙을 찍으려면 최소 두 번 재고를 다시 채워야 한다.**
그 사이 슬롯이 줄어 경로가 닫히므로 만렙은 "오래 안전했던 후방"에서만 나온다.

### 제거된 것 (되살리지 말 것)

- **특화 4종** (포병/전차공장/요새/보급소) — 사용자가 명시적으로 제거 요청. 7개 파일 132곳 삭제
- **공격력 축** — 곡선까지 제시했으나 "공속만 늘리는 게 낫겠다"로 결정
- **유닛 전투력 축** — 두 번 만들고 두 번 제거 (매치 내 등급 상점 → 로비 영구 강화). §-3.
  > **2026-07-31에 사용자 지시로 되살아났다 (§-20).** 이번에는 로비 영구 강화가 아니라
  > **유닛 종류가 든다** — 상점에서 산 종류가 `PlayerMods.unitPower` 를 정한다.
  > 위 "되살리지 말 것"은 더 이상 유효하지 않다.
- `unitPowerOf()`, `Tower.spec`, `Tower.reload`, `Command 'specialize'`,
  `MatchState.specSlots`, `TickEvents.specializes`, `TickEvents.shots`

---

## 4.5 화면 구조 (2026-07-28 추가)

```
로비 [자동 매칭] [친구랑 하기] [상점] → 매치 → 승부 → [다시 하기] | [로비로]
         ↓             ↓          ↕
    상대를 찾는 중   [방 만들기]  상점[돌아가기]
    (실패 → 봇전,   [코드 참가]
     화면은 동일)   (실패 → 오류)
```

PVP는 [다시 하기]가 없다 — 같은 상대와 같은 시드로 다시 시작할 수 없다.

설계 문서: `docs/superpowers/specs/2026-07-28-lobby-design.md` (상점 화면은 그 뒤에 붙었다 — §-4)

**rAF 루프는 `main.ts`에 하나뿐이다.** 매 프레임 현재 씬의 `frame(dt)`만 부른다.
매치의 고정 타임스텝 루프는 `MatchScene` 안에 있다. 씬마다 루프를 돌리면 전환 순간에 겹쳐 돈다.

**`Renderer`는 앱 수명 동안 하나만 만든다.** 매치마다 새로 만들면 스프라이트 27장을
매번 다시 로드한다.

**누를 수 있는 것은 전부 DOM이다.** 캔버스는 승리/패배 글자와 어둡게 깔기까지만 그린다.
매치 내 상점 버튼 하나 때문에 `renderer.shopHit`을 손으로 짜고 그 뒤에서 절단 스와이프가
시작되는 버그를 따로 막아야 했다 — 버튼이 늘수록 그 비용이 선형으로 늘어난다.
로비 상점을 DOM으로 만든 이유가 이것이다. 지금 캔버스에는 누를 것이 하나도 없다.

**`InputController.dispose()` 를 씬 나갈 때 반드시 부를 것.** 안 부르면 로비를 다녀올 때마다
리스너가 쌓여 드래그 한 번에 같은 Command가 여러 개 나간다. 경로 토글은 짝수 번이면
없던 일이 되므로 증상이 "가끔 안 먹힘"으로 나타나 원인을 찾기 어렵다.

**`[hidden] { display: none !important }` 가 CSS에 필요하다.** `#stage`에 `display:block`이
걸려 있어 `hidden` 속성만으로는 안 숨는다. 이게 없으면 로비 위를 캔버스가 덮는다.

PVP 버튼은 아직 **화면에 만들지 않았다.** 갈 곳이 없어서다.
로비 상점은 2026-07-29에 붙었다 (§-0.5).

### 계정 + 재화 (2026-07-28 추가)

``game/src/account/account.ts` 한 파일. localStorage 키 `towerwar.account.v1`.

> **2026-07-29에 계정 판정이 서버로 갔다 (§-10).** 아래 내용은 오프라인 경로로 남아 있다 —
> 서버에 붙으면 서버가 진짜이고 localStorage는 사본이다. 분기는 `account/store.ts` 에 있다.

**서버가 아니라 localStorage인 이유**: 서버 저장은 Verse8 PVP 네트코드 작업에 딸려 오는
것이라 그때 같이 옮기는 게 맞다. 옮길 때 손댈 곳이 이 파일 하나가 되도록 저장/불러오기를
전부 여기로 몰아뒀다.

**`sim/`은 이 모듈을 몰라야 한다.** 계정 상태가 시뮬레이션에 흘러들면 서버 권위 PVP에서
클라이언트마다 결과가 갈라진다.

보상 (매치 결과, `rewardFor`):

```
승리 100 / 무승부 50 / 패배 30  +  끝까지 들고 있던 타워 × 8
```

타워 보너스가 붙는 이유: 이겨도 간신히 이긴 판과 압도한 판을 가른다.

**계정은 `main.ts`가 들고 씬은 콜백으로만 건드린다.** 씬마다 localStorage를 직접 읽으면
어느 쪽이 최신인지 알 수 없게 된다.

**보상은 판당 한 번만 들어간다.** `MatchScene.resultShown` 플래그가 그 블록을 막는다.
이 플래그를 건드릴 때 조심할 것.

localStorage는 사생활 보호 모드에서 접근만 해도 던진다. 읽기·쓰기·삭제 전부 try로 감쌌고,
깨진 JSON·버전 불일치·음수/NaN 필드는 전부 기본값으로 떨어진다 (검증 완료).

**저장 형식은 v2다.** v1 저장본은 필드가 늘기만 했으므로 마이그레이션해서 읽는다 —
버전이 다르다고 기본값으로 떨어뜨리면 플레이어 재화가 날아간다.

다음 순서: §-1 수치 결정 → PVE 캠페인 → PVP 네트코드.

## 5. 렌더링

### 진영색

`renderer.ts` 의 `OWNER_COLOR` **한 곳**이 타워·유닛·경로·HUD 색을 전부 결정한다.
타워만 따로 칠하면 경로와 색이 어긋나므로 여기만 고칠 것.

| 진영 | main | 비고 |
|---|---|---|
| 0 중립 | `#8b9bb0` | |
| 1 P1 | `#3fbdf1` 파랑 | |
| 2 P2 | `#f2555f` 빨강 | 2026-07-28에 주황 `#fb923c` 에서 변경 |

**`CUT_COLOR` 는 빨강을 쓸 수 없다.** P2가 빨강이 되면서 "이건 취소된다"는 신호와 적 색이
같아지기 때문이다. 호박색 `#fbbf24` 로 옮겼다 — 절단 궤적, 끊길 경로, 거부된 레벨업이 전부 이 색이다.

**이 표는 경로·유닛 배지·HUD 색만 정한다.** 건물 스프라이트 색은 Tiny Swords 원본에
구워져 있어 이 표를 바꿔도 안 따라온다 (에셋을 다시 뽑아야 한다). 표 값은 실제 스프라이트에서
샘플링해 맞췄다 — 파랑 건물 지배색 `#4090A0`, 빨강 건물 `#E05050`.

### 레벨업 거부 피드백

레벨업 명령이 거부되면 시뮬레이션이 `TickEvents.upgradeRejects` 에
`{ towerId, reason: 'cost' | 'maxLevel', need }` 를 넣는다. 렌더러는 그 타워를 0.55초 동안
좌우로 흔들고(진폭 4.5px), 붉은 링을 퍼뜨리고, 위에 `재고 N 필요` 또는 `최대 레벨` 을 띄운다.

**판정은 sim 한 곳에만 둔다.** 렌더러가 같은 조건을 다시 계산하면 서버 권위 PVP에서
화면과 실제가 어긋난다. `▲비용` 상시 표시를 지운 자리를 이 일회성 라벨이 메운다.

남의 타워·없는 타워에는 거부 이벤트를 내지 않는다. 낼 수 없는 명령인데다 상대 상태를
알려주는 창구가 되기 때문이다. 연타하면 age가 0으로 되돌아가 흔들림이 다시 시작된다.

봇은 조건을 미리 검사하고 명령을 내므로 거부를 만들지 않는다 — 20판 측정에서 **0건**.
즉 봇 대 봇 기준 수치(§6)는 이 변경의 영향을 받지 않는다.

### 제스처 3개

| 입력 | 동작 |
|---|---|
| 내 타워에서 드래그 | 경로 개설/차단 (`toggleRoute`) |
| 내 타워 탭 | 레벨업 (`upgrade`) |
| 빈 곳에서 스와이프 | 가로지른 **내** 경로 절단. 적 경로는 못 자른다 |

**캔버스 위에 누를 것은 없다.** 매치 내 상점 버튼(`renderer.shopHit`)과
길게 누르기(특화 메뉴)는 둘 다 제거됐다. 강화는 전부 로비 DOM에서 산다.

### 타워에 그려지는 것

레벨별 건물 스프라이트 + 발치 바닥 타원(재고 게이지) + 발치 배지의 재고 숫자 + 그 아래 슬롯 점 3개.

레벨은 두 갈래로 드러난다 — 반지름(`16 + level*3`, Lv1 19px ~ Lv5 31px)과 건물 종류
(집 → 큰 집 → 병영 → 석탑 → 성채). 재고는 건물이 아니라 바닥 타원이 맡는다.
재고는 매 틱 변하는 값이라 건물 교체를 걸면 화면이 계속 튀지만, 레벨은 한 판에 많아야
네 번 바뀐다. 타원이 한 바퀴 차면 흰색이 되고 그게 상한 도달 신호다.

**제거됨**: 레벨 눈금 호, `▲비용` 표시, 만석 노란 깜빡 링, 중계 회전 점선 링, 요새 굵은 테두리, 특화 라벨.
전부 사용자 요청. 만석 표시는 재고 링의 흰색 전환이 대신한다.

### 경로 반반 색칠

**정면 맞물림에서만** 색이 갈린다. 단순 X자 교차는 색을 안 바꾼다 (충돌도 안 하므로 일관).

전선 위치 = `headOnFront()`:
```
양쪽에 유닛 있음 → 두 선두의 중점. 같은 속도로 마주 오므로 부딪힐 때까지 안 움직인다
한쪽만 있음     → 그 선두 자신
양쪽 다 없음    → frontMemory 의 마지막 값
```
`frontMemory` 는 Renderer의 `Map<string, Vec>`, 키는 방향 무관하게 정렬된 타워쌍.
`state.tick` 이 되감기면(새 판) 비운다.

`splitPointOn()` 은 `{ mid, whole }` 을 돌려준다. 양쪽 구간이 각각 `SPLIT_MARGIN(14)` px
이상 남을 때만 `mid` 가 있다. 못 나눌 때 `whole` 이 어느 쪽이 통로를 차지했는지 알려주고,
전선이 출발점에 붙었으면(`'far'`) **통로 전체가 상대 색**이 된다 (`overrun`).
상대가 내 문앞까지 밀고 들어온 통로를 내 색으로 칠하면 거짓말이라서.

대시 흐름은 분할 지점에서 `lineDashOffset` 을 앞 구간 길이만큼 밀어 이어붙인다.

---

## 6. 검증 방법

브라우저 패널 없이도 되는 헤드리스 검증이 이 프로젝트의 표준이다.
`mcp__Claude_Browser__javascript_tool` 로 캐시 버스팅한 동적 import:

```js
const v = '?v=' + Date.now();
const S = await import('/src/sim/sim.ts' + v);
const M = await import('/src/sim/maps.ts' + v);
const B = await import('/src/ai/bot.ts' + v);
const C = await import('/src/sim/config.ts' + v);
```

**렌더러 검증은 캔버스 컨텍스트를 Proxy로 가로채서** `moveTo`/`lineTo`/`strokeStyle` 을
기록하면 실제로 화면에 나가는 선분과 색을 좌표까지 그대로 잡을 수 있다.
스크린샷보다 정확하고 패널 없이 된다. 대시 경로는 `getLineDash().join('/') === '9/13'` 로 거른다.

**시각 확인이 필요하면** 전체화면 캔버스를 오버레이로 붙이고 rAF 루프로 계속 그린 뒤 스크린샷.
한 번만 그리면 `ResizeObserver` 가 리사이즈를 걸어 지워버린다. 확인 후 반드시 오버레이 제거할 것.

### 진영 편향 테스트 — 주의

단순히 "강한 봇을 P1에 / P2에" 붙이는 건 **틀린 테스트다.** 명령을 항상 P1 먼저 처리하기 때문에
진영 편향이 아니라 처리 순서 효과를 재게 된다 (완전 대칭 상황은 칼날 위라 순서가 승부를 가른다.
같은 봇끼리 30판 무승부 0판, 순서 뒤집으면 20/20 승자 뒤집힘).

**올바른 테스트**: 강한 봇과 명령 우선권을 **함께** 좌우로 옮긴다. 완전한 거울상이므로
결과도 거울처럼 나와야 한다.

2026-07-28 재측정 (시드 1~40, 강한 봇 vs `BOT_NORMAL`):

| 강한 봇 설정 | 승패 거울 | 소수점까지 일치 | 강한 쪽 승률 |
|---|---|---|---|
| `interval 0.35 / range 380 / feed 16` | **40/40** | 36/40 | 21/40 |
| `interval 0.2 / range 420 / feed 12` | **40/40** | 36/40 | 33/40 |
| `interval 0.1 / range 500 / feed 10` | **40/40** | 34/40 | 39/40 |

**승패는 세 설정 모두 40/40 완전 거울.** 진영 편향 없음.
소수점 6자리까지 완전 일치하는 판은 34~36/40 으로, 나머지는 부동소수 누적 차이 수준이다
(승자·타워 수는 같고 전투력 끝자리와 충돌 수가 1~2 다르다).

강한 쪽 승률을 같이 봐야 한다. 첫 줄은 21/40 이라 사실상 같은 세기의 봇끼리라
칼날 위 상황이고, 그 상태에서도 거울이 유지된다는 게 이 표의 핵심이다.

### 회귀를 확인하는 방법 — 같은 시드로 전후를 돌린다

수치 표끼리 비교하는 건 회귀 판정이 아니다. 시드가 다르면 값이 크게 흔들린다:
같은 코드에서 **시드 1~10은 판당 충돌 188.3, 시드 1~30은 159.67** 이 나온다.

제대로 하려면 **바꾸기 전 코드의 사본을 만들어 같은 시드로 나란히 돌리고 상태를 대조한다.**
git이 없으니 `game/src/sim/__baseline_sim.ts` 처럼 사본을 두고 두 모듈을 동시에 import해
매 틱 상태 전체를 해시로 먹인 뒤 비교하면 된다 (`sim/` 은 모듈 전역 상태가 없어서 병렬로
돌려도 서로 간섭하지 않는다). 확인 끝나면 사본을 지울 것.

2026-07-28 레벨업 거부 이벤트 추가 때 이 방식으로 확인한 결과:

| 검사 | 결과 |
|---|---|
| 봇 대 봇 30시드, 매 틱 상태 해시 + 최종 상태 JSON | **차이 0** |
| 매 틱 레벨업을 난사해 거부 46,538건을 실제로 유발한 20시드 | **차이 0** |
| 시드 1~10 지표 6종 전후 | **전부 동일** |

### 현재 기준 수치 (봇 10판, BOT_NORMAL 양쪽, 시드 1~10)

| 지표 | 시드 1~10 (2026-07-28 실측) | 시드 1~30 | 옛 표 (시드 미상) |
|---|---|---|---|
| 승부남 / KO | 10/10, 0 | 30/30 | 10/10, 0 |
| 판당 충돌 | 188.3 | 159.7 | 163 |
| 판당 점령 | 51.6 | 52.4 | 46 |
| 판당 중계 | 12.7 | 7.4 | 10 |
| 평균 최대 유닛 | 60.3 | 61.1 | 63.5 |
| 미점령 중립 | 1 | 0.03 | 0 |
| 평균 최고 레벨 | 4.7 | 4.53 | 4.7 |
| 진영 대칭성(승패) | 40/40 (비용 140) | 40/40 (상점 전) | 19/20 |
| 프레임당 렌더 | 0.688ms | — | 0.22ms |

**이 표는 낡았다. 현재 기준 수치는 §-2의 "수정 후" 열이다.**

경위: 2026-07-29 재측정에서 강화 0단계 / 시드 1~30 값이 충돌 175.5 / 점령 46.0 / 중계 6.5 /
최대유닛 65.2 로 나왔다 — §4 "유닛 등급" 절의 "상점 끔" 열과 소수점까지 같아서, 매치 내 상점
제거와 `PlayerMods` 도입으로 인한 회귀는 없다는 것이 확인됐다.
그 뒤 §-2의 배출 쿨다운 수정으로 값이 다시 바뀌었다 (충돌 198.2 / 점령 45.4 / 중계 10.1).

위 표의 "시드 1~30" 열(충돌 159.7 / 점령 52.4)은 훨씬 이전 코드의 값이라 어느 쪽과도
비교하면 안 된다. 강화를 켠 값은 §-0.75의 표를 볼 것.

**옛 표와 값이 다른 것을 회귀로 읽지 말 것.** 옛 표는 시드 기록이 없고, 위에서 보듯
시드 묶음만 바꿔도 충돌이 159~188로 흔들린다. 판정은 위 "회귀를 확인하는 방법" 절대로 할 것.

프레임당 렌더 0.22 → 0.688ms 는 스프라이트 도입분이다. 예산 16.7ms 대비 4.1%.
(Kenney 무채색 + 틴트 0.330ms → Tiny Swords 건물·유닛 애니메이션 0.591ms → 상점 HUD 0.688ms)

---

## 7. 남은 일

### 즉시 이어서 할 것

**에셋 — 조사·정리·렌더러 통합까지 완료.** 전문 + 측정 근거는 `docs/assets-research.md`.

채택: [Tiny Swords](https://pixelfrog-assets.itch.io/tiny-swords) (Pixel Frog).
Kenney 두 팩(무채색 포탑 + 탱크)에서 갈아탔다 — 사용자가 "프로토타입 같다, 진짜 타워 같은 걸로"
요청. Kenney TD 스프라이트는 원래 TD 포탑이라 성탑으로 안 읽혔다.

**라이선스가 CC0가 아니다.** 개인·상용 사용 및 수정은 허용, 크레딧 불필요,
다만 **에셋 재배포·재판매·재포장 금지**. 게임에 넣어 쓰는 건 허용된 용도지만,
에셋 팩 자체를 다시 배포하는 형태는 안 된다. 원문은 `game/public/assets/LICENSE-tiny-swords.txt`.
같은 페이지에서 CC0로 명시된 구버전 zip도 배포하니, 조항이 걸리면 그쪽으로 갈아탈 수 있다.

```
assets-src/            원본 1925파일 15.4MB. game/ 밖이라 빌드에 안 실린다
game/public/assets/    실제 배포물 68파일 0.187MB
  tower/{p1,p2,neutral}/lv1~5.png   진영별 건물 5단계 (15장)
  unit/{p1,p2}/{종류}/run{0..N}.png  유닛 5종 러닝 사이클 (52장, §-4)
  LICENSE-tiny-swords.txt
```

URL은 `/assets/tower/p1/lv1.png` 형태. dev 서버에서 전 경로 **HTTP 200 확인함**.

### 레벨 사다리

건물 5종으로 레벨을 표현한다. 실루엣이 단계마다 확실히 갈리는 조합으로 골랐다.

| 레벨 | 건물 | 원본 크기 |
|---|---|---|
| 1 | House1 (작은 집) | 112×157 |
| 2 | House3 (큰 집) | 122×135 |
| 3 | Barracks (병영) | 184×187 |
| 4 | Tower (석탑) | 120×184 |
| 5 | Castle (성채) | 312×208 |

진영은 `Blue / Red / Black` 폴더를 그대로 쓴다. **중립을 Black으로 둔 이유**: Purple·Yellow는
제3 세력으로 읽히고, Yellow는 `CUT_COLOR` 호박색과 겹친다. Black은 어두운 슬레이트라
파랑과 안 겹치면서 "주인 없음"으로 읽힌다.

### 색을 코드로 입히지 않는다

Tiny Swords는 blue/red/purple/yellow/black 진영 변형이 원본에 들어 있다. 그래서
**이전 Kenney 무채색 시절의 `color` 합성 틴트는 통째로 제거했다.**
대가: 진영색을 바꾸려면 에셋을 다시 뽑아야 한다(코드 한 줄로 안 된다).

### 추출 규칙 — `scratchpad/export_ts.ps1`

원본은 그림 주위 투명 여백이 크다. 추출할 때 알파 경계로 잘라 뒀다.
**유닛 6프레임은 프레임마다 자르면 달릴 때 스프라이트가 튄다.** 6프레임 합집합 상자로
잘라서 전부 같은 크기(93×91)로 맞췄다. 에셋을 다시 뽑을 일이 있으면 이 규칙을 지킬 것.

### 배치 — 3/4 시점이라 원 안에 안 넣는다

Tiny Swords 건물은 위에서 비스듬히 본 그림이라 세로가 길다. Kenney 정탑다운 때처럼
원 안에 욱여넣으면 안 된다. **바닥을 타워 지점(`t.y + r * TOWER_FOOT`)에 대고 세운다.**

- 크기: 높이 상한 `2r × 1.85`, 폭 상한 `2r × 1.75` 중 **빡빡한 쪽**. 높이만 맞추면
  성채(312×208)가 지름의 2.8배까지 벌어져 경로를 덮는다
- 재고 게이지: 정원으로 두르면 링이 건물을 관통한다. **발치에 눕힌 타원**
  (`GROUND_SQUASH = 0.42`)이라야 발판으로 읽힌다. 다 차면 흰색 = 상한 = 중계기
- 재고 숫자: 발치 배지(어두운 원 + 진영색 테두리). 건물 벽돌 무늬 위에 그냥 쓰면 묻힌다
- 유닛: 사람 모양이라 **진행 방향으로 회전시키면 옆으로 눕는다. 좌우 반전만 한다.**
  러닝 사이클은 `time * 12 + u.id * 2` 로 유닛마다 위상을 어긋내 전부 같은 발로 안 뛰게 했다

스프라이트 로딩 전에는 예전 원·도형으로 폴백한다. 지우지 말 것 — 첫 프레임과
헤드리스 검증이 여기 기댄다.

**검증**: 레벨 1~5 × 소유자 3종 15개 강제 배치 렌더 확인, 봇 대 봇 60초 진행 화면 확인,
전 경로 HTTP 200, 콘솔 에러 0, **프레임당 0.591ms** (예산 16.7ms).

game-icons.net 은 안 받았다. 나중에 받으면 **CC BY 3.0이라 크레딧 문구가 필요**하다.

### 미확인 항목

- **아군 보급로 22건(0.26%)에 적 색이 섞인다.** 목적지가 넘어간 직후 등 정상 케이스로 추정하나
  확인 안 함. 180건 → 22건으로 줄인 시점에서 멈췄다
- 충돌 912건 중 2건이 측정으로 설명 안 됨. 코드상 `continue` 가드를 통과할 방법이 없어
  같은 틱에 열린 경로를 측정이 못 잡은 것으로 추정

### 착수 안 한 큰 덩어리

- **서버 권위 시뮬레이션** — 계정은 서버로 갔지만(§-10) **승패와 타워 수는 아직
  클라이언트 보고를 믿는다.** `sim/` 을 서버에서 돌려야 막힌다.
  걸림돌 둘: `$roomTick` 이 200~1000ms라 30Hz를 그대로 못 돌고, `server.js` 단일 파일이
  `game/src/sim/*.ts` 를 import 할 수 없다 — SDK의 "구조화된 서버 프로젝트"로 옮겨야 한다
- **계정 성장 곡선 / 수익화** — 재화와 상점 배관은 붙었다(§-0.5). 남은 것은 §-1의 수치 결정
- **PVE 캠페인 구조** — 강화가 붙는 쪽이 사람뿐이라 스테이지 난이도가 같이 올라야 한다
- **봇에게 중계 운용 가르치기** — ~~봇은 재고 20에서 경로를 열어 성장을 멈추므로 60에 잘
  못 닿는다. 판당 중계가 사실상 0~10이다~~ **이 관찰은 낡았다 (§-35에서 다시 잼).**
  지금은 판당 중계 중앙값 **284회**, 도착 유닛의 **27.9%** 가 중계를 거친다.
  더 가르칠 것이 있는지부터 다시 봐야 한다
- **설계 문서 미작성** — brainstorming 스킬의 마지막 단계
  (`docs/superpowers/specs/YYYY-MM-DD-*-design.md` → writing-plans)를
  사용자가 "일단 프로토타입부터"로 미뤄둔 상태. 섹션 1(매치 내 코어)에서 멈춰 있다

---

## 8. 사용자 작업 스타일

- 한국어로 소통
- 짧고 단정적인 지시를 준다. 애매하면 물어보는 걸 선호
- **사실 확인을 요구한다.** "추측하지말고 사실로 판단해서 보고해" 라고 명시적으로 지시한 적 있음.
  추측이면 추측이라고 밝힐 것
- **밸런스 수치는 대신 재지 말 것.** 2026-07-29에 "승률같은거 측정하지마 내가 테스트해보면되니까"
  라고 중단시켰다. 승률 스윕·강화 단계별 A/B·봇 난이도 조준은 사용자가 직접 플레이해 판단한다.
  헤드리스 검증(§6)은 **정확성** 확인용으로만 쓸 것 — 결정론, 상태 해시 비교, 회귀 0건,
  DOM·저장 형식. 이 문서에 남은 승률 표들은 그 지시 이전에 잰 것이다
- 프로토타입 우선. 설계 승인 게이트를 명시적으로 건너뛰라고 했음
- 기능 제거에 거리낌이 없다. 특화 시스템 전체, 공격력 축 모두 검토 후 잘라냈다
